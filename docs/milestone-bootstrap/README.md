# Milestone Bootstrap — v0.0.127

This current-session bootstrap refresh records the Hisys subsystem role-separation prep. Hisys is defined as `Altas + DARS + Judge`: Altas finds and projects, DARS challenges and improves, and Judge decides and bounds.

The refresh accepts only `hisys_altas_dars_judge_role_separation_recorded`. DARS is retained as a bounded advisory productized baseline, not upgraded to raw provider API readiness, adapter-native readiness, bounded unattended advisory operation readiness, release action authority, credential lookup authority, live external action authority, standing Hisys live-model authority, vault mutation authority, or human-review removal.

The next safe task is `HISYS-ALTAS-DARS-JUDGE-MODULE-SKELETON`, which should add minimal package/module skeletons and public seams without migrating behavior or expanding live authority.

No further tmux session, background agent, external API call, credential lookup, publication, deployment, destructive Git action, remote push, vault mutation, or other externally visible action is authorized by this refresh.

## Legacy checkpoint continuity

Prior checkpoint phrases are retained for tests and resume readers: Hermes child-session smoke was recorded in v0.0.126; live_provider_advisory_smoked is usable only under scoped human review; the DARS panel productization closure returned to the codebase-analysis queue.

See `index.md` for artifact pointers.
