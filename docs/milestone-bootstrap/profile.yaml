selected_profile: develop
detected_profile: develop
target_workspace: /home/cbchoi/workspaces/develop/repos/hisys
version: v0.0.29
focus: M23-LSP-ADAPTER-CLI landed a thin hisys lsp-adapter argparse pass-through over the M23-RED-GREEN runner; next safe row pins a deterministic golden-fixture round-trip
formal_hisys_result: not_run_in_this_bootstrap
local_advisory_result: RALPH_START_READY_WITH_CONTROLS
next_safe_task: M23-LSP-ADAPTER-GOLDEN
remote_push_authorized: false
live_external_action_authorized: false
live_model_call_authorized: false
bootstrap_arguments: "M23-LSP-ADAPTER-CLI"
bootstrap_inference: "Authored docs/plans/m23-cli-lsp-adapter-implementation-tasks.md and added a thin `hisys lsp-adapter --instance <root> --date <YYYYMMDD> --bundle <path> [--current-head-short <hash>]` argparse subparser + `_load_lsp_adapter_bundle` + `_cmd_lsp_adapter` dispatcher inside src/hisys/cli/main.py. The CLI re-imports the M23-RED-GREEN Pydantic models (LspAdapterCommand, LspAdapterRequest) so it does not duplicate shape definitions; the pure runner re-validates every safety invariant. Four focused CLI tests in tests/unit/test_domain_cli.py pin the happy path (monkey-patched subprocess.run returning canned ruff JSON), missing-command rejection, bad-date rejection, and command-not-in-allowlist rejection. No real LSP server is invoked. Next safe row pins a deterministic golden-fixture round-trip against checked-in expected JSON/Markdown."
roadmap_ref: docs/plans/m23-advanced-codebase-adapter-integration-plan.md
next_artifact_ref: docs/plans/m23-golden-lsp-adapter-implementation-tasks.md
planning_baseline_head: "1874ad5 feat: add local lsp adapter"
current_head_at_plan_creation: "1874ad5 feat: add local lsp adapter"
previous_bootstrap_version: v0.0.28
tmux_or_background_agent_spawned: false
production_code_created: true
tests_created: true
