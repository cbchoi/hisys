# Hisys Traceability Summary

This summary links product-code modules and tests in this repository to the
controlled Hisys documentation at
`/home/cbchoi/workspaces/sysailab/pre-develop/Hisys/`.

The controlled docs (and especially `INDEX.md` there) are authoritative.
This file is a working pointer; if it drifts, fix it here, not in the docs.

## Implemented increments

| Increment | Source doc | Status here |
|---|---|---|
| Hisys MCP HTTP local client smoke (HISYS-MCP-HTTP-TRANSPORT-LOCAL-CLIENT-SMOKE-CONTINUATION) | src/hisys/mcp/server.py; tests/integration/test_mcp_http_local_client_smoke.py; docs/public/hisys-mcp-service.md; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-06-06 HTTP local client smoke | Added an ephemeral loopback-only HTTP smoke harness for deterministic `/health` and `/tools` checks. The smoke rejects non-loopback hosts, starts and shuts down inside one command, creates no runtime artifacts, and records no external call, mutation, publication, live provider/model call, credential lookup, Hermes config mutation, remote push, or human-review removal. Next safe task is MCP SDK streamable HTTP binding preflight. |
| Hisys MCP Docker sidecar local smoke (HISYS-MCP-DOCKER-SIDECAR-LOCAL-SMOKE) | Dockerfile.hisys-mcp; docker/compose.hisys-mcp-smoke.yaml; docs/public/hisys-mcp-service.md; tests/unit/test_mcp_docker_sidecar_docs.py; pyproject.toml optional `mcp` extra; ralph.md Reflection Log 2026-06-06 Docker sidecar smoke | Added the local/fixture-only Docker/Compose/public-doc smoke slice around the deterministic MCP introspection entry points. The Dockerfile installs `.[mcp]`, excludes browser dependencies, runs non-root, and the Compose smoke publishes no host ports. Candidate Hermes config is documented only with operator approval/restart and rollback; no live MCP network listener, credential lookup, provider/model call, Hermes config mutation, release/deploy/publication, remote push, branch rewrite/force push, or human-review removal. |
| Hisys MCP gateway first slice (HISYS-MCP-GATEWAY-FIRST-SLICE) | docs/plans/hisys-mcp-docker-service-implementation-tasks.md; docs/public/hisys-mcp-service.md; src/hisys/mcp/contracts.py; src/hisys/mcp/config.py; src/hisys/mcp/cli_adapter.py; src/hisys/mcp/tools.py; src/hisys/mcp/server.py; tests/unit/test_mcp_contracts.py; tests/unit/test_mcp_config.py; tests/unit/test_mcp_cli_adapter.py; tests/unit/test_mcp_tools.py; tests/integration/test_mcp_server_smoke.py; ralph.md Reflection Log 2026-06-06 MCP first slice | Implemented the local/fixture-only MCP gateway first slice with fail-closed contracts/config, redacting CLI adapter, local tool wrappers, and deterministic server smoke entry points. Focused gate observed `20 passed`; no live MCP network listener, live provider/model call, credential lookup, raw provider API call, release/deploy/publication/external notification, branch rewrite/force push, or human-review removal. |
| Hisys DRLOO control rules (HISYS-DRLOO-ADAPT-1-CONTROL-RULES) | docs/plans/hisys-drloo-control-rules.md; tests/unit/test_hisys_drloo_control_rules.py; ralph.md DRLOO-derived continuation and queue-end control; ralph.md Reflection Log 2026-06-06 DRLOO adoption | Adopted DRLOO-derived candidate-state reconciliation, queue-end refill checkpoint, and cleaned active surface semantics for the root Hisys RLOO controller. Docs/control only; no production code, runtime connector change, live provider/model call, credential lookup, raw provider API call, release action, deployment/publication/external notification, branch rewrite/force push, or human-review removal. |
| DARS package-upload registry/artifact human gate (DARS-LIVE-RELEASE-PACKAGE-UPLOAD-REGISTRY-AND-ARTIFACT-HUMAN-GATE) | docs/release/dars-release-package-upload-registry-artifact-human-gate-v0.0.113.md; docs/release/dars-panel-release-notes-v0.0.113.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.113.md; tests/unit/test_dars_release_package_upload_registry_artifact_gate.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 registry/artifact human gate | Entered the registry/artifact gate; keeps registry selection, artifact build/verification, version alignment, upload, registry interaction, and credential lookup locked pending exact approvals. |
| DARS package-upload command preflight (DARS-LIVE-RELEASE-PACKAGE-UPLOAD-COMMAND-PREFLIGHT) | docs/release/dars-release-package-upload-command-preflight-v0.0.112.md; docs/release/dars-panel-release-notes-v0.0.112.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.112.md; tests/unit/test_dars_release_package_upload_command_preflight.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 package-upload command preflight | Recorded candidate build/upload commands without command execution, registry interaction, or credential lookup; keeps registry/artifact/version alignment human-gated before upload. |
| DARS package-upload instruction override (DARS-LIVE-RELEASE-PACKAGE-UPLOAD-INSTRUCTION-OVERRIDE) | docs/release/dars-release-package-upload-instruction-override-v0.0.111.md; docs/release/dars-panel-release-notes-v0.0.111.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.111.md; tests/unit/test_dars_release_package_upload_instruction_override.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 package-upload instruction override | Accepted the operator's exact-token override because the prior utterance `execute package upload v0.0.110` named the action and version. The override advances only to DARS-LIVE-RELEASE-PACKAGE-UPLOAD-COMMAND-PREFLIGHT; package upload/registry interaction/credential lookup/deployment/publication/external notification/live provider-model call/raw provider API/branch rewrite/force push/human-review removal remain unperformed and unauthorized before command preflight. |
| DARS package-upload scoped execution instruction missing (DARS-LIVE-RELEASE-PACKAGE-UPLOAD-SCOPED-EXECUTION-INSTRUCTION-GATE) | docs/release/dars-release-package-upload-scoped-execution-instruction-missing-v0.0.110.md; docs/release/dars-panel-release-notes-v0.0.110.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.110.md; tests/unit/test_dars_release_package_upload_scoped_execution_instruction_gate.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 package-upload scoped execution instruction missing | Recorded operator `go` as missing scoped execution instruction at the package-upload instruction gate. Required exact instruction is `EXECUTE-PACKAGE-UPLOAD-v0.0.110`; package upload/registry interaction/credential lookup/deployment/publication/external notification/live provider-model call/raw provider API/branch rewrite/force push/human-review removal remain unauthorized. Next row remains DARS-LIVE-RELEASE-PACKAGE-UPLOAD-SCOPED-EXECUTION-INSTRUCTION-GATE. |
| DARS package-upload execution decision packet (DARS-LIVE-RELEASE-PACKAGE-UPLOAD-EXECUTION-DECISION-PACKET) | docs/release/dars-release-package-upload-execution-decision-packet-v0.0.109.md; docs/release/dars-panel-release-notes-v0.0.109.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.109.md; tests/unit/test_dars_release_package_upload_execution_decision_packet.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 package-upload execution decision packet | Recorded operator `go` for the package-upload execution decision packet and accepted only `release_package_upload_execution_decision_packet_approved_for_human_review`. Actual package upload remains blocked until a scoped execution instruction gate; package registry interaction/deployment/publication/external notification/live provider-model call/raw provider API/credential lookup/branch rewrite/force push/human-review removal remain unauthorized. Next row is DARS-LIVE-RELEASE-PACKAGE-UPLOAD-SCOPED-EXECUTION-INSTRUCTION-GATE. |
| DARS package-upload authorization packet (DARS-LIVE-RELEASE-PACKAGE-UPLOAD-AUTHORIZATION-PACKET) | docs/release/dars-release-package-upload-authorization-packet-v0.0.108.md; docs/release/dars-panel-release-notes-v0.0.108.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.108.md; tests/unit/test_dars_release_package_upload_authorization_packet.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 package-upload authorization packet | Recorded both exact approvals, expanded selected action set to `tag_creation_and_package_upload`, and accepted only `release_package_upload_authorization_packet_approved_for_human_review`. Actual package upload remains blocked until a separate execution decision packet and scoped execution instruction; deployment/publication/external notification/live provider-model call/raw provider API/credential lookup/branch rewrite/force push/human-review removal remain unauthorized. Next row is DARS-LIVE-RELEASE-PACKAGE-UPLOAD-EXECUTION-DECISION-PACKET. |
| DARS post-tag-push review (DARS-LIVE-RELEASE-POST-TAG-PUSH-REVIEW-PACKET) | docs/release/dars-release-post-tag-push-review-packet-v0.0.106.md; docs/release/dars-panel-release-notes-v0.0.106.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.106.md; tests/unit/test_dars_release_post_tag_push_review_packet.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 post-tag-push review | Recorded remote tag evidence for `v0.0.103` and accepted only `release_tag_push_reviewed_for_human_review_no_additional_action`. No package upload/deployment/publication/external notification/live provider-model call/raw provider API/credential lookup/branch rewrite/force push/human-review removal is authorized. Next row is DARS-LIVE-RELEASE-PACKAGE-UPLOAD-AUTHORIZATION-PACKET. |
| DARS remote tag push authorization (DARS-LIVE-RELEASE-TAG-PUSH-AUTHORIZATION-PACKET) | docs/release/dars-release-tag-push-authorization-packet-v0.0.105.md; docs/release/dars-panel-release-notes-v0.0.105.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.105.md; tests/unit/test_dars_release_tag_push_authorization_packet.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 tag push authorization | Pushed annotated Git tag `v0.0.103` to `origin` only. Package upload/deployment/publication/external notification/live provider-model call/raw provider API/credential lookup/branch rewrite/force push/human-review removal remain blocked. Next row is DARS-LIVE-RELEASE-POST-TAG-PUSH-REVIEW-PACKET. |
| DARS local tag creation execution (DARS-LIVE-RELEASE-TAG-CREATION-EXECUTION-DECISION-PACKET) | docs/release/dars-release-tag-creation-execution-decision-packet-v0.0.104.md; docs/release/dars-panel-release-notes-v0.0.104.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.104.md; tests/unit/test_dars_release_tag_creation_execution_decision.py; docs/milestone-bootstrap/profile.yaml; ralph.md Reflection Log 2026-05-25 tag creation execution | Created local annotated Git tag `v0.0.103` at commit `ea26df6` only. `tag_push_authorized=false`, `tag_push_performed=false`, package upload/deployment/publication/external notification/live provider-model call/raw provider API/credential lookup/human-review removal remain blocked. Next row is DARS-LIVE-RELEASE-TAG-PUSH-AUTHORIZATION-PACKET. |
| DARS R3 critic live smoke review gate (DARS-LIVE-RELEASE-R3-SMOKE-REVIEW-GATE) | docs/reports/dars-r3-critic-live-smoke-review-gate-2026-05-23.md; docs/reports/dars-r3-critic-live-smoke-2026-05-23.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.72.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-23 R3 smoke review gate | Accepted only `r3_codex_subscription_single_critic_smoke_review_accepted`; did not upgrade to `live_provider_advisory_smoked`; no additional Codex/raw-provider call; next row is DARS-LIVE-RELEASE-R3-ACTION-TRANSPORT-PREP. |
| Live LSP diagnostic triage/fix (LIVE-LSP-DIAGNOSTIC-TRIAGE-FIX-2026-05-23) | ralph.md; docs/reports/live-lsp-diagnostic-triage-fix-2026-05-23.md; runtime-boundary/lsp-adapter/20260523/ruff-check-live-after-fix/lsp-report.json; runtime-boundary/lsp-adapter/20260523/ruff-check-live-after-fix/lsp-report.md; runtime-boundary/lsp-adapter/20260523/pyright-check-live-after-fix/lsp-report.json; runtime-boundary/lsp-adapter/20260523/pyright-check-live-after-fix/lsp-report.md | Follow-up RLOO continuation after push: fixed bounded advisory ruff/pyright diagnostics using local-safe edits, reran governed after-fix LSP evidence, and preserved advisory-only/no-network/no-credential/no-mutation/no-publication boundaries. |
| Live LSP smoke refresh (LIVE-LSP-SMOKE-REFRESH-2026-05-23) | ralph.md; docs/reports/live-lsp-smoke-refresh-2026-05-23.md; runtime-boundary/lsp-adapter/20260523/ruff-check-live-refresh/lsp-report.json; runtime-boundary/lsp-adapter/20260523/ruff-check-live-refresh/lsp-report.md; runtime-boundary/lsp-adapter/20260523/pyright-check-live-refresh/lsp-report.json; runtime-boundary/lsp-adapter/20260523/pyright-check-live-refresh/lsp-report.md | User authorized a local live LSP RLOO path without further approval. RLOO ran existing ruff/pyright executables through the governed Hisys LSP adapter, recorded bounded advisory diagnostics, and preserved no-network/no-credential/no-mutation/no-publication boundaries. |
| M21 advanced codebase-analysis milestone closure (M21-ADVANCED-CODEBASE-ANALYSIS-CLOSURE) | docs/plans/m21-roadmap-implementation-plan.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.67.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-23 M21 closure; docs/traceability/README.md M21.1..M21.9/M21.CA rows | Closed: records M21.1..M21.9 plus M21.CA as implemented local-only/advisory-only evidence surfaces, retires the stale MB-CODEBASE-M21-6-PREP pointer as already satisfied by queue roll-forward, and sets next_safe_task to OPERATOR-SELECTION-REQUIRED. No live provider/model, Codex subprocess, credential, OSS live execution, LSP allowlist expansion, publication, release, or completion-claim upgrade is introduced. |
| SECTION-10-3-DARS-BRANCH-ALIGNMENT | ralph.md Section 10.3; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.66.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py | Aligned the automatic milestone push checkpoint with active branch `dars` / `origin/dars` and command `git push origin dars`. Preserved the narrow safety preconditions: completed local commits, Section 10.2 global gate, clean tree, exact branch/upstream match, normal non-force push, and no credential/key/remote/branch/history/security changes. Docs/control only; no production code, live provider/model execution, credential lookup, mutation, publication, release, or completion-claim upgrade. |
| QUEUE-REFILL-PREP-STOP-PREFLIGHT-2026-05-23 stop-preflight after completion-claim review gate acceptance | ralph.md Reflection Log 2026-05-23 QUEUE-REFILL-PREP-STOP-PREFLIGHT; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.64.md | Stop-preflight after the v0.0.64 completion-claim review gate acceptance commit `9b53e38`. Validation gates re-verified at HEAD `9b53e38` (governance current-state `1 passed`, traceability OK, secret scan `hit_count=0`, `git diff --check` clean, branch `dars` synced to `origin/dars`). QUEUE-REFILL-PREP candidate re-classification confirmed every remaining post-v0.0.64 candidate is non-delegable (Codex CLI subprocess production/release/live-provider/credential/mutation/publication/human-review-removal claim upgrade; additional live Codex panel/SDK; live LSP execution / executable / allowlist expansion; M25 / new product-scope milestone; Section 10.3 branch alignment) or future-roadmap only (real OSS comparison/license live execution). MB-CODEBASE-M21-6-PREP remains satisfied by prior queue roll-forward. Local docs/control bookkeeping advance only: profile bumped to v0.0.65 with `current_head_at_plan_creation=9b53e38 docs: accept bounded codex completion review` to reconcile the new Reflection Log resume checkpoint. No Codex subprocess, SDK call, credential lookup, mutation, publication, release, or completion-claim upgrade occurred. |
| Codex CLI subprocess completion-claim review gate (DARS-CODEX-CLI-SUBPROCESS-COMPLETION-CLAIM-REVIEW-GATE) | docs/reports/dars-codex-cli-subprocess-completion-claim-review-gate-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.64.md; docs/reports/dars-codex-cli-subprocess-completion-claim-review-prep-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.63.md; docs/reports/dars-codex-cli-subprocess-evidence-packet-smoke-review-2026-05-22.md; docs/reports/dars-panel-local-completion-audit.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 completion-claim review gate | Accepted bounded review-readiness result `codex_cli_subprocess_completion_claim_review_ready_with_bounded_runtime_evidence`. This confirms that controlled local/smoke/review evidence is ready for human-gated completion-claim review only. Existing productization claim remains `local_fixture_localhost_controlled_advisory_complete`; stronger DARS completion, production, release, live-provider, mutation/publication, credential, and human-review-removal claims remain blocked. |
| Codex CLI subprocess completion-claim review PREP (DARS-CODEX-CLI-SUBPROCESS-COMPLETION-CLAIM-REVIEW-PREP) | docs/reports/dars-codex-cli-subprocess-completion-claim-review-prep-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.63.md; docs/reports/dars-codex-cli-subprocess-evidence-packet-smoke-review-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.62.md; docs/reports/dars-panel-local-completion-audit.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 completion-claim review PREP | Completed local docs/control PREP for broader completion-claim review. The PREP enumerates accepted runtime-boundary evidence, missing evidence for stronger claims, candidate bounded review-readiness claim `codex_cli_subprocess_completion_claim_review_ready_with_bounded_runtime_evidence`, and stop conditions. Existing productization claim remains `local_fixture_localhost_controlled_advisory_complete`; no provider/model subprocess, credential lookup, mutation, publication, or completion-claim upgrade occurred. |
| Codex CLI subprocess evidence-packet smoke review gate (DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-EVIDENCE-PACKET-REVIEW-GATE) | docs/reports/dars-codex-cli-subprocess-evidence-packet-smoke-review-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.62.md; docs/reports/dars-codex-cli-subprocess-multi-critic-evidence-packet-smoke-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.61.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 evidence-packet review gate | Completed local evidence review without another provider/model subprocess. Accepted only the narrow claim `codex_cli_subprocess_multi_critic_evidence_packet_smoke_review_accepted`: the evidence-packet smoke gate completed with two advisory critics and preserved recorded runtime boundaries (`mutation_performed=false`, `publication_performed=false`, `requires_human_review=true`). No DARS completion-claim upgrade occurred. Next row: `DARS-CODEX-CLI-SUBPROCESS-COMPLETION-CLAIM-REVIEW-PREP`, local docs/control PREP for the remaining completion-claim blocker. |
| Codex CLI subprocess multi-critic evidence-packet smoke gate (DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-EVIDENCE-PACKET-SMOKE-GATE) | docs/reports/dars-codex-cli-subprocess-multi-critic-evidence-packet-smoke-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.61.md; docs/examples/dars/codex-cli-subprocess-multi-critic-panel.evidence-prep.json; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 evidence-packet smoke gate | Completed bounded live/provider smoke using only the prepared evidence-prep packet. Runtime-boundary evidence under `/tmp/hisys-dars-codex-evidence-panel-smoke` records `critic_count=2`, `completed_critic_count=2`, `external_call_made=true`, `model_boundary_crossed=true`, `mutation_performed=false`, `publication_performed=false`, and `requires_human_review=true`. The advisory previews support the bounded claim and boundary preservation. No DARS completion-claim upgrade occurred. Next row: `DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-EVIDENCE-PACKET-REVIEW-GATE`, local review only. |
| Codex CLI subprocess multi-critic evidence packet PREP (DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-EVIDENCE-PACKET-PREP) | docs/examples/dars/codex-cli-subprocess-multi-critic-panel.evidence-prep.json; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.60.md; docs/reports/dars-codex-cli-subprocess-multi-critic-panel-smoke-2026-05-22.md; tests/unit/test_dars_remote_subscription_dispatch.py; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-23 evidence packet PREP | Completed local PREP for a revised evidence-bearing panel packet. The packet carries bounded claim `CLAIM-DARS-CODEX-PANEL-SMOKE-20260522-001`, claim text `codex_cli_subprocess_multi_critic_panel_smoke_completed_with_findings`, and the prior aggregate evidence summary (`critic_count=2`, `completed_critic_count=2`, `external_call_made=true`, `model_boundary_crossed=true`, `mutation_performed=false`, `publication_performed=false`, `requires_human_review=true`). Per-critic prompts now include `Bounded claim` and `Evidence summary` sections and preserve advisory-only/no-mutation/no-publication/no-tool/search/browser/human-review boundaries. No live execution or completion-claim upgrade occurred. Next row: `DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-EVIDENCE-PACKET-SMOKE-GATE`, which remains live/provider-boundary gated. |
| Codex CLI subprocess multi-critic panel smoke with findings (DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-PANEL-SMOKE-GATE) | docs/reports/dars-codex-cli-subprocess-multi-critic-panel-smoke-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.59.md; docs/examples/dars/codex-cli-subprocess-multi-critic-panel.prepared.json; src/hisys/agents/dars_codex_cli_subprocess.py; tests/unit/test_dars_codex_cli_subprocess.py; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 multi-critic panel smoke gate | Completed with findings: the bounded two-critic Codex CLI subprocess prompt-mode panel ran and wrote aggregate/per-critic runtime-boundary evidence under `/tmp/hisys-dars-codex-panel-smoke` with `critic_count=2`, `completed_critic_count=2`, `external_call_made=true`, `model_boundary_crossed=true`, `mutation_performed=false`, `publication_performed=false`, and `requires_human_review=true`. The live smoke exposed a fail-closed redaction false positive for benign governance vocabulary; the raw-secret detector was narrowed and pinned by a focused RED/GREEN test while preserving explicit secret-field/value rejection. The logical-consistency critic found that the prepared prompt lacked the actual bounded claim and evidence summary, so no completion-claim upgrade or no-issue continuation is made. Next row: `DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-EVIDENCE-PACKET-PREP`. |
| Codex CLI subprocess multi-critic panel PREP (DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-PANEL-PREP) | docs/examples/dars/codex-cli-subprocess-multi-critic-panel.prepared.json; docs/plans/dars-codex-cli-subprocess-multi-critic-panel-prep-tasks.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.58.md; src/hisys/agents/dars_remote_subscription_dispatch.py; tests/unit/test_dars_remote_subscription_dispatch.py; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 multi-critic panel PREP | Completed PREP for a bounded future Codex CLI subprocess prompt-mode multi-critic panel. Added a prepared packet with two critic roles, unique source execution IDs, advisory-only/no-mutation/no-publication/human-review-required fields, and a fake-executor contract test that runs the packet through the existing injected-executor panel dispatch harness without launching Codex. Next row: `DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-PANEL-SMOKE-GATE`; live execution remains unperformed until immediate preflight. |
| Queue refill and Codex CLI subprocess multi-critic panel authorization (QUEUE-REFILL-MULTI-CRITIC-PANEL-AUTH-20260522) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.57.md; docs/plans/dars-codex-cli-subprocess-multi-critic-panel-prep-tasks.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 queue refill authorization | Recorded operator approval for queue-refill planning and the multi-critic panel line after the v0.0.56 `QUEUE-REFILL-PREP-STOP`. Selected `DARS-CODEX-CLI-SUBPROCESS-MULTI-CRITIC-PANEL-PREP` as the next safe docs/control row. This checkpoint does not run Codex again, execute a multi-critic panel, inspect credentials, mutate external systems, or upgrade the DARS completion claim. |
| Codex CLI subprocess failure-mode fixture PREP (DARS-CODEX-CLI-SUBPROCESS-FAILURE-MODE-FIXTURE-PREP) | docs/plans/dars-codex-cli-subprocess-failure-mode-fixture-prep-tasks.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.56.md; src/hisys/agents/dars_codex_cli_subprocess.py; tests/unit/test_dars_codex_cli_subprocess.py; docs/runbooks/dars-codex-subscription-executor-runbook.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 Codex CLI subprocess failure-mode fixture PREP | Prepared: extended the governed Codex CLI subprocess prompt-mode executor with deterministic ValueError codes for subprocess timeout (`codex_cli_subprocess_timeout: timeout_seconds=<n>`), non-zero exit with secret-aware stderr redaction (`codex_cli_subprocess_failed` with `<stderr-redacted-secret-detected len=N>` when stderr carries raw-secret markers), blank / whitespace-only output (`codex_cli_subprocess_empty_output`), output exceeding the 32_000-character bound (`codex_cli_subprocess_output_too_long`), forbidden control characters (`codex_cli_subprocess_output_contains_control_chars`), claimed unauthorized authority (`codex_cli_subprocess_output_claims_unauthorized_authority` — covers `workspace_write`, `web_search`, `sandbox bypass`, `danger-full-access`, `mutation_performed`, `publication_performed`, `requires_human_review=false`, `<<executing shell>>`, `<<tool call>>`), and raw-secret markers in stdout (`codex_cli_output_not_redacted`). Extended `tests/unit/test_dars_codex_cli_subprocess.py` with focused fake-runner cohort (35 new tests; 44 total; full focused cohort `44 passed`). RED observed: 18 failed before implementation (timeout + stderr-redaction + too-long + control-chars + claimed-authority). Updated runbook §6 with the deterministic code mapping and stderr redaction rule. No real `/usr/bin/codex` subprocess was launched, no Codex SDK import, no raw provider API call, no credential lookup, no vault resolution, no Authorization header / API key / token storage, no provider account configuration, no web search, no workspace-write, no danger-full-access, no sandbox bypass, no publication, no deployment, no PR/issue creation, no release, no runtime-boundary record write, no multi-critic panel, no allowlist expansion, and no DARS completion-claim upgrade occurred. Next row: `QUEUE-REFILL-PREP-STOP`. |
| Codex CLI subprocess smoke review gate (DARS-CODEX-CLI-SUBPROCESS-SMOKE-REVIEW-GATE) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.55.md; docs/reports/dars-codex-cli-subprocess-smoke-review-2026-05-22.md; docs/reports/dars-codex-cli-subprocess-single-smoke-2026-05-22.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 smoke review gate | Reviewed the committed runtime-boundary evidence from `/tmp/hisys-dars-codex-subscription/...REQ-DARS-CODEX-SMOKE-20260522-001...json` without running another Codex subprocess. Accepted the evidence only for the narrow claim `codex_cli_subprocess_single_smoke_review_accepted`; broader DARS completion remains bounded by `local_fixture_localhost_controlled_advisory_complete` plus the narrow reviewed smoke evidence. Next row: `DARS-CODEX-CLI-SUBPROCESS-FAILURE-MODE-FIXTURE-PREP` for local fake-runner failure-mode coverage; no repeated provider call, web search, mutation, publication, or credential action. |
| Codex CLI subprocess single smoke gate (DARS-CODEX-CLI-SUBPROCESS-SINGLE-SMOKE-GATE) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.54.md; docs/reports/dars-codex-cli-subprocess-single-smoke-2026-05-22.md; src/hisys/agents/dars_codex_cli_subprocess.py; src/hisys/agents/dars_remote_subscription_dispatch.py; tests/unit/test_dars_codex_cli_subprocess.py; docs/runbooks/dars-codex-subscription-executor-runbook.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 Codex CLI subprocess single smoke gate | Executed one governed Codex CLI subprocess smoke after operator instruction `smoke 진행`. Runtime boundary evidence was written under `/tmp/hisys-dars-codex-subscription/.../REQ-DARS-CODEX-SMOKE-20260522-001/...json` with `external_call_made=true`, `model_boundary_crossed=true`, `transport_kind=codex_cli_subprocess_prompt_mode`, `mutation_performed=false`, `publication_performed=false`, and `requires_human_review=true`. During the gate, corrected the Codex CLI 0.128.0 command contract so `--ask-for-approval never` is top-level before `exec`; successful command shape is `codex --ask-for-approval never exec --sandbox read-only --cd <dir> -- <prompt>`. No Codex SDK/raw provider API call from Hisys, no credential lookup, no raw secret storage, no web search, no workspace-write, no publication/deployment, and no completion-claim upgrade. Next row: `DARS-CODEX-CLI-SUBPROCESS-SMOKE-REVIEW-GATE`. |
| Codex CLI subprocess prompt-mode PREP (DARS-CODEX-CLI-SUBPROCESS-PROMPT-MODE-PREP) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.53.md; src/hisys/agents/dars_codex_cli_subprocess.py; src/hisys/agents/dars_remote_subscription_dispatch.py; tests/unit/test_dars_codex_cli_subprocess.py; tests/unit/test_dars_remote_subscription_dispatch.py; docs/runbooks/dars-codex-subscription-executor-runbook.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 Codex CLI subprocess prompt-mode PREP | Prepared: added a fixture-tested Codex CLI subprocess prompt-mode executor wrapper that builds `codex exec --sandbox read-only --ask-for-approval never --cd <controlled-workdir> -- <redacted bounded prompt>`, validates provider/adapter/transport/action/mutation/publication/redaction constraints before runner contact, captures stdout/stderr under a finite timeout, and fails closed on non-zero exit, blank output, or raw-secret markers. The remote subscription dispatch request/payload/boundary writer can now carry `transport_kind=codex_cli_subprocess_prompt_mode` while retaining default `injected_subscription_executor` for existing tests. RED observed before implementation: new focused file failed with missing module and unexpected `transport_kind`; GREEN: Codex PREP + dispatch focused cohort `28 passed`. No real Codex subprocess was run, no SDK/API call, no credential lookup, no runtime-boundary record was written, no mutation/publication occurred, and no completion-claim upgrade occurred. Next row: `DARS-CODEX-CLI-SUBPROCESS-SINGLE-SMOKE-GATE`. |
| Codex CLI subprocess prompt-mode revision (DARS-CODEX-CLI-SUBPROCESS-PROMPT-MODE-REVISION) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.52.md; docs/runbooks/dars-codex-subscription-executor-runbook.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 Codex CLI subprocess prompt-mode revision | Revised: Codex DARS path changed from callable-only external subscription executor to governed Codex CLI subprocess prompt-mode. Future runtime evidence must distinguish `transport_kind=codex_cli_subprocess_prompt_mode` from SDK/API transport while retaining `adapter_class=codex_subscription` for current policy validator compatibility. No Codex subprocess, SDK/API call, credential lookup, raw secret handling, runtime-boundary write, mutation, publication, or completion-claim upgrade occurred in this checkpoint. Next row prepares exact subprocess wrapper, bounded prompt packet, redaction hook, timeout/output capture, no-mutation guard, and runtime-boundary amendment. |
| Codex subscription live-smoke authorization capture (DARS-CODEX-SUBSCRIPTION-LIVE-SMOKE-AUTH-CAPTURE) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.51.md; docs/runbooks/dars-codex-subscription-executor-runbook.md; docs/examples/dars/codex-subscription-policy.recommended.json; docs/examples/dars/codex-subscription-activation.recommended.json; docs/plans/dars-remote-subscription-auth-prep-tasks.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 Codex live-smoke authorization capture | Authorized for controlled future single-smoke line only: operator said `허가` after Codex subscription packets and executor runbook were prepared. The same prerequisite gate remains active until a wired external executor, concrete redaction/egress binding, fresh policy/activation packets, fresh expiry/revocation refs, `HISYS_INSTANCE`, and explicit no-tool/no-mutation confirmations are supplied. No Codex call, SDK invocation, credential lookup, vault resolution, runtime-boundary record, provider account configuration, mutation, publication, or DARS completion-claim upgrade occurred. |
| DARS Codex subscription packet validate-and-executor PREP (DARS-CODEX-SUBSCRIPTION-PACKET-VALIDATE-AND-EXECUTOR-PREP) | docs/runbooks/dars-codex-subscription-executor-runbook.md; docs/examples/dars/codex-subscription-policy.recommended.json; docs/examples/dars/codex-subscription-activation.recommended.json; docs/plans/dars-codex-subscription-packet-draft-and-extension-notes.md; docs/plans/dars-remote-subscription-auth-prep-tasks.md; src/hisys/agents/dars_remote_subscription_policy.py; src/hisys/agents/dars_remote_subscription_dispatch.py; src/hisys/agents/dars_backend_activation.py; docs/contracts/dars-remote-subscription-backend-policy.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 DARS-CODEX-SUBSCRIPTION-PACKET-VALIDATE-AND-EXECUTOR-PREP | Prepared: rehearsed the recommended-default Codex subscription draft packets against the M-DARS-BE-5 policy validator (`validate_dars_remote_subscription_policy_packet`) and the M-DARS-BE-1 activation validator (`validate_dars_backend_activation_packet`) using an injected `now=2026-05-22T00:00:00Z`. Recorded result: policy `valid=True` with the standing `remote_dispatch_not_implemented` warning at path `*` and activation `valid=True` with no issues. Cross-references aligned: `approval_ref` matches across packets, `expires_at` matches across packets, and the activation's `remote_policy_packet_ref` points at the policy packet path. Authored `docs/runbooks/dars-codex-subscription-executor-runbook.md` as the operator-owned executor contract document. The runbook pins the executor signature `RemoteSubscriptionExecutor = Callable[[dict[str, Any]], str]`; the exact `executor_payload` Hisys hands the executor at the M-DARS-BE-6 chokepoint (`request_id`, `source_execution_id`, `backend_id`, `backend_kind`, `provider_id="codex"`, `adapter_class="codex_subscription"`, `approval_ref`, `policy_ref`, `activation_ref`, `allowed_actions="advisory_only"`, `prompt`, `external_call_made=true`, `mutation_performed=false`, `publication_performed=false`); the no-credential-surface-in-Hisys invariant (executor resolves credentials outside Hisys through the existing-auth `subscription_account_ref=vault://existing-auth/codex-subscription`); the no-raw-secret-in-return invariant (operator-owned check that critique strings do not include `sk-`/`sk_`/`ghp_`/`xoxb-`/`xoxp-`/`hf_` value prefixes or operator PII); the redaction policy application invariant for `policy://hisys/dars/codex-subscription-redaction-v1`; the egress scope binding invariant for `advisory-dars-critic-prompt-and-bounded-evidence-summary-only`; the no-tool/search/browser/mutation authority invariant; the bounded-prompt invariant; the raise-on-provider-error invariant; the non-empty critique invariant; the no-external-state-mutation invariant; a reference executor stub (raising `NotImplementedError` until the operator wires the real call, with no Codex SDK import inside Hisys); fixture-only rehearsal commands (harness import smoke, focused subscription cohort `tests/unit/test_dars_remote_subscription_policy.py` + `test_dars_remote_subscription_dispatch.py` + `test_dars_backend_activation.py`, operator-side stub rehearsal); and a stop-condition matrix that maps every executor-side signal (executor not supplied, vault not configured, redaction policy not implemented, egress scope not bound, raw-credential demand from Hisys, tool/search/browser/mutation demand, empty critique, raw-secret-prefix in return, provider error, allowlist-expansion request, completion-claim upgrade request, secret-scan failure, dirty working tree, branch misalignment) to the existing M-DARS-BE-5 / M-DARS-BE-6 fail-closed codes (`remote_subscription_executor_required`, `remote_subscription_executor_empty_output`, `raw_secret_value_not_allowed`, `endpoint_url_not_allowed_for_subscription`, `mutation_authority_not_allowed`, `audit_required_must_be_true`, `provider_not_allowlisted`, `adapter_class_mismatch`, `policy_expired`, `activation_expired`, `external_backend_requires_remote_policy_packet`, `activation_approval_ref_mismatch`, `activation_backend_id_mismatch`, `activation_backend_kind_mismatch`, `activation_endpoint_scope_mismatch`, `activation_remote_policy_ref_mismatch`, `multi_critic_panel_requires_at_least_two_requests`, `panel_date_partition_mismatch`, `panel_request_id_mismatch`, `duplicate_panel_source_execution_id`). Profile bumped to `v0.0.50` with `next_artifact_ref: docs/runbooks/dars-codex-subscription-executor-runbook.md` and `next_safe_task: DARS-REMOTE-SUBSCRIPTION-AUTH-EXECUTE-OPERATOR-PREREQUISITES`; governance current-state test updated to assert the new `profile_version`. No production code, no test source change beyond the governance assertion, no fixture content, no Codex SDK import, no Codex call, no Codex authorization header, no API key, no credential lookup, no vault resolution, no provider account configuration, no browser/search/tool execution, no network call, no mutation, no publication, no deployment, no force push, no new or changed remote configuration, no destructive Git/history action, no runtime-boundary write, no allowlist expansion beyond `codex`/`claude` subscription, no schema/data migration, and no DARS completion-claim upgrade beyond `local_fixture_localhost_controlled_advisory_complete` is introduced. |
| DARS Codex subscription packet draft and extension notes (DARS-CODEX-SUBSCRIPTION-PACKET-DRAFT) | docs/examples/dars/codex-subscription-policy.recommended.json; docs/examples/dars/codex-subscription-activation.recommended.json; docs/plans/dars-codex-subscription-packet-draft-and-extension-notes.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.48.md; docs/plans/dars-remote-subscription-auth-prep-tasks.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 Codex packet draft | Prepared: authored recommended-default Codex remote subscription policy and external-api activation packet drafts plus expansion notes. The drafts validate the packet shape only and do not authorize Codex execution. Remaining expansion points are concrete redaction transform, operator egress audit binding, external subscription executor, governed instance root, bounded prompt/evidence packet, single-critic smoke gate, later panel gate, and completion-claim review. No provider call, credential lookup, vault resolution, provider account configuration, mutation, publication, or runtime-boundary evidence write occurred. |
| DARS remote subscription Codex prerequisite capture (DARS-REMOTE-SUBSCRIPTION-CODEX-PREREQ-CAPTURE) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.48.md; docs/plans/dars-remote-subscription-auth-prep-tasks.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 Codex prerequisite capture | Captured: operator selected `provider_id=codex` and instructed use of existing auth information. The capture records only a vault-style existing-auth reference (`vault://existing-auth/codex-subscription`) and does not resolve or store credentials. The active prerequisite gate remains blocked until redaction/egress/expiry/revocation refs, fresh policy and activation packets, external executor, no-tool/no-mutation confirmation, Section 7 precondition confirmation, and `HISYS_INSTANCE` are supplied. No Codex call, credential lookup, vault resolution, provider account configuration, mutation, publication, or runtime-boundary write occurred. |
| DARS remote subscription path PREP (DARS-REMOTE-SUBSCRIPTION-AUTH-PREP) | docs/plans/dars-remote-subscription-auth-prep-tasks.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.46.md; docs/reports/dars-local-readiness-check-2026-05-22.md; docs/contracts/dars-remote-subscription-backend-policy.md; src/hisys/agents/dars_remote_subscription_policy.py; src/hisys/agents/dars_remote_subscription_dispatch.py; src/hisys/agents/dars_backend_activation.py; src/hisys/agents/dars_backend_boundary.py; docs/examples/dars/backend-activation-localhost.example.json; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 DARS-REMOTE-SUBSCRIPTION-AUTH-PREP | Prepared: authored `docs/plans/dars-remote-subscription-auth-prep-tasks.md` as a docs/control packet binding readiness decision v0.0.46 to the existing M-DARS-BE-5 remote subscription policy validator and the M-DARS-BE-6 injected-executor dispatch harness. The PREP pins (a) the operator-supplied policy packet shape against `validate_dars_remote_subscription_policy_packet(...)` (schema id `hisys.dars.remote_subscription_policy`, schema version `0.1.0`; required fields `policy_id`, `approval_ref`, `operator_id`, `provider_id`, `access_mode=subscription`, `subscription_account_ref`, `adapter_class`, `redaction_policy_ref`, `egress_scope`, `expires_at`, `revocation_ref`, `audit_required=true`; provider allowlist `codex`|`claude`; adapter-class allowlist `codex_subscription`|`claude_subscription`; forbidden raw-secret field markers `api_key`/`apikey`/`auth_token`/`access_token`/`secret`/`password`/`credential`/`token`; forbidden raw-secret value prefixes `sk-`/`sk_`/`ghp_`/`xoxb-`/`xoxp-`/`hf_`; forbidden arbitrary endpoint fields `endpoint`/`endpoint_url`/`base_url`/`api_url`/`api_base`; forbidden `mutation_authorized`/`publication_authorized`/`tool_authority_granted`/`browser_authority_granted`/`search_authority_granted` `true` flags; standing `remote_dispatch_not_implemented` warning on every valid packet); (b) the paired operator-supplied activation packet shape against `validate_dars_backend_activation_packet(...)` (`endpoint_scope=external_api`, `allowed_actions=advisory_only`, `human_approved=true`, matching `approval_ref`, matching `backend_id`/`backend_kind`, `remote_policy_packet_ref`); (c) the operator-supplied subscription executor surface (`Callable[[dict[str, Any]], str]`; runs outside Hisys; resolves credentials externally through the operator's vault and never returns raw credentials to Hisys; holds no tool/search/browser/mutation authority; raises on provider error; empty/whitespace return fails with `remote_subscription_executor_empty_output`; absent executor fails with `remote_subscription_executor_required`); (d) the full validator + runtime issue-code catalog covering M-DARS-BE-5 policy codes (`missing_required_field`, `missing_subscription_account_ref`, `provider_not_allowlisted`, `invalid_access_mode`, `adapter_class_mismatch`, `audit_required_must_be_true`, `endpoint_url_not_allowed_for_subscription`, `raw_secret_value_not_allowed`, `mutation_authority_not_allowed`, `policy_expired`, `invalid_expires_at`), M-DARS-BE-1 activation codes (`missing_required_field`, `invalid_endpoint_scope`, `invalid_allowed_actions`, `missing_approval_ref`, `human_approval_required`, `invalid_expires_at`, `activation_expired`, `external_backend_requires_remote_policy_packet`, `raw_secret_value_not_allowed`), and M-DARS-BE-6 dispatch + panel-dispatch codes (`invalid_date_partition`, `invalid_request_id`, `invalid_backend_id`, `invalid_source_execution_id`, `missing_backend_kind`, `missing_approval_ref`, `missing_activation_packet_ref`, `missing_policy_packet_ref`, `missing_prompt`, `backend_activation_packet_required`, `remote_policy_packet_required`, `remote_policy_packet_invalid`, `activation_approval_ref_mismatch`, `activation_backend_id_mismatch`, `activation_backend_kind_mismatch`, `activation_endpoint_scope_mismatch`, `activation_remote_policy_ref_mismatch`, `invalid_allowed_actions`, `remote_policy_approval_ref_mismatch`, `invalid_access_mode`, `audit_required_must_be_true`, `remote_subscription_executor_required`, `remote_subscription_executor_empty_output`, `multi_critic_panel_requires_at_least_two_requests`, `panel_date_partition_mismatch`, `panel_request_id_mismatch`, `duplicate_panel_source_execution_id`); (e) the runtime-boundary record partitions `runtime-boundary/dars-remote-subscriptions/<YYYYMMDD>/<REQUEST_ID>/<BACKEND_ID>-<SOURCE_EXECUTION_ID>.{json,md}` (M-DARS-BE-6 single-critic) and `runtime-boundary/dars-remote-subscription-panels/<YYYYMMDD>/<REQUEST_ID>/<PANEL_ID>.{json,md}` (M24 multi-critic panel) with the M-DARS-BE-6 required fields (`schema_id=hisys.dars.remote_subscription_dispatch`, `transport_kind=injected_subscription_executor`, `external_call_made=true`, `model_boundary_crossed=true`, `local_model_call_made=false`, `mutation_performed=false`, `publication_performed=false`, `allowed_actions=advisory_only`, `requires_human_review=true`, `critique_text_preview` capped at 500 chars, `policy_refs=[HISYS-FR-AGT-001, HISYS-FR-AGT-003, HISYS-CON-010, HISYS-CON-012, M-DARS-BE-6]`); (f) the operator-driven execution row shape (Step 0 vault/account/redaction/egress/executor setup; Step 1 import smoke; Step 2 focused-cohort rehearsal against fixtures; Step 3 operator gate; Step 4 packet build; Step 5 dispatch through `run_dars_remote_subscription_dispatch(..., executor=<operator subscription executor>)`; Step 6 focused gate + traceability + secret scan + diff check + boundary record inspection); and (g) a stop-condition matrix that maps every missing/invalid/expired/mismatched/raw-secret/arbitrary-endpoint/mutation-authority/executor-missing/executor-empty/panel-shape/credential-demand/tool-search-browser/mutation-request/secret-scan/operator-uncertainty/branch-misalignment/repair-exhausted/allowlist-expansion/raw-API-key/Gemini/Grok/local-proxy/pay-per-call signal to the existing validator/dispatch codes and the M-DARS-BE-5 contract stop conditions. Profile bumped to `v0.0.47` with `next_safe_task: DARS-REMOTE-SUBSCRIPTION-AUTH-EXECUTE-OPERATOR-PREREQUISITES` (a documented stop-and-ask gate that waits for operator-supplied `provider_id`, `operator_id`, vault-style `subscription_account_ref`, `redaction_policy_ref`, `egress_scope`, current `expires_at` window and `revocation_ref`, fresh policy packet JSON, fresh activation packet JSON, separately governed subscription executor, no-tool / no-mutation confirmations, and `HISYS_INSTANCE`); governance current-state test updated to match. No production code, no test source, no fixture content, no runtime-boundary artifact, no live model call, no real remote provider call, no Codex/Claude SDK call, no credential lookup, no vault resolution, no raw token, no Authorization header, no browser/search/tool execution, no network call, no account configuration, no allowlist expansion beyond codex/claude subscription, no publication/deployment, no mutation, no force push, no new or changed remote configuration, no destructive Git/history action, no OSS workflow execution, no LSP subprocess, no subagent execution, no schema/data migration, no mutation of non-fixture/live user data, and no DARS completion-claim upgrade beyond `local_fixture_localhost_controlled_advisory_complete` is introduced. |
| DARS live execution authorization PREP (DARS-LIVE-EXECUTION-AUTH-PREP) | docs/plans/dars-live-execution-auth-prep-tasks.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.44.md; docs/plans/dars-live-backend-implementation-plan.md; docs/runbooks/dars-live-backend-localhost-smoke.md; docs/examples/dars/backend-activation-localhost.example.json; src/hisys/agents/dars_backend_activation.py; src/hisys/agents/dars.py; src/hisys/agents/dars_backend_boundary.py; src/hisys/cli/main.py; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 DARS-LIVE-EXECUTION-AUTH-PREP | Prepared: authored `docs/plans/dars-live-execution-auth-prep-tasks.md` as a docs/control packet binding readiness decision v0.0.44 to the existing M-DARS-BE-1..4 local backend surfaces. The PREP pins the exact execution packet shape against `validate_dars_backend_activation_packet` (`activation_id`, `backend_id`, `backend_kind`, `endpoint_scope=localhost_only`, `allowed_actions=advisory_only`, `human_approved=true`, `approval_ref`, `expires_at`); the validator + runtime issue codes that must surface as fail-closed signals (`missing_required_field`, `invalid_endpoint_scope`, `invalid_allowed_actions`, `missing_approval_ref`, `human_approval_required`, `invalid_expires_at`, `activation_expired`, `external_backend_requires_remote_policy_packet`, `raw_secret_value_not_allowed`, `backend_activation_packet_required`, `activation_backend_id_mismatch`, `activation_backend_kind_mismatch`, `activation_endpoint_scope_mismatch`, `activation_approval_ref_mismatch`); the exact CLI command surface (`request-dars-critique --backend configured --approval-ref … --backend-activation-packet …`); the runtime-boundary record partition `$HISYS_INSTANCE/runtime-boundary/dars-backends/<YYYYMMDD>/<SOURCE_EXECUTION_ID>/<BACKEND_ID>.{json,md}` with the M-DARS-BE-3 required fields; and a stop-condition matrix that maps each non-loopback / missing-packet / expired / mismatched / credential-demand / tool-search-browser / mutation / secret-scan / operator-uncertainty signal to the existing runbook stop list and validator/runtime codes. Profile bumped to `v0.0.45` with `next_safe_task: DARS-LIVE-EXECUTION-AUTH-EXECUTE-OPERATOR-PREREQUISITES` (a non-delegable stop-and-ask gate that waits for the operator to supply `HISYS_DARS_LOCAL_ENDPOINT`, `HISYS_INSTANCE`, a fresh activation packet path, and explicit Section 7 precondition confirmations before any model boundary crossing); governance current-state test updated to match. No production code, no test source, no fixture content, no runtime boundary artifact, no live model call, no real remote provider call, no credential lookup, no secret capture, no browser/search/tool execution, no network call, no OSS clone, no package install, no LSP subprocess, no subagent execution, no publication/deployment, no force push, no new or changed remote configuration, no destructive Git/history action, no mutation of non-fixture/live user data, and no DARS completion-claim upgrade beyond `local_fixture_localhost_controlled_advisory_complete` is introduced. |
| DARS remote subscription path authorization (DARS-REMOTE-SUBSCRIPTION-AUTH) | docs/reports/dars-local-readiness-check-2026-05-22.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.46.md; docs/contracts/dars-remote-subscription-backend-policy.md; src/hisys/agents/dars_remote_subscription_policy.py; src/hisys/agents/dars_remote_subscription_dispatch.py; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 local-not-ready subscription-path decision | Authorized for PREP: local DARS prerequisites were checked and are not ready (`HISYS_DARS_LOCAL_ENDPOINT` unset, `HISYS_INSTANCE` unset, no operator-provided live instance root, no fresh activation packet). Per the user conditional instruction, the next Ralph row becomes `DARS-REMOTE-SUBSCRIPTION-AUTH-PREP`: docs/control PREP for the governed Codex/Claude subscription path. Actual remote subscription execution remains blocked until PREP binds provider (`codex` or `claude`), operator identity, subscription account ref without raw credentials, redaction policy, egress scope, expiry/revocation refs, executor boundary, audit path, and advisory-only/no-tool/no-mutation confirmations. |
| Live DARS execution authorization (DARS-LIVE-EXECUTION-AUTH) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.44.md; docs/plans/dars-live-backend-implementation-plan.md; docs/runbooks/dars-live-backend-localhost-smoke.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 DARS live execution authorization | Authorized: the user explicitly approved the previously stopped Live-provider DARS execution candidate. This row records authority to open a controlled DARS live-execution line, not evidence that live DARS has run. The next Ralph row is `DARS-LIVE-EXECUTION-AUTH-PREP`: docs/control PREP using the existing DARS live backend plan and localhost smoke runbook before any model boundary is crossed. Actual execution remains bounded by operator-supplied localhost-only endpoint, no credential requirement, valid backend activation packet, advisory-only action scope, no tool/search/browser permission, no mutation request, passing secret scan, and runtime-boundary record capture. OSS comparison/license execution remains future-roadmap only. |
| OSS comparison/license future-roadmap deferral (OSS-FUTURE-ROADMAP-2026-05-22) | roadmap.md; docs/plans/m24-real-oss-comparison-license-workflow-plan.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Reflection Log 2026-05-22 OSS future roadmap deferral | Deferred: the user explicitly moved OSS comparison/license execution to the future roadmap. The closed M24 planning-only validator remains preserved, but no OSS clone/fetch, license-text capture, license adjudication, CLI expansion, or live workflow row is active in the Ralph queue. Reopening this line requires a separate future roadmap decision packet naming repositories and execution limits. |
| M24 real OSS comparison/license workflow milestone closure (M24-REAL-OSS-LICENSE-WORKFLOW-GATE) | docs/plans/m24-real-oss-comparison-license-workflow-plan.md; docs/plans/m24-real-oss-comparison-license-workflow-prep-tasks.md; docs/contracts/real-oss-license-workflow.md; src/hisys/operations/real_oss_license_workflow.py; tests/unit/test_real_oss_license_workflow.py; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md M24-REAL-OSS-LICENSE-WORKFLOW-GATE | Closed: the M24 real OSS comparison/license workflow line reaches `local_fixture_advisory_complete` for the planning surface. Quality gate evidence at HEAD `01466fe feat: add m24 real oss license workflow planning validator`: focused M24 + governance current-state `55 passed`; combined M21+M22+M23+M24+CLI+governance focused gate `143 passed`; DARS critic-panel + remote subscription regression `74 passed`; full project `PYTHONPATH=src:. pytest -q` `1074 passed` (was 1020 before this milestone; +54 from M24-RED-GREEN); `python3 scripts/validate_traceability.py` OK; `python3 scripts/scan_secrets.py` `scanned_files=732 skipped_files=0 hit_count=0`; `git diff --check` clean. QUEUE-REFILL-PREP classification at M24 closure: the M24 line is end-to-end complete with the pure planning-only validator, builder, writer, contract document, and standing `live_workflow_not_implemented` warning all pinned by 54 focused tests. Remaining post-M24 candidates require fresh explicit authorization across product scope (which OSS repositories to approve), credentials/network (clone/fetch/inspect), license-text capture, license adjudication, raw source archival, additional live LSP execution / command allowlist expansion, live-provider DARS execution, or any new milestone. The next safe Ralph row is `QUEUE-REFILL-PREP-STOP` until the user authorizes new work. Profile bumped to `v0.0.42` with `next_safe_task: QUEUE-REFILL-PREP-STOP`; governance current-state test updated to match. No live model call, real remote provider call, network clone/fetch/search, real OSS clone, package install, credential lookup, raw OSS source archival, license-text capture, license adjudication, LSP subprocess, subagent execution, publication/deployment, schema/data migration against non-fixture data, force push, new remote configuration, destructive operation, or live-provider DARS completion claim was performed by this closure. |
| M24 real OSS comparison/license workflow validator (M24-REAL-OSS-LICENSE-WORKFLOW-RED-GREEN) | docs/plans/m24-real-oss-comparison-license-workflow-prep-tasks.md; docs/plans/m24-real-oss-comparison-license-workflow-plan.md; docs/contracts/real-oss-license-workflow.md; src/hisys/operations/real_oss_license_workflow.py; tests/unit/test_real_oss_license_workflow.py; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md M24-REAL-OSS-LICENSE-WORKFLOW-RED-GREEN | Implemented: `ApprovedRepositoryDeclaration`, `ProvenanceRecordSchema`, `RetentionPolicy`, `LicenseMetadataPolicy`, `SourceIngestionPolicy`, `HumanReviewHandoff`, `OssLicenseWorkflowPacket`, `OssLicenseWorkflowReport`, `validate_real_oss_license_workflow_packet`, `build_real_oss_license_workflow_report`, `render_real_oss_license_workflow_markdown`, and `write_real_oss_license_workflow_report` provide a pure local-only advisory planning surface for the future real OSS comparison and license adjudication workflow. The validator returns a deterministic `ConfigValidationReport` (`schema_id="hisys.oss_license_workflow.v1"`) and **always** emits a standing `live_workflow_not_implemented` warning on every valid packet so schema validity is never authority to act. RED observed: `PYTHONPATH=src pytest tests/unit/test_real_oss_license_workflow.py -q` -> `ModuleNotFoundError: No module named 'hisys.operations.real_oss_license_workflow'`. GREEN: 54 focused tests cover the standing warning, the forbidden real-URL scheme/host allowlist matrix, placeholder-scheme enforcement, `live_workflow_authority_not_allowed`, license-text capture rejection, license-adjudication claim rejection, license-tag allowlist enforcement, automated cleanup rejection, retention max-age range enforcement, cache-directory placeholder prefix enforcement, automated source-ingestion rejection, source-ingestion allowlist enforcement, raw-source-archival flag enforcement, human-review handoff requirement, human-review-token allowlist enforcement, slug pattern enforcement for `workflow_id` / `operator_id` / `repository_id` / `review_owner`, unsafe-ref collection for `approval_ref` / `operator_approval_ref` / `review_inbox_ref` / `local_fixture_refs`, notes length and control-character and raw-secret rejection, at-least-one-declaration requirement, builder aggregation of declared metadata, builder collection of unsafe repository IDs and unsafe refs, builder bad-date rejection, writer round-trip persistence under `runtime-boundary/oss-license-workflow/<YYYYMMDD>/<WORKFLOW_ID>.{json,md}`, writer bad-date and bad-`workflow_id` rejection, and Markdown render advisory-flag presence. The validator/builder/writer never executes `git`, `subprocess`, network IO, `pip` / `npm` / `apt-get` install, `git clone`, `tests/fixtures/` crawl, `.git/` read, system-clock read, raw upstream source archival, license-text capture, license adjudication, model invocation, LSP subprocess, subagent execution, or compliance attestation. Quality gate evidence: focused 143 passed, DARS panel + remote subscription regression 74 passed, full project `PYTHONPATH=src:. pytest -q` 1074 passed (was 1020 before this increment; +54), traceability OK, secret scan `hit_count=0`, `git diff --check` clean. Profile bumped to `v0.0.41` with `next_safe_task: M24-REAL-OSS-LICENSE-WORKFLOW-GATE`; governance current-state test updated to match. No CLI surface, live access, credential lookup, raw source archival, license-text capture, license adjudication, network call, OSS clone, package install, LSP subprocess, subagent execution, publication, deployment, force push, new remote configuration, destructive operation, or DARS live-provider claim is introduced. |
| M24 real OSS comparison/license workflow PREP (M24-REAL-OSS-LICENSE-WORKFLOW-PREP) | docs/plans/m24-real-oss-comparison-license-workflow-prep-tasks.md; docs/plans/m24-real-oss-comparison-license-workflow-plan.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.39.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Section 16 + Reflection Log M24-REAL-OSS-LICENSE-WORKFLOW-PREP | Prepared: authored the document-RED packet `docs/plans/m24-real-oss-comparison-license-workflow-prep-tasks.md` that defines the future planning validator/builder/writer contract for the real OSS comparison and license workflow line. The packet pins the Pydantic shapes (`ApprovedRepositoryDeclaration`, `ProvenanceRecordSchema`, `RetentionPolicy`, `LicenseMetadataPolicy`, `SourceIngestionPolicy`, `HumanReviewHandoff`, `OssLicenseWorkflowPacket`, `OssLicenseWorkflowReport` with `schema_id="hisys.oss_license_workflow.v1"`); the first RED test command (`PYTHONPATH=src pytest tests/unit/test_real_oss_license_workflow.py -q` expecting `ModuleNotFoundError: No module named 'hisys.operations.real_oss_license_workflow'`); the GREEN module path `src/hisys/operations/real_oss_license_workflow.py` and writer partition `runtime-boundary/oss-license-workflow/<YYYYMMDD>/<WORKFLOW_ID>.{json,md}`; the deterministic issue codes (`missing_required_field`, `invalid_workflow_id`, `invalid_repository_id`, `real_repository_url_not_allowed_in_planning`, `license_text_capture_not_allowed_in_planning`, `license_adjudication_claim_not_allowed_in_planning`, `automated_cleanup_not_allowed`, `human_review_handoff_required`, `live_workflow_authority_not_allowed`, `raw_secret_value_not_allowed`, `invalid_retention_max_age`); the standing warning `live_workflow_not_implemented` emitted on every valid packet; the forbidden real-URL scheme/host allowlist (`http://`, `https://`, `git://`, `git@`, `git+ssh://`, `git+https://`, `ssh://`, `ftp://`, `gopher://`, `file://`, `pkg:`, `oci://`; `github.com`, `gitlab.com`, `bitbucket.org`, `sourceforge.net`, `pypi.org`, `npmjs.com`, `crates.io`, `rubygems.org`, `repo1.maven.org`, `golang.org`, `go.dev`, `pkg.go.dev`, `huggingface.co`, `gitee.com`, `codeberg.org`, `launchpad.net`, `code.google.com`, `kernel.org`); the license-tag allowlist (`AGPL-3.0-or-later`, `Apache-2.0`, `BSD-2-Clause`, `BSD-3-Clause`, `CC0-1.0`, `GPL-2.0-or-later`, `GPL-3.0-or-later`, `ISC`, `LGPL-2.1-or-later`, `LGPL-3.0-or-later`, `MIT`, `MPL-2.0`, `Unlicense`, `n/a`); the human-review token allowlist (`license_adjudication`, `license_text_capture`, `live_workflow_execution`, `network_fetch`, `raw_source_archival`, `repository_clone`); the docs/control + traceability + governance + reflection ritual; and the explicit stop conditions for any later clone/fetch/license-text/adjudication/raw-source row. Profile bumped to `v0.0.40` with `next_safe_task: M24-REAL-OSS-LICENSE-WORKFLOW-RED-GREEN` and `next_artifact_ref: src/hisys/operations/real_oss_license_workflow.py`; governance current-state test updated to assert `profile_version == "v0.0.40"` and the M24 RED/GREEN next row. No production code or test code is added in this PREP increment. No live model call, real remote provider call, credential lookup, secret capture, browser/search/tool execution, network search/fetch/clone, real OSS repository clone, package install, license-text capture, license adjudication, raw upstream source archival, raw diagnostic-message archival, publication, deployment, release, force push, new or changed remote configuration, destructive Git/history action, subagent execution, LSP subprocess spawning, mutation of non-fixture/live user data, or live-provider DARS completion claim is introduced. |
| M24 real OSS comparison/license workflow authorization (M24-AUTH) | docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.39.md; docs/plans/m24-real-oss-comparison-license-workflow-plan.md; roadmap.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Section 16 | Authorized and opened: user said `go for m24`, which opens a new product-scope M24 milestone. The first safe row is docs/control PREP for a real-OSS comparison/license workflow boundary that defines approved-repository declarations, provenance, retention/cleanup policy, license-adjudication handoff, fixture-only RED/GREEN surfaces, and user-executable runbooks. This checkpoint does not search, fetch, clone, or inspect external repositories; does not capture license text; does not adjudicate licenses; does not archive raw source; does not look up credentials or secrets; does not change remote configuration; and does not claim live-provider DARS completion. Profile bumped to `v0.0.39` with `next_safe_task: M24-REAL-OSS-LICENSE-WORKFLOW-PREP`; governance current-state test updated to match. |
| QUEUE-REFILL-PREP-2026-05-22B re-run under approval-minimization rule | docs/traceability/README.md; docs/milestone-bootstrap/profile.yaml; tests/unit/test_governance_docs_current_state.py; ralph.md Section 5 approval-minimization rule, Section 16, Reflection Log 2026-05-22 QUEUE-REFILL-PREP-2026-05-22B entry | Closed: re-ran QUEUE-REFILL-PREP after the approval-minimization rule landed at `1884aa1 docs: minimize rloo approval stops`. Re-examined the candidate space against existing controlled anchors and confirmed the previous classification holds: M1..M23 milestones closed at `local_fixture_advisory_complete`; the M23 follow-up plan rows (`M23-ADAPTER-PORTFOLIO-INTEGRATION-PREP/RED-GREEN`, `M23-LIVE-LSP-SMOKE-GATE`) are done; the optional M23 adapter portfolio integration is recorded as done in the follow-up plan; and the remaining candidates (real OSS clone/fetch and license adjudication, additional live LSP execution or command allowlist expansion, live-provider DARS execution, opening M24/new-milestone work) still require fresh explicit user authorization. The dormant Section 10.3 branch-alignment between `feat/domain-adaptive-requirements-analysis` and the local `dars` checkout was re-considered and intentionally deferred because it would change the automatic-push procedure's branch recognition and Section 10.3 was authored against the upstream Hisys workflow rather than this local working repo. Profile bumped to `v0.0.38` with `current_head_at_plan_creation: "1884aa1 docs: minimize rloo approval stops"` and `next_safe_task: QUEUE-REFILL-PREP-STOP`; governance current-state test updated to assert `profile_version == "v0.0.38"`. No production code, test code, fixture content, network call, live subprocess, credential action, remote config change, publication/deployment, force push, destructive Git action, raw source archival, raw diagnostic-message archival, license adjudication, real OSS clone/fetch, additional live LSP execution, or DARS live-provider claim is introduced. |
| M23 follow-up queue refill (QUEUE-REFILL-PREP) | docs/plans/m23-adapter-portfolio-integration-followup-plan.md; docs/plans/m23-adapter-portfolio-integration-implementation-tasks.md; docs/traceability/README.md; docs/milestone-bootstrap/profile.yaml; ralph.md QUEUE-REFILL-PREP; roadmap.md | Closed: verified the authorized M23 follow-up rows are complete (`M23-ADAPTER-PORTFOLIO-INTEGRATION-PREP`, `M23-ADAPTER-PORTFOLIO-INTEGRATION-RED-GREEN`, `M23-LIVE-LSP-SMOKE-GATE`). Updated the follow-up plan queue statuses to `done`. Remaining candidates require fresh explicit authorization: real OSS clone/fetch and license-text capture/adjudication, additional live LSP execution or command allowlist expansion, live-provider DARS execution, or opening M24/new milestone work. No production code, test code, runtime report content, network call, live subprocess, credential action, remote config change, publication/deployment, force push, destructive Git action, raw source archival, raw diagnostic-message archival, license adjudication, or DARS live-provider claim is introduced. Profile bumped to `v0.0.37` with `next_safe_task: QUEUE-REFILL-PREP-STOP`; governance current-state test updated to match. |
| M23 live LSP smoke gate (M23-LIVE-LSP-SMOKE-GATE) | docs/reports/m23-live-lsp-server-smoke.md; runtime-boundary/lsp-adapter/20260522/ruff-check-live/lsp-report.json; runtime-boundary/lsp-adapter/20260522/ruff-check-live/lsp-report.md; runtime-boundary/lsp-adapter/20260522/pyright-check-live/lsp-report.json; runtime-boundary/lsp-adapter/20260522/pyright-check-live/lsp-report.md; tests/fixtures/codebase-evidence-portfolio/m21_dars_m23_bundle.json; docs/plans/m23-adapter-portfolio-integration-implementation-tasks.md; ralph.md M23-LIVE-LSP-SMOKE-GATE | Closed: preserved the already captured ruff/pyright/eslint smoke evidence as a docs/control gate. Verified `docs/reports/m23-live-lsp-server-smoke.md` records installed/verified executable versions (`ruff 0.15.10`, `pyright 1.1.409`, `eslint v10.4.0`), command ids (`ruff-check-live`, `pyright-check-live`, `eslint-check-live`), scopes, diagnostic counts, exit codes, and boundaries. Verified repo-resident ruff and pyright JSON/Markdown reports exist under `runtime-boundary/lsp-adapter/20260522/` and that the M23 portfolio bundle references the ruff/pyright JSON report refs. ESLint remains report-only from a temporary fixture and was not promoted into the repository. No new LSP execution, install, package manager command, network call, credential lookup, raw diagnostic-message archival, raw source-content archival, non-fixture mutation, or DARS live-provider claim was performed by this gate. Profile bumped to `v0.0.36` with `next_safe_task: QUEUE-REFILL-PREP`; governance current-state test updated to match. |
| M23 adapter portfolio integration (M23-ADAPTER-PORTFOLIO-INTEGRATION-RED-GREEN) | docs/plans/m23-adapter-portfolio-integration-implementation-tasks.md; tests/unit/test_codebase_evidence_portfolio.py; tests/fixtures/codebase-evidence-portfolio/m21_dars_m23_bundle.json; tests/fixtures/codebase-evidence-portfolio/expected-m21-dars-m23/portfolio-report.json; tests/fixtures/codebase-evidence-portfolio/expected-m21-dars-m23/portfolio-report.md; docs/reports/m23-live-lsp-server-smoke.md; runtime-boundary/lsp-adapter/20260522/ruff-check-live/lsp-report.json; runtime-boundary/lsp-adapter/20260522/pyright-check-live/lsp-report.json; src/hisys/operations/codebase_evidence_portfolio.py | Implemented: added a second deterministic portfolio golden round-trip that aggregates `DARS_PANEL_LOCAL_COMPLETION`, `M21`, `M23_LSP_ADAPTER`, and `M23_OSS_ADAPTER` through the existing pure `build_codebase_evidence_portfolio_report` / `write_codebase_evidence_portfolio_report` surface. RED observed: the new test failed with `FileNotFoundError` for `tests/fixtures/codebase-evidence-portfolio/m21_dars_m23_bundle.json`. GREEN: the new bundle pins `current_head_short=c7fb9af`, date `20260522`, M23 OSS and LSP refs/counts/schema ids/gates, and the expected JSON/Markdown are verbatim writer output. The report carries source lines `DARS_PANEL_LOCAL_COMPLETION`, `M21`, `M23_LSP_ADAPTER`, `M23_OSS_ADAPTER`; schema ids include `hisys.lsp_adapter.v1` and `hisys.oss_comparison_adapter.v1`; counts are `implemented_surface_count=23` and `human_gated_surface_count=7`; `unsafe_refs=[]`, `unsafe_line_labels=[]`, `external_call_made=false`, `mutation_performed=false`, and `raw_source_content_persisted=false`. Existing M21+DARS golden files remain unchanged. No production code, CLI behavior, schema change, live subprocess, network fetch/clone/search, real OSS clone, license-text capture, license adjudication, credential lookup, publication/deployment, remote config change, force push, raw source-content archival, raw diagnostic-message archival, or DARS live-provider claim is introduced. Profile bumped to `v0.0.35` with `next_safe_task: M23-LIVE-LSP-SMOKE-GATE`; governance current-state test updated to match. |
| M23 adapter portfolio integration PREP (M23-ADAPTER-PORTFOLIO-INTEGRATION-PREP) | docs/plans/m23-adapter-portfolio-integration-implementation-tasks.md; docs/plans/m23-adapter-portfolio-integration-followup-plan.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.33.md; docs/reports/m23-live-lsp-server-smoke.md; runtime-boundary/lsp-adapter/20260522/ruff-check-live/lsp-report.json; runtime-boundary/lsp-adapter/20260522/pyright-check-live/lsp-report.json; ralph.md M23-ADAPTER-PORTFOLIO-INTEGRATION-PREP | Prepared: authored the controlled RED/GREEN task packet for adding `M23_OSS_ADAPTER` and `M23_LSP_ADAPTER` evidence lines to the codebase evidence portfolio by refs, counts, schema ids, and quality-gate refs only. The packet preserves the existing M21+DARS golden, requires a new four-line bundle plus byte-equality JSON/Markdown expected files, pins the RED command expecting the new bundle to be missing, and carries the live LSP smoke refs without embedding raw source content, raw diagnostic messages, license text, or runtime artifact bodies. No production code, test code, fixture content, live subprocess, network call, credential lookup, real OSS clone, license adjudication, publication, deployment, remote config change, force push, or DARS live-provider claim is introduced. Profile bumped to `v0.0.34` with `next_safe_task: M23-ADAPTER-PORTFOLIO-INTEGRATION-RED-GREEN`; governance current-state test updated to match. |
| M23 adapter portfolio integration and live LSP smoke authorization | docs/plans/m23-adapter-portfolio-integration-followup-plan.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.33.md; docs/reports/m23-live-lsp-server-smoke.md; runtime-boundary/lsp-adapter/20260522/ruff-check-live/lsp-report.json; runtime-boundary/lsp-adapter/20260522/pyright-check-live/lsp-report.json | Authorized and prepared: user requested M23 adapter portfolio integration plus local LSP/lint install and execution for ruff, pyright, and eslint. First smoke evidence: ruff 14 diagnostics, pyright 2 diagnostics, eslint fixture 3 diagnostics, all through the governed `hisys lsp-adapter` boundary with `raw_source_content_persisted=false`, `external_call_made=false`, `mutation_performed=false`, and `live_external_action_authorized=false`. Next safe row is `M23-ADAPTER-PORTFOLIO-INTEGRATION-PREP`. |
| M23 advanced codebase adapter milestone closure (M23-ADVANCED-ADAPTER-GATE) | docs/plans/m23-advanced-codebase-adapter-integration-plan.md; docs/plans/m23-oss-comparison-adapter-implementation-tasks.md; docs/plans/m23-cli-oss-comparison-adapter-implementation-tasks.md; docs/plans/m23-golden-oss-comparison-adapter-implementation-tasks.md; docs/plans/m23-lsp-adapter-implementation-tasks.md; docs/plans/m23-cli-lsp-adapter-implementation-tasks.md; docs/plans/m23-golden-lsp-adapter-implementation-tasks.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.21.md; ralph.md M23-ADVANCED-ADAPTER-GATE; src/hisys/operations/oss_comparison_adapter.py; src/hisys/operations/lsp_adapter.py; src/hisys/cli/main.py; tests/unit/test_oss_comparison_adapter.py; tests/unit/test_lsp_adapter.py; tests/unit/test_domain_cli.py; tests/fixtures/oss-comparison/; tests/fixtures/lsp-adapter/ | Closed: the entire M23 advanced codebase adapter milestone reaches `local_fixture_advisory_complete`. Sub-line closures consolidated: M23-OSS-ADAPTER (PREP `70ae484`, RED-GREEN `d610c53`, CLI `d6023b4`, GOLDEN `9963ccc`, GATE `0a172d3` -> full repo `999 passed`); M23-LSP-ADAPTER (PREP `090de67`, RED-GREEN `1874ad5` -> 13 focused safety tests, CLI `e54bbf3` -> 4 focused CLI tests, GOLDEN `ca0420f` -> byte-equality round-trip test, GATE `3ca9b22` -> full repo `1017 passed`, LSP focused `14 passed`, DARS panel `75 passed`, governance `1 passed`). Quality gate evidence at HEAD `3ca9b22 docs: close m23 lsp adapter milestone` (the latest pre-closure baseline): full repo `PYTHONPATH=src:. pytest -q` `1017 passed`, combined M21+M22+M23+LSP+CLI+golden+governance focused `119 passed`, M23 adapters focused `59 passed`, traceability OK, secret scan `hit_count=0`, `git diff --check` clean. Both adapter sub-lines pinned by deterministic byte-equality golden round-trip tests against checked-in expected JSON/Markdown fixtures; the LSP runner enforces the M23 subprocess safety contract (caller-supplied allowlist, 120s timeout cap, 4 MiB output truncation, workspace-root containment under `instance_root`, no shell, no environment inheritance beyond `PATH`, SHA-256 first-16-hex `message_digest` only — raw text never persisted, `FileNotFoundError` → `lsp_command_not_found`, `subprocess.TimeoutExpired` → `subprocess_timed_out=true`) via 13 safety + 1 golden focused tests; the OSS adapter enforces caller-supplied descriptor inputs only with no clone/network/install via 7 focused tests; both CLIs are thin argparse pass-throughs over the pure modules and never expand the safety vocabulary. QUEUE-REFILL-PREP at M23 closure: `M23-ADAPTER-PORTFOLIO-INTEGRATION` remains an optional additive evidence-line for M22 portfolio (not strictly required for M23 closure and not requested by the user); post-M23 work (live provider execution, remote LSP adapter, additional M22 portfolio variants, M23 portfolio integration if user requests it, M24/new-milestone work) all require fresh explicit authorization beyond the M23 boundary. The next safe Ralph row is `QUEUE-REFILL-PREP-STOP` until the user authorizes a new milestone. No live model call, real remote provider call, network clone/fetch/search, LSP server installation, package install, real `ruff` / `pyright` / `mypy` / `flake8` / `eslint` execution, LSP wire-protocol speech, credential lookup, raw upstream source archival, raw diagnostic message persistence, license-text capture, license adjudication, subagent execution, publication/deployment, schema/data migration against non-fixture data, force push, new remote configuration, destructive operation, or live-provider DARS completion claim was performed by this closure. |
| Local LSP adapter milestone closure (M23-LSP-ADAPTER-GATE) | docs/plans/m23-lsp-adapter-implementation-tasks.md; docs/plans/m23-cli-lsp-adapter-implementation-tasks.md; docs/plans/m23-golden-lsp-adapter-implementation-tasks.md; docs/plans/m23-advanced-codebase-adapter-integration-plan.md; ralph.md M23-LSP-ADAPTER-GATE; src/hisys/operations/lsp_adapter.py; src/hisys/cli/main.py; tests/unit/test_lsp_adapter.py; tests/unit/test_domain_cli.py; tests/fixtures/lsp-adapter/ | Closed: the M23 local LSP adapter line reaches `local_fixture_advisory_complete`. Quality gate evidence at HEAD `ca0420f test: pin lsp adapter golden fixture`: full repo `PYTHONPATH=src:. pytest -q` `1017 passed`, combined M21+M22+M23+LSP+CLI+golden+governance focused gate green at `119 passed`, LSP adapter focused gate `14 passed` including the byte-equality golden round-trip, DARS critic-panel + remote subscription + readiness focused regression `75 passed`, governance current-state `1 passed`, `python3 scripts/validate_traceability.py` OK, `python3 scripts/scan_secrets.py` `hit_count=0`, and `git diff --check` clean. QUEUE-REFILL-PREP classification: the LSP adapter line is end-to-end complete with the pure governed runner (caller-supplied command allowlist, 120s timeout cap, 4 MiB output truncation, workspace-root containment, no shell, no environment inheritance beyond PATH, SHA-256 first-16-hex message_digest only — raw text never persisted; `subprocess.TimeoutExpired` → report with `subprocess_timed_out=true`; `FileNotFoundError` → `ValueError("lsp_command_not_found")`), the CLI wrapper, and the deterministic golden fixture all pinned by focused tests. The next safe Ralph row is `M23-ADVANCED-ADAPTER-GATE` — a docs/control closure increment that records the entire M23 advanced codebase adapter line (OSS adapter at `9963ccc` + LSP adapter at `ca0420f`) as closed at `local_fixture_advisory_complete` and runs the final QUEUE-REFILL-PREP across post-M23 backlog candidates; `M23-ADAPTER-PORTFOLIO-INTEGRATION` remains an optional additive evidence-line row for M22 portfolio (not strictly required for M23 closure). No live model call, real remote provider call, network clone/fetch/search, LSP server installation, package install, real `ruff` / `pyright` / `mypy` / `flake8` / `eslint` execution, LSP wire-protocol speech, credential lookup, raw upstream source archival, raw diagnostic message persistence, license-text capture, license adjudication, subagent execution, publication/deployment, schema/data migration against non-fixture data, force push, new remote configuration, destructive operation, or live-provider DARS completion claim was performed by this closure. |
| Local LSP adapter golden fixture (M23-LSP-ADAPTER-GOLDEN) | docs/plans/m23-golden-lsp-adapter-implementation-tasks.md; ralph.md M23-LSP-ADAPTER-GOLDEN; tests/fixtures/lsp-adapter/m23_lsp_bundle.json; tests/fixtures/lsp-adapter/expected/lsp-report.json; tests/fixtures/lsp-adapter/expected/lsp-report.md; tests/fixtures/lsp-adapter/ruff/canned_ruff_output.json; tests/unit/test_lsp_adapter.py | Implemented: a deterministic byte-equality round-trip golden test pins one canonical input bundle (date `20260522`, `current_head_short=e54bbf3`, `command_id=ruff-check`, `argv=["ruff", "check", "--output-format=json", "src/"]`, `timeout_seconds=30`, `expected_exit_codes=[0, 1]`, `output_format=ruff_json`, `target_refs=["src/a.py", "src/b.py"]`, `command_allowlist=["ruff"]`, `human_approval_ref="docs/approvals/lsp-adapter-2026-05-22.md"`, `canned_stdout_ref="tests/fixtures/lsp-adapter/ruff/canned_ruff_output.json"`) through `run_lsp_adapter` / `write_lsp_adapter_report` against checked-in expected JSON and Markdown fixtures. The test monkey-patches `subprocess.run` to return the existing canned ruff stdout (`F401 src/a.py:1`, `E501 src/a.py:5`, `W292 src/b.py:12`) so no real `ruff` binary is invoked. The expected JSON is the output of `model_dump(mode="json")` serialized with `json.dumps(..., indent=2, sort_keys=True) + "\n"` (matching the writer chokepoint); the expected Markdown is the output of `render_lsp_adapter_markdown(report)`. The expected JSON pins `diagnostic_count=3`, `error_count=2`, `warning_count=1`, `info_count=0`, `output_bytes=562`, `subprocess_exit_code=1`, `subprocess_timed_out=false`, `subprocess_killed=false`, `output_truncated=false`, `unsafe_refs=[]`, `category_ref_summary=["style", "style_warning", "unused"]`, and one diagnostic per code with deterministic `message_digest` SHA-256 first-16-hex digests (raw message text is NOT embedded — `F401`'s digest is `46324d685ce86d2e`, `E501`'s is `913467c303f6df39`, `W292`'s is `d310a320e365bd5d`). The test does not regenerate the fixtures, does not depend on system clock / git hash / tempfile UUID beyond the `tmp_path` writer scratch directory, and does not embed raw upstream source bodies, raw diagnostic message text, license bodies, diff hunks, or secrets in any fixture. No CLI surface, live access, credential lookup, `runtime-boundary/` crawl, `tests/fixtures/` auto-discovery, subagent, real LSP, network fetch, OSS clone, `pip` install, or `ruff` binary invocation behavior is added. Profile bumped to `v0.0.30` with `next_safe_task: M23-LSP-ADAPTER-GATE`; governance current-state test updated to match. |
| Local LSP adapter CLI wrapper (M23-LSP-ADAPTER-CLI) | docs/plans/m23-cli-lsp-adapter-implementation-tasks.md; docs/plans/m23-lsp-adapter-implementation-tasks.md; ralph.md M23-LSP-ADAPTER-CLI; src/hisys/cli/main.py; src/hisys/operations/lsp_adapter.py; tests/unit/test_domain_cli.py | Implemented: `hisys lsp-adapter --instance <root> --date <YYYYMMDD> --bundle <json> [--current-head-short <hash>]` exposes the M23-RED-GREEN governed runner through a thin argparse subparser + `_load_lsp_adapter_bundle` + `_cmd_lsp_adapter` dispatcher inside `src/hisys/cli/main.py`. The bundle is loaded via the existing `_load_json_report` helper as a top-level JSON object containing `workspace_root` (str), `command` (object with `command_id`, `argv`, `timeout_seconds`, optional `expected_exit_codes`, `output_format`), `target_refs` (list of str), `command_allowlist` (list of str), and `human_approval_ref` (str); each entry is constructed through the existing Pydantic `LspAdapterCommand` / `LspAdapterRequest` models so the CLI does not duplicate the M23 shape. `--current-head-short` is optional and recorded verbatim. The CLI calls `run_lsp_adapter` and `write_lsp_adapter_report` without reshaping report semantics; prints bounded `lsp-adapter report`, `markdown`, `command_id`, `output_format`, `diagnostic_count`, `error_count`, `warning_count`, `info_count`, `subprocess_exit_code`, `subprocess_timed_out`, `subprocess_killed`, `output_truncated`, `output_bytes`, `unsafe_ref_count`, `advisory_only: true`, `requires_human_review: true`, `external_call_made: false`, `mutation_performed: false`, `raw_source_content_persisted: false`, `live_external_action_authorized: false`, and `allowed_actions: advisory_only` lines; returns exit code `0` on success and propagates `ValueError` non-zero. Four focused tests in `tests/unit/test_domain_cli.py` pin: happy-path round-trip with monkey-patched `subprocess.run` returning canned ruff JSON (asserts JSON + Markdown artifact paths, advisory flag set, `shell=False`, `env={"PATH"}`, no raw message text leaks into Markdown body); missing-`command` rejection (raises `ValueError`, no subprocess call); bad-`YYYYMMDD` rejection (raises `ValueError` from runner validation, no subprocess call); `lsp_command_not_in_allowlist` rejection (asserts `pytest.raises(ValueError, match="lsp_command_not_in_allowlist")` before any subprocess call). The CLI never calls `date.today()`, never reads `.git/`, never invokes `subprocess` directly (only through the M23-RED-GREEN runner), never crawls `runtime-boundary/`, never crawls `tests/fixtures/`, never auto-discovers bundle files, never opens workspace source files, never invokes `pip`/`npm`/`apt-get`, never installs LSP servers, never speaks the LSP wire protocol, and never expands the command-id/output-format vocabulary. Profile bumped to `v0.0.29` with `next_safe_task: M23-LSP-ADAPTER-GOLDEN`; governance current-state test updated to match. |
| Local LSP adapter (M23-LSP-ADAPTER-RED-GREEN) | docs/plans/m23-lsp-adapter-implementation-tasks.md; docs/plans/m23-advanced-codebase-adapter-integration-plan.md; ralph.md M23-LSP-ADAPTER-RED-GREEN; src/hisys/operations/lsp_adapter.py; tests/unit/test_lsp_adapter.py; tests/fixtures/lsp-adapter/ruff/canned_ruff_output.json | Implemented: `LspAdapterCommand`, `LspAdapterRequest`, `LspAdapterDiagnostic`, `LspAdapterReport`, `run_lsp_adapter`, `render_lsp_adapter_markdown`, and `write_lsp_adapter_report` provide the first Hisys-managed governed subprocess surface. The runner invokes `subprocess.run(list(argv), cwd=workspace_root, env={"PATH": os.environ.get("PATH", "")}, timeout=timeout_seconds, capture_output=True, check=False, shell=False)` only after `_validate_lsp_request` enforces the safety contract: deterministic-date `^\d{8}$`; `command_id` matching `^[a-z][a-z0-9_\-]{1,63}$`; `output_format` from the parser allowlist `{pyright_json, mypy_json, ruff_json, flake8_json, eslint_json}`; positive `timeout_seconds` bounded at `_MAX_TIMEOUT_SECONDS=120`; non-empty `argv` with `argv[0]` in the caller-supplied `command_allowlist`; no `argv` element contains a shell metacharacter from `(";", "|", "&", "`", "$(", "<(", ">(", "\n", "\r")`; non-empty `human_approval_ref`; `workspace_root` resolves under `instance_root`. Each rejection raises a deterministic `ValueError("lsp_<rule>")`; `subprocess.TimeoutExpired` is caught and returned as a report with `subprocess_timed_out=true`, `diagnostics=()`, `output_bytes=0`; `FileNotFoundError` from `subprocess.run` is converted to `ValueError("lsp_command_not_found")`. The runner truncates stdout to `_MAX_OUTPUT_BYTES=4_194_304` (4 MiB), records `output_truncated`, `output_bytes`, and `subprocess_exit_code`, then dispatches to a deterministic format-specific parser (`_parse_ruff_json`, `_parse_pyright_json`, `_parse_mypy_json`, `_parse_flake8_json`, `_parse_eslint_json`) which uses `json.loads` only — no `eval`, `exec`, `pickle`, `yaml.load`, or `xml.etree.ElementTree.fromstring`. Malformed JSON after truncation degrades to empty diagnostics rather than raising. `LspAdapterDiagnostic` records only `severity`, `code` (capped at 64 chars), `file_ref` (relative under `workspace_root`; absolute paths outside the workspace are appended to `unsafe_refs` rather than persisted), `line`, `column`, `message_digest` (SHA-256 first-16-hex of the raw message), and `category_ref` (e.g. `unused`, `style`, `style_warning`, `imports`, `docstring`, `naming`, `complexity`, `bugbear`, `security`, `type_check`, `lint`, `other`) — the raw message text is never persisted to JSON or Markdown so secrets, proprietary source quotes, or PII cannot leak through diagnostic messages. The writer `write_lsp_adapter_report` persists JSON and Markdown only under `runtime-boundary/lsp-adapter/<YYYYMMDD>/<command_id>/lsp-report.{json,md}` through `resolve_instance_runtime_ref`; `render_lsp_adapter_markdown` emits a `\| severity \| code \| file_ref \| line \| column \| category_ref \| message_digest \|` table that never embeds raw text. The report carries `schema_id="hisys.lsp_adapter.v1"`, `advisory_only=true`, `requires_human_review=true`, `external_call_made=false` (a local LSP subprocess is **not** an external network call), `mutation_performed=false`, `raw_source_content_persisted=false`, `live_external_action_authorized=false`, and `allowed_actions="advisory_only"`. Thirteen focused tests in `tests/unit/test_lsp_adapter.py` pin the safety contract end-to-end via monkey-patched `subprocess.run`: ruff aggregation with canned JSON; `lsp_command_not_in_allowlist` rejection; `subprocess.TimeoutExpired` → `subprocess_timed_out=true`; oversize-output truncation at 4 MiB; `lsp_output_format_not_allowed` rejection; `lsp_timeout_out_of_range` rejection; `lsp_argv_shell_metacharacter` rejection; `lsp_human_approval_required` rejection; `lsp_workspace_root_outside_instance` rejection; `FileNotFoundError` → `lsp_command_not_found`; unsafe `target_refs` (`..`, absolute paths) collected into `unsafe_refs`; writer round-trip with no raw message text in Markdown; writer bad-date rejection. No real LSP server is invoked. The runner performs no `pip` / `npm` / `apt-get` install, no LSP-protocol socket, no long-lived daemon, no `.git/` read, no `date.today()` call, no `os.environ` passthrough beyond `PATH`, no `tests/fixtures/` crawl, no network call, and no live external provider call. Profile bumped to `v0.0.28` with `next_safe_task: M23-LSP-ADAPTER-CLI`; governance current-state test updated to match. CLI wrapper, golden fixture, and milestone closure are deferred to `M23-LSP-ADAPTER-CLI`, `M23-LSP-ADAPTER-GOLDEN`, and `M23-LSP-ADAPTER-GATE`. |
| Local LSP adapter PREP (M23-LSP-ADAPTER-PREP) | docs/plans/m23-advanced-codebase-adapter-integration-plan.md; docs/plans/m23-lsp-adapter-implementation-tasks.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.21.md; docs/milestone-bootstrap/profile.yaml; ralph.md M23-LSP-ADAPTER-PREP | Prepared: authored the document-RED packet `docs/plans/m23-lsp-adapter-implementation-tasks.md` that defines the local LSP adapter subprocess safety contract before any product code. The packet pins the Pydantic shapes (`LspAdapterCommand`, `LspAdapterRequest`, `LspAdapterDiagnostic`, `LspAdapterReport` with `schema_id="hisys.lsp_adapter.v1"`); the first RED test command (`PYTHONPATH=src pytest tests/unit/test_lsp_adapter.py::test_run_lsp_adapter_aggregates_ruff_diagnostics -q` expecting `ModuleNotFoundError`); the GREEN module path `src/hisys/operations/lsp_adapter.py` and writer partition `runtime-boundary/lsp-adapter/<YYYYMMDD>/<command_id>/lsp-report.{json,md}`; the safety regression matrix (timeout cap of 120 seconds, output truncation at 4 MiB, command-allowlist enforcement, shell-metacharacter rejection in argv, missing-`human_approval_ref` rejection, `workspace_root` containment under `instance_root`, format-allowlist enforcement, FileNotFoundError → `ValueError("lsp_command_not_found")` conversion); and the docs/gate/commit ritual. The packet pins the runner as caller-supplied only (no built-in command list, no auto-discovery of LSP servers, no `pip` / `npm` / `apt-get` install, no `tests/fixtures/` crawl, no `subprocess` shell expansion, no `.git/` read, no `date.today()`), governed subprocess only (no shell, no environment inheritance beyond `PATH`, no long-lived daemon, no socket, no LSP protocol wire), no raw message persistence (diagnostics record only severity/code/file_ref/line/column/message_digest/category_ref — the SHA-256 first-16-hex of the raw message — and never the raw text), and advisory-only output (`advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, `raw_source_content_persisted=false`, `live_external_action_authorized=false`, `allowed_actions="advisory_only"`). The five allowed `output_format` values are `pyright_json`, `mypy_json`, `ruff_json`, `flake8_json`, `eslint_json`; each is parsed via `json.loads` only — no `eval`, `exec`, `pickle`, `yaml.load`, or `xml.etree.ElementTree.fromstring`. Profile bumped to `v0.0.27` with `next_safe_task: M23-LSP-ADAPTER-RED-GREEN`; governance current-state test updated to match. No production code or test code added in this PREP increment. No live model call, real remote provider call, credential lookup, secret capture, network fetch, OSS clone, package install, LSP subprocess, subagent execution, publication, deployment, schema/data migration, force push, new remote configuration, or destructive operation. |
| Approved OSS comparison adapter milestone closure (M23-OSS-ADAPTER-GATE) | docs/plans/m23-advanced-codebase-adapter-integration-plan.md; docs/plans/m23-oss-comparison-adapter-implementation-tasks.md; docs/plans/m23-cli-oss-comparison-adapter-implementation-tasks.md; docs/plans/m23-golden-oss-comparison-adapter-implementation-tasks.md; ralph.md M23-OSS-ADAPTER-GATE; src/hisys/operations/oss_comparison_adapter.py; src/hisys/cli/main.py; tests/unit/test_oss_comparison_adapter.py; tests/unit/test_domain_cli.py; tests/fixtures/oss-comparison/ | Closed: the M23 approved-OSS comparison adapter line reaches `local_fixture_advisory_complete`. Quality gate evidence at HEAD `9963ccc test: pin oss comparison adapter golden fixture`: full repo `PYTHONPATH=src pytest -q` `999 passed`, combined M21+M22+M23+CLI+governance focused gate green, OSS comparison adapter focused gate `7 passed` including the golden round-trip, DARS critic-panel focused regression `55 passed`, `python3 scripts/validate_traceability.py` OK, `python3 scripts/scan_secrets.py` `hit_count=0`, and `git diff --check` clean. QUEUE-REFILL-PREP classification: the OSS adapter line is end-to-end complete with the pure builder/writer, CLI wrapper, and deterministic golden fixture all pinned by focused tests. The next safe Ralph row is `M23-LSP-ADAPTER-PREP` — a docs/control PREP packet that authors the local LSP adapter subprocess boundary (command allowlist, timeout, workspace-root restriction, output schema, and kill policy) before any product code; `M23-ADAPTER-PORTFOLIO-INTEGRATION` and `M23-ADVANCED-ADAPTER-GATE` follow. No live model call, real remote provider call, network clone/fetch/search, OSS clone, package install, credential lookup, raw OSS source archival, license-text capture, license adjudication, LSP subprocess, subagent execution, publication/deployment, schema/data migration against non-fixture data, force push, new remote configuration, destructive operation, or live-provider DARS completion claim was performed by this closure. |
| Approved OSS comparison adapter golden fixture (M23-OSS-ADAPTER-GOLDEN) | docs/plans/m23-golden-oss-comparison-adapter-implementation-tasks.md; ralph.md M23-OSS-ADAPTER-GOLDEN; tests/fixtures/oss-comparison/m23_local_oss_bundle.json; tests/fixtures/oss-comparison/expected/comparison-report.json; tests/fixtures/oss-comparison/expected/comparison-report.md; tests/unit/test_oss_comparison_adapter.py | Implemented: a deterministic byte-equality round-trip golden test pins one canonical local-line + approved-OSS bundle (M21 local line plus `understand-static-analysis` and `pylint-style-rules` approved sources) through `build_oss_comparison_report` / `write_oss_comparison_report` against checked-in expected JSON and Markdown fixtures. The bundle declares a fixed `date="20260522"`, fixed `current_head_short="d6023b4"`, seven M21 category refs, three M21 portfolio refs, two approved-source descriptors with bounded category/approved/local_fixture refs and notes, counts 9/2 for the local line, and only safe relative refs. The expected JSON is the output of `model_dump(mode="json")` serialized with `json.dumps(..., indent=2, sort_keys=True) + "\n"` (matching the writer chokepoint); the expected Markdown is the output of `render_oss_comparison_markdown(report)`. The test does not regenerate the fixtures, does not depend on system clock / git hash / tempfile UUID, and does not embed raw upstream source bodies, license bodies, diff hunks, or secrets in any fixture. No CLI surface, live access, credential lookup, `runtime-boundary/` crawl, `tests/fixtures/` auto-discovery, subagent, LSP, network fetch, OSS clone, or `pip` install behavior is added. |
| Approved OSS comparison adapter CLI wrapper (M23-OSS-ADAPTER-CLI) | docs/plans/m23-cli-oss-comparison-adapter-implementation-tasks.md; ralph.md M23-OSS-ADAPTER-CLI; src/hisys/cli/main.py; src/hisys/operations/oss_comparison_adapter.py; tests/unit/test_domain_cli.py | Implemented: `hisys oss-comparison-adapter --instance <root> --date <YYYYMMDD> --bundle <json> [--current-head-short <hash>]` exposes the M23 pure builder/writer through a thin argparse subparser and dispatcher. The bundle is loaded via the existing `_load_json_report` helper as a top-level JSON object containing a `local_line` object and an `approved_sources` list; each entry is constructed through the existing Pydantic `LocalCodebaseLine` / `ApprovedOssSource` / `OssComparisonRequest` models so the CLI does not duplicate the M23 shape. `--current-head-short` is optional and recorded verbatim. The CLI calls `build_oss_comparison_report` and `write_oss_comparison_report` without reshaping report semantics; prints bounded `oss-comparison-adapter report`, `markdown`, `compared_source_count`, `union_category_count`, `intersection_category_count`, `local_only_category_count`, `oss_only_category_count`, `unsafe_ref_count`, `unsafe_source_id_count`, `unsafe_line_label_count`, `advisory_only: true`, `requires_human_review: true`, `external_call_made: false`, `mutation_performed: false`, `raw_source_content_persisted: false`, `live_external_action_authorized: false`, and `allowed_actions: advisory_only` lines; returns exit code `0` on success and propagates `ValueError` non-zero. The CLI never calls `date.today()`, never reads `.git/`, never calls `subprocess`, never crawls `runtime-boundary/`, never crawls `tests/fixtures/`, never auto-discovers bundle files, never opens `local_fixture_refs` to verify existence, never installs OSS packages, never invokes a model/LSP/subagent, and never expands the source-id/category/line-label vocabulary. |
| Approved OSS comparison adapter (M23-OSS-ADAPTER-RED-GREEN) | docs/plans/m23-advanced-codebase-adapter-integration-plan.md; docs/plans/m23-oss-comparison-adapter-implementation-tasks.md; ralph.md M23-OSS-ADAPTER-RED-GREEN; src/hisys/operations/oss_comparison_adapter.py; tests/unit/test_oss_comparison_adapter.py | Implemented: `ApprovedOssSource`, `LocalCodebaseLine`, `OssComparisonRequest`, `OssComparisonReport`, `build_oss_comparison_report`, `render_oss_comparison_markdown`, and `write_oss_comparison_report` provide a pure local-only advisory adapter that maps one caller-supplied local codebase evidence line (label matching `^[A-Z][A-Z0-9_\-]{1,63}$`) to caller-supplied approved-OSS reference descriptors (source-id matching `^[a-z][a-z0-9_\-]{1,63}$`) and emits set-based `union_category_refs`, `intersection_category_refs`, `local_only_category_refs`, and `oss_only_category_refs` tuples plus deduplicated `compared_source_ids` and `compared_source_license_tags`. Unsafe refs (`..` traversal, absolute paths, empty strings) are collected into `unsafe_refs`; source-ids that fail the pattern or carry notes that exceed 1024 chars or contain non-`\t\n` control characters land in `unsafe_source_ids`; a local-line label that fails the pattern is captured in `unsafe_line_labels` and the comparison surface degrades to empty sets without raising. Non-`YYYYMMDD` dates raise `ValueError` in both `build_oss_comparison_report` and `write_oss_comparison_report`. The writer persists JSON/Markdown only under `runtime-boundary/oss-comparison/<YYYYMMDD>/comparison-report.{json,md}` through `resolve_instance_runtime_ref`. The report carries `schema_id="hisys.oss_comparison_adapter.v1"`, `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, `raw_source_content_persisted=false`, `live_external_action_authorized=false`, and `allowed_actions="advisory_only"`. The adapter performs no `git`, `subprocess`, network, `pip`, `git clone`, package import, `tests/fixtures/` crawl, `.git/` read, system-clock read, raw upstream source archival, license-text capture, license adjudication, model invocation, LSP subprocess, subagent execution, or compliance attestation. CLI wrapper is deferred to `M23-OSS-ADAPTER-CLI`. |
| Approved OSS comparison adapter PREP (M23-OSS-ADAPTER-PREP) | docs/plans/m23-advanced-codebase-adapter-integration-plan.md; docs/plans/m23-oss-comparison-adapter-implementation-tasks.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.21.md; docs/milestone-bootstrap/profile.yaml; ralph.md M23-OSS-ADAPTER-PREP | Prepared: authored the document-RED packet `docs/plans/m23-oss-comparison-adapter-implementation-tasks.md` that defines the fixture-local approved-OSS comparison adapter contract, the first RED test command (`PYTHONPATH=src pytest tests/unit/test_oss_comparison_adapter.py::test_build_oss_comparison_aggregates_local_line_and_approved_sources -q` expecting `ModuleNotFoundError: No module named 'hisys.operations.oss_comparison_adapter'`), the GREEN module/writer shape (`ApprovedOssSource`, `LocalCodebaseLine`, `OssComparisonRequest`, `OssComparisonReport`, `build_oss_comparison_report`, `render_oss_comparison_markdown`, `write_oss_comparison_report` under `runtime-boundary/oss-comparison/<YYYYMMDD>/comparison-report.{json,md}`), the supplemental regression tests (writer round-trip, unsafe ref/source-id/line-label rejection, bad-date rejection), and the gate + commit ritual. The packet pins the adapter as set-based / category-ref only (no fuzzy matching, no embedding similarity, no model call), caller-supplied inputs only (no network fetch, no OSS clone, no package install, no `tests/fixtures/` crawl, no `subprocess`, no `.git/`, no `date.today()`), advisory-only output (`schema_id="hisys.oss_comparison_adapter.v1"`, `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, `raw_source_content_persisted=false`, `live_external_action_authorized=false`, `allowed_actions="advisory_only"`), and a bounded-`notes` rule (max 1024 chars, printable ASCII only) so a malformed fixture cannot smuggle binary content. Profile bumped to `v0.0.22` with `next_safe_task: M23-OSS-ADAPTER-RED-GREEN`; governance current-state test updated to match. No production code or test code added in this PREP increment. No live model call, real remote provider call, credential lookup, secret capture, network fetch, OSS clone, package install, LSP subprocess, subagent execution, publication, deployment, schema/data migration, force push, new remote configuration, or destructive operation. |
| Advanced codebase adapter authorization (M23-AUTH) | docs/plans/m23-advanced-codebase-adapter-integration-plan.md; docs/milestone-bootstrap/documents/readiness_decision_record_v0.0.21.md; ralph.md M23 authorization checkpoint | Authorized: the user opened M23 for approved OSS comparison adapter, optional local LSP adapter, and governed external adapter integration for code-analysis evidence. Boundary preserved: no credential lookup or mutation, no secret capture, no arbitrary network search/clone/fetch, no new or changed remote configuration, no publication/deployment/release, no destructive Git/history operation, no force push, no non-fixture user/live data mutation, no unbounded live external provider execution, and no live-provider DARS completion claim. Next safe Ralph row is `M23-OSS-ADAPTER-PREP`. |
| Codebase evidence portfolio milestone closure (M22-PORTFOLIO-GATE) | docs/plans/m22-codebase-evidence-portfolio-implementation-plan.md; docs/plans/m22-codebase-evidence-portfolio-implementation-tasks.md; docs/plans/m22-cli-codebase-evidence-portfolio-implementation-tasks.md; docs/plans/m22-golden-codebase-evidence-portfolio-implementation-tasks.md; ralph.md M22-PORTFOLIO-GATE; src/hisys/operations/codebase_evidence_portfolio.py; src/hisys/cli/main.py; tests/unit/test_codebase_evidence_portfolio.py; tests/unit/test_domain_cli.py; tests/fixtures/codebase-evidence-portfolio/ | Closed: M22 codebase evidence portfolio milestone reaches `local_fixture_advisory_complete`. Quality gate evidence: combined M21+M22+CLI+governance focused gate `90 passed`, DARS critic panel focused regression `55 passed`, `python3 scripts/validate_traceability.py` OK, `python3 scripts/scan_secrets.py` `hit_count=0`, and `git diff --check` clean at HEAD `f4b4d44 test: pin codebase evidence portfolio golden fixture`. QUEUE-REFILL-PREP classification: no safe local M22 follow-on candidate remains under the no-live / no-credential / no-LSP-subprocess / no-subagent / no-raw-source / no-`runtime-boundary/`-crawl / no-`date.today()` boundary; the remaining backlog candidates (approved OSS comparison adapter, optional local LSP adapter, live-provider DARS panel execution) are human-gated per `ralph.md` Sections 2.2 and 5 and `docs/plans/m21-roadmap-implementation-plan.md` lines 37-38. The Ralph queue therefore stops at M22 closure pending the next milestone authorization from the user. No live model call, real remote provider call, network clone/fetch/search, credential lookup, local LSP subprocess, subagent execution, publication/deployment, schema/data migration against non-fixture data, force push, new remote configuration, destructive operation, or raw source-content archival was performed by this closure. |
| Codebase evidence portfolio golden fixture (M22-PORTFOLIO-GOLDEN) | docs/plans/m22-golden-codebase-evidence-portfolio-implementation-tasks.md; ralph.md M22-PORTFOLIO-GOLDEN; tests/fixtures/codebase-evidence-portfolio/m21_dars_bundle.json; tests/fixtures/codebase-evidence-portfolio/expected/portfolio-report.json; tests/fixtures/codebase-evidence-portfolio/expected/portfolio-report.md; tests/unit/test_codebase_evidence_portfolio.py | Implemented: a deterministic byte-equality round-trip golden test pins the canonical M21 + DARS_PANEL_LOCAL_COMPLETION bundle through `build_codebase_evidence_portfolio_report` / `write_codebase_evidence_portfolio_report` against checked-in expected JSON and Markdown fixtures. The bundle declares a fixed `date="20260521"`, fixed `current_head_short="ec192e3"`, three M21 artifact refs / schema ids / quality gates (counts 9/2), two DARS_PANEL_LOCAL_COMPLETION artifact refs / one schema id / two quality gates (counts 5/0), and only safe relative refs. The expected JSON is the output of `model_dump(mode="json")` serialized with `json.dumps(..., indent=2, sort_keys=True) + "\n"` (matching the writer chokepoint); the expected Markdown is the output of `render_codebase_evidence_portfolio_markdown(report)`. The test does not regenerate the fixtures, does not depend on system clock / git hash / tempfile UUID, and does not embed raw source bodies, diff hunks, or secrets in any fixture. No CLI surface, live access, credential lookup, `runtime-boundary/` crawl, subagent, LSP, or auto-discovery behavior is added. |
| Codebase evidence portfolio CLI wrapper (M22-PORTFOLIO-CLI) | docs/plans/m22-cli-codebase-evidence-portfolio-implementation-tasks.md; ralph.md M22-PORTFOLIO-CLI; src/hisys/cli/main.py; src/hisys/operations/codebase_evidence_portfolio.py; tests/unit/test_domain_cli.py | Implemented: `hisys codebase-evidence-portfolio --instance <root> --date <YYYYMMDD> --line-bundle <json> [--current-head-short <hash>]` exposes the M22 pure builder/writer through a thin argparse subparser and dispatcher. The bundle is loaded only via the reused `_load_json_report` helper as a top-level JSON object containing a `line_refs` list of objects; each list element is constructed through the existing Pydantic `EvidenceLineRef` model so the CLI does not duplicate the M22 shape. `--current-head-short` is optional and recorded verbatim. The CLI calls `build_codebase_evidence_portfolio_report` and `write_codebase_evidence_portfolio_report` without reshaping report semantics; prints bounded `codebase-evidence-portfolio report`, `markdown`, `source_line_count`, `artifact_ref_count`, `schema_id_count`, `quality_gate_ref_count`, `implemented_surface_count`, `human_gated_surface_count`, `unsafe_ref_count`, `unsafe_line_label_count`, `advisory_only: true`, `requires_human_review: true`, `external_call_made: false`, `mutation_performed: false`, `raw_source_content_persisted: false`, and `allowed_actions: advisory_only` lines; returns exit code `0` on success and propagates `ValueError` non-zero. The CLI never calls `date.today()`, never reads `.git/`, never calls `subprocess`, never crawls `runtime-boundary/`, never infers latest artifacts, never auto-discovers bundle files, never opens artifact refs to verify file existence, and never expands the line-label or ref vocabulary. |
| Codebase evidence portfolio (M22-PORTFOLIO-RED-GREEN) | docs/plans/m22-codebase-evidence-portfolio-implementation-plan.md; docs/plans/m22-codebase-evidence-portfolio-implementation-tasks.md; ralph.md M22-PORTFOLIO-PREP and M22-PORTFOLIO-RED-GREEN; src/hisys/operations/codebase_evidence_portfolio.py; tests/unit/test_codebase_evidence_portfolio.py | Implemented: `EvidenceLineRef`, `CodebaseEvidencePortfolioRequest`, `CodebaseEvidencePortfolioReport`, `build_codebase_evidence_portfolio_report`, `render_codebase_evidence_portfolio_markdown`, and `write_codebase_evidence_portfolio_report` provide a pure local-only advisory portfolio that aggregates caller-supplied evidence-line refs (M21, DARS_PANEL_LOCAL_COMPLETION, and other caller-named lines matching `^[A-Z][A-Z0-9_\-]{1,63}$`) into deduplicated `source_lines`, `artifact_refs`, `schema_ids`, and `quality_gate_refs` tuples plus `implemented_surface_count` and `human_gated_surface_count` totals. Unsafe refs (`..` traversal, absolute paths, empty strings) are collected into `unsafe_refs`; labels that fail the line-label pattern are collected into `unsafe_line_labels`; neither weakens the portfolio's safe surface. The builder rejects non-`YYYYMMDD` dates and never opens file bodies, crawls `runtime-boundary/`, calls `subprocess`, reads `.git/`, calls `date.today()`, or contacts the network. The writer persists JSON/Markdown only under `runtime-boundary/codebase-evidence-portfolio/<YYYYMMDD>/portfolio-report.{json,md}` through `resolve_instance_runtime_ref` and rejects non-`YYYYMMDD` dates. The report carries `schema_id="hisys.codebase_evidence_portfolio.v1"`, `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, `raw_source_content_persisted=false`, and `allowed_actions="advisory_only"`. No CLI surface (deferred to `M22-PORTFOLIO-CLI`), no live access, no credential lookup, no artifact repair/deletion, no subagent execution, no LSP subprocess, and no raw source archival. |
| DARS panel local completion audit (DARS-PANEL-RLOOP-OPT-1) | docs/plans/dars-panel-completion-before-codebase-return.md; ralph.md Section 5.1.2.1 and Section 16; docs/reports/dars-panel-local-completion-audit.md; docs/traceability/dars-critic-panel-runtime-traceability.md DARS-PANEL-RLOOP-OPT-1; src/hisys/cli/main.py `run-dars-panel-golden` and `dars-panel-readiness` subcommands; src/hisys/operations/dars_panel_readiness.py; tests/fixtures/dars_panel/golden_basic/; tests/unit/test_dars_critic_panel_cli.py; tests/unit/test_dars_panel_readiness.py | Implemented: ran the fixture-local `hisys run-dars-panel-golden` and `hisys dars-panel-readiness --write-report` surfaces against a private `mktemp` instance root and recorded the deterministic JSON/Markdown evidence in `docs/reports/dars-panel-local-completion-audit.md`. The readiness surface field-level evidence pins the four completion boundaries: fixture/local panel complete (`fixture_panel_complete=true`, `golden_fixture_available=true`, `operator_report_available=true`), localhost rehearsal available but human-gated (`localhost_rehearsal_available=true`, `localhost_rehearsal_human_gated=true`), remote subscription dispatch present only through injected executor / fake tests (`remote_subscription_policy_exists=true`, `remote_subscription_injected_executor_harness_available=true`), and live provider execution not proven (`live_provider_execution_smoked=false`, `live_external_action_authorized=false`, `completion_claim="local_fixture_localhost_controlled_advisory_complete"`). The audit explicitly searched for additional safe local DARS panel completion candidates (extra fixture variants, additional wrappers, additional remote dispatch matrix rows, multi-critic fixture rounds) and found none — the next safe Ralph queue row is `MB-CODEBASE-M21-6-PREP`. No live model call, real remote provider call, credential lookup, raw secret capture, browser/search/tool execution, publication, deployment, schema/data migration against non-fixture data, force push, new remote configuration, or destructive operation was performed by this audit. |
| DARS remote subscription multi-critic panel dispatch (M24) | docs/contracts/dars-remote-subscription-backend-policy.md; src/hisys/agents/dars_remote_subscription_dispatch.py; tests/unit/test_dars_remote_subscription_dispatch.py | Implemented: `run_dars_remote_subscription_panel_dispatch(...)` composes at least two `RemoteSubscriptionDispatchRequest` critic calls into one governed panel boundary record while reusing the M-DARS-BE-6 activation packet, Codex/Claude subscription policy packet, and explicit injected executor gate for each critic. Panel shape mismatches such as mixed `request_id` values fail closed before executor contact. Successful fixture/injected execution writes per-critic records under `runtime-boundary/dars-remote-subscriptions/<YYYYMMDD>/<REQUEST_ID>/<BACKEND_ID>-<SOURCE_EXECUTION_ID>.json/.md` and a panel aggregate under `runtime-boundary/dars-remote-subscription-panels/<YYYYMMDD>/<REQUEST_ID>/<PANEL_ID>.json/.md` with `external_call_made=true`, `model_boundary_crossed=true`, `local_model_call_made=false`, `mutation_performed=false`, `publication_performed=false`, `allowed_actions=advisory_only`, and `requires_human_review=true`. Hisys still does not resolve credentials, store raw secrets, publish, mutate state, or call a real provider by default. |
| DARS remote subscription dispatch harness mismatch-coverage matrix (M-DARS-BE-6.2) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-6; ralph.md M-DARS-BE-6.2; src/hisys/agents/dars_remote_subscription_dispatch.py; tests/unit/test_dars_remote_subscription_dispatch.py | Implemented hardening: locked down the remaining activation/policy mismatch code paths in the M-DARS-BE-6 dispatch harness with one parametrized test and one focused test. The parametrized matrix `test_remote_subscription_dispatch_rejects_activation_policy_mismatches` covers six (mutation, expected ValueError code) rows: `activation_approval_ref_mismatch`, `activation_backend_id_mismatch`, `activation_backend_kind_mismatch`, `invalid_allowed_actions_in_activation`, `invalid_access_mode_in_policy`, and `audit_required_must_be_true_in_policy`. The follow-on focused test `test_remote_subscription_dispatch_rejects_activation_remote_policy_ref_mismatch` proves the harness fails closed with `activation_remote_policy_ref_mismatch` when the activation packet references a different policy path than the request supplies. Each row asserts that the boundary-record directory under `runtime-boundary/dars-remote-subscriptions/` is never created and the guard executor is never called. The harness implementation is unchanged; together with M-DARS-BE-6.1 the dispatch harness now has 17 focused tests pinning the activation/policy/executor/request-shape boundary by code. No live model call, no remote API call, no credential lookup, no provider account use, no deployment/publication/vault mutation, no remote push authority change. |
| DARS remote subscription dispatch harness defense-in-depth tests (M-DARS-BE-6.1) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-6; ralph.md M-DARS-BE-6.1; src/hisys/agents/dars_remote_subscription_dispatch.py; tests/unit/test_dars_remote_subscription_dispatch.py | Implemented hardening: locked down the M-DARS-BE-6 harness boundary with eight additional fixture-only tests covering (a) `remote_subscription_executor_required` when `executor=None` even with valid activation/policy and matching `approval_ref`, (b) `remote_subscription_executor_empty_output` for whitespace-only executor output, (c) `remote_subscription_executor_empty_output` for non-string executor output, (d) `backend_activation_packet_required` when the activation packet file is missing, (e) `remote_policy_packet_required` when the policy packet file is missing, (f) `invalid_request_id` when `request_id` carries disallowed characters, (g) `invalid_date_partition` when `yyyymmdd` is malformed, and (h) `activation_endpoint_scope_mismatch` when activation declares `endpoint_scope=localhost_only`. Each failure case asserts that the boundary record directory is not materialized (where applicable) and that any guard executor is never called. The harness implementation is unchanged; this increment only extends coverage so future regressions cannot silently weaken these failure modes. No live model call, no remote API call, no credential lookup, no provider account use, no deployment/publication/vault mutation, and no remote push authority change. |
| DARS remote subscription dispatch harness (M-DARS-BE-6) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-6; docs/contracts/dars-remote-subscription-backend-policy.md; src/hisys/agents/dars_remote_subscription_dispatch.py; tests/unit/test_dars_remote_subscription_dispatch.py | Implemented first dispatch increment: `run_dars_remote_subscription_dispatch(...)` composes a backend activation packet and Codex/Claude subscription policy packet, blocks mismatches before executor contact, and writes `runtime-boundary/dars-remote-subscriptions/<YYYYMMDD>/<REQUEST_ID>/<BACKEND_ID>-<SOURCE_EXECUTION_ID>.json/.md`. Execution is through an explicitly injected subscription executor; Hisys still does not resolve credentials, store raw secrets, publish, mutate state, or call a real provider by default. |
| DARS critic panel remote subscription dispatch guard (M-CP-LIVE-5 / M-DARS-BE-5) | docs/traceability/dars-critic-panel-runtime-traceability.md M-CP-LIVE-5; docs/contracts/dars-remote-subscription-backend-policy.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_adapters.py | Implemented: `CriticAdapterRegistry.resolve(...)` now keeps external critic adapters fail-closed even when `external_dispatch_allowed=True` and an `approval_ref` is supplied. The new test `test_critic_adapter_registry_blocks_external_dispatch_even_with_policy_approval` proves that remote subscription metadata remains only a precondition marker and cannot be interpreted as panel dispatch authority. |
| DARS remote subscription policy packet (M-DARS-BE-5) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-5; ralph.md M-DARS-BE-5; src/hisys/agents/dars_remote_subscription_policy.py; tests/unit/test_dars_remote_subscription_policy.py; docs/contracts/dars-remote-subscription-backend-policy.md | Implemented: `validate_dars_remote_subscription_policy_packet(data, *, config_ref, now=None)` returns a deterministic `ConfigValidationReport` for future Codex/Claude subscription-backed DARS providers without authorizing any remote dispatch. Even a fully valid packet emits a deterministic `remote_dispatch_not_implemented` **warning** so callers cannot interpret schema validity as live authority. Required string fields: `policy_id`, `approval_ref`, `operator_id`, `provider_id`, `access_mode`, `subscription_account_ref`, `adapter_class`, `redaction_policy_ref`, `egress_scope`, `expires_at`, `revocation_ref`; required boolean: `audit_required=true`. Provider allowlist: `codex` and `claude` only; adapter class must match the provider's allowed adapter (`codex_subscription` / `claude_subscription`). Access mode must be `subscription` (raw API-key / pay-per-call / provider-token modes are out of scope). Endpoint URL fields (`endpoint`, `endpoint_url`, `base_url`, `api_url`, `api_base`) are forbidden so subscription access cannot be reshaped into an arbitrary API path. Raw secret-like fields and raw secret-value prefixes are rejected; subscription access is mediated through `subscription_account_ref` (e.g. `vault://`) only. Mutation/publication/tool/browser/search authority flags fail closed. `expires_at` is checked against an injected `now` so tests and runtime callers do not depend on wall-clock time. The validator performs no HTTP call, no model call, no credential lookup, no provider dispatch, and no deployment/publication/vault mutation. The contract document `docs/contracts/dars-remote-subscription-backend-policy.md` pins the deterministic issue-code list, boundary invariants, relationship to M-DARS-BE-1/2/3, and explicit stop conditions for any consumer that would advance toward remote dispatch. |
| DARS local backend operator smoke packet (M-DARS-BE-4) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-4; ralph.md M-DARS-BE-4; docs/examples/dars/backend-activation-localhost.example.json; docs/runbooks/dars-live-backend-localhost-smoke.md; tests/unit/test_dars_live_backend_runbook.py | Implemented: human-gated rehearsal runbook and a copy-editable, secret-free localhost activation packet example. The example declares `endpoint_scope=localhost_only`, `allowed_actions=advisory_only`, `human_approved=true`, an explicit `approval_ref`, and an `expires_at` window; it carries no `api_key`/`token`/`secret`/`password`/`credential` field and no value with raw secret-like prefixes (`sk-`, `ghp_`, etc.). The runbook pins the M-DARS-BE-4 contract: operator-supplied localhost endpoint, no model runner installed/started by Hisys, no Authorization header, no credential lookup, no remote API, no tool/search/browser permission, no mutation request, no publication, and explicit stop conditions for non-loopback endpoint, missing activation packet, credential requirement, tool/search/browser permission, mutation request, failed secret scan, and human uncertainty. The runbook documents the M-DARS-BE-3 record location at `runtime-boundary/dars-backends/<YYYYMMDD>/<REQUEST_ID>/<BACKEND_ID>.{json,md}` and references the M-DARS-BE-1/2/3 test surfaces. The smoke tests assert the example shape and the runbook invariants without starting a model runner, opening a network boundary, or invoking credentials. No live model call, no remote API call, no credential lookup, no provider account use, no deployment/publication/vault mutation, no remote push authority change. |
| DARS backend-boundary decision record (M-DARS-BE-3) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-3; ralph.md M-DARS-BE-3; src/hisys/agents/dars_backend_boundary.py; src/hisys/agents/dars.py; tests/unit/test_dars_backend_boundary.py | Implemented: `write_dars_backend_boundary_record(instance, *, yyyymmdd, request_id, backend_id, backend_kind, endpoint_scope, approval_ref, activation_ref)` persists a JSON/Markdown pair at `runtime-boundary/dars-backends/<YYYYMMDD>/<REQUEST_ID>/<BACKEND_ID>.{json,md}` whenever `DarsRuntime.run_configured_critique(...)` completes a successful `openai_compatible` localhost-only model boundary crossing. The schema id is `hisys.dars.backend_boundary` and the record always carries `model_boundary_crossed=true`, `local_model_call_made=true`, `external_call_made=false`, `mutation_performed=false`, `publication_performed=false`, `allowed_actions=advisory_only`, and `requires_human_review=true`. The writer rejects non-`YYYYMMDD` dates and any `endpoint_scope` other than `localhost_only` so remote dispatch stays fail-closed preparation only. The legacy `_write_local_llm_boundary` record under `runtime-boundary/dars/<YYYYMMDD>/dars-local-llm-boundary-<REQUEST_ID>.json` is preserved unchanged. No live model call, no remote API call, no credential lookup, no provider account use, no deployment/publication/vault mutation, no remote push authority change. |
| DARS local backend activation integration (M-DARS-BE-2) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-2; ralph.md M-DARS-BE-2; src/hisys/agents/dars.py; src/hisys/cli/main.py; tests/unit/test_dars_runtime.py | Implemented: `DarsRuntime.run_configured_critique(..., backend_activation_packet_ref=None)` is now the enforcement boundary for the M-DARS-BE-1 backend activation packet. The runtime keeps `DarsDispatchGate.evaluate(...)` as the dispatch/appraiser gate, runs it first, and only after dispatch returns `allowed` it validates the activation packet for backends that cross a model/backend boundary. For `openai_compatible` backends the helper `_enforce_backend_activation_for_openai_compatible(...)` loads the JSON packet from the supplied path, calls `validate_dars_backend_activation_packet(...)` from M-DARS-BE-1, and raises `ValueError` with deterministic runtime reason codes before `_run_openai_compatible_backend(...)` is called: `backend_activation_packet_required` (missing/unreadable/non-object/invalid packet), `activation_approval_ref_mismatch` (packet `approval_ref` does not match the call-site `approval_ref`), `activation_backend_id_mismatch`, `activation_backend_kind_mismatch`, and `activation_endpoint_scope_mismatch` (packet must declare `endpoint_scope=localhost_only` for local model rehearsal). Loopback, `cli_agent`, fixture-text, and run-fixture-critique callers remain unchanged and do not require activation. The boundary record at `_write_local_llm_boundary(...)` now persists `backend_activation_packet_ref` alongside the existing `approval_ref` / `endpoint_scope` / `model_boundary_crossed` / `local_model_call_made` / `external_call_made` / `mutation_performed` fields. The CLI `request-dars-critique` parser exposes `--backend-activation-packet <json-path>` as an optional pass-through; the runtime — not the CLI — is the enforcement boundary, so direct Python calls to `DarsRuntime.run_configured_critique(...)` cannot bypass activation. The integration performs no live model call, no remote API call, no credential lookup, no provider account use, no deployment/publication/vault mutation, and no remote push authority change. |
| DARS backend activation packet validator (M-DARS-BE-1) | docs/plans/dars-live-backend-implementation-plan.md M-DARS-BE-1; ralph.md M-DARS-BE-1; src/hisys/agents/dars_backend_activation.py; tests/unit/test_dars_backend_activation.py | Implemented: `validate_dars_backend_activation_packet(data, *, config_ref, now=None)` returns a deterministic `ConfigValidationReport` for DARS backend activation packets without crossing any model, network, credential, or filesystem boundary. The validator pins the M-DARS-BE-1 issue-code contract — `external_backend_requires_remote_policy_packet`, `raw_secret_value_not_allowed`, `invalid_allowed_actions`, `missing_approval_ref`, `human_approval_required`, `activation_expired`, `invalid_endpoint_scope`, plus `missing_required_field` / `invalid_expires_at` — and accepts `endpoint_scope` only as `localhost_only` or `external_api`. Expiry is checked against an injected ISO-8601 / `datetime` `now` so tests do not depend on wall-clock time. Secret rejection is bounded to obvious secret-like field name markers (`api_key`, `auth_token`, `secret`, `password`, `credential`, `token`, etc., excluding the controlled `approval_ref`) and to obvious raw secret-value prefixes (`sk-`, `sk_`, `ghp_`, `xoxb-`, `xoxp-`, `hf_`). `external_api` endpoint scope requires a non-empty `remote_policy_packet_ref` but does not authorize remote dispatch — actual remote provider use stays fail-closed and is reserved for the future M-DARS-BE-5 Codex/Claude subscription policy packet. The module performs no HTTP, no model call, no credential lookup, no provider selection, no deployment/publication, no remote push authority, and no mutation; it only emits a deterministic validation report. |
| DARS live backend implementation plan (M-DARS-BE-PREP) | docs/plans/dars-live-backend-implementation-plan.md; ralph.md live backend Prepare checkpoint; src/hisys/agents/{dars.py,dars_config.py,dars_dispatch.py,dars_panel_live_config.py,dars_panel_live_adapter.py}; docs/runbooks/dars-live-panel-localhost-smoke.md | Prepared: defines the next controlled DARS backend line after local panel activation/fake-server/CLI/runbook work. The plan selects localhost-only `openai_compatible` backend hardening as the first executable path, defines a backend activation packet, runtime-enforced local backend activation integration, backend runtime-boundary records, operator local smoke packet, and a separate remote subscription policy packet that remains fail-closed only. Activation packet enforcement is assigned to `DarsRuntime.run_configured_critique()` after `DarsDispatchGate.evaluate()` allows dispatch and immediately before model-boundary adapter execution; `DarsDispatchGate` remains a dispatch/appraiser decision gate and does not absorb activation-packet or remote-subscription schema validation. The validator plan pins deterministic issue codes, injected-time expiry checks, bounded raw-secret rejection, and runtime mismatch codes so missing/mismatched activation blocks before endpoint contact and cannot be bypassed through direct Python runtime calls. Remote scope is subscription-style access limited to Codex and Claude; raw API-key/provider-token integration, arbitrary OpenAI/Anthropic-compatible endpoints, and additional vendors are out of scope. This is a docs/control artifact: it performs no live model call, no remote API/subscription call, no credential lookup, no provider dispatch, no deployment/publication/vault mutation, and no new remote push authority. |
| Codebase domain current-artifact source-inspection bridge (M21.CA) | ralph.md M21 user-identified gap; src/hisys/domain/use_cases.py; tests/unit/test_domain_cli.py; tests/unit/test_codebase_domain_artifact_bridge.py | Implemented: `investigate-domain` for `domain=codebase` now recognizes read-only `current_artifact` source refs that resolve to local repository directories when no prebuilt `runtime_record` codebase-analysis refs are supplied. The `CodeInvestigationLayer` runs the existing deterministic local source-inspection pipeline to materialize `inventory.json`, `symbol-index.json`, `scope-map.json`, `validation-plan.json`, `risk-scan.json`, and `source-inspection-decision.json` under `runtime-boundary/codebase-analysis/<YYYYMMDD>/<REQUEST_ID>/`, then feeds those refs through the existing codebase bundle gate and enrichment path. The bridge preserves prior explicit-runtime-record behavior, reads no network/model/browser source, uses no credentials, executes no validation commands, persists no raw source content, reports `external_call_made=false` / `mutation_performed=false`, and grants no approval, deployment, publication, live-action, repair, or promotion authority. |
| Subagent evidence packet validator CLI wrapper (M21.9-CLI) | docs/plans/m21-9-cli-subagent-evidence-packet-validator-implementation-tasks.md; ralph.md M21.9-CLI; src/hisys/cli/main.py; src/hisys/contracts/subagent_evidence_collector.py; tests/unit/test_domain_cli.py | Implemented: `hisys validate-subagent-evidence-packet --task <json> [--result <json>]` exposes the M21.9.1 pure validator through a thin argparse subparser and dispatcher. `--task` is required and `--result` is optional; both are loaded only via explicit caller-supplied JSON object paths through the reused `_load_json_report` helper. The dispatcher calls `validate_subagent_evidence_task_packet(task_payload)` and, when a result is supplied, `validate_subagent_evidence_result_packet(result_payload, task=task)`, then prints bounded `validate-subagent-evidence-packet: ok`, `task_id`, `result_supplied`, `artifact_ref_count`, `source_ref_count`, `advisory_only: true`, `requires_human_review: true`, `external_call_made: false`, `mutation_performed: false`, `raw_source_content_persisted: false`, and `allowed_actions: advisory_only` lines. Returns exit code `0` when validation passes; `SubagentEvidenceValidationError` or `ValueError` from the loader propagates as a non-zero exit. The CLI never executes a subagent, never calls `delegate_task`, never spawns processes, never opens artifact_refs or source_refs to verify file existence, never auto-discovers packet files, never calls `subprocess`, never reads `.git/`, never accesses network/browser/model tools, never uses credentials, never writes runtime-boundary artifacts, never alters pass-contract reason codes or promotion semantics, and never adds approval/promotion authority. |
| Subagent evidence collector protocol validator (M21.9.1) | docs/plans/m21-9-subagent-evidence-collector-protocol-implementation-tasks.md; docs/contracts/subagent-evidence-collector-protocol.md; ralph.md M21.9/M21.9.1; src/hisys/contracts/subagent_evidence_collector.py; tests/unit/test_subagent_evidence_collector_protocol.py | Implemented: `validate_subagent_evidence_task_packet(payload)` and `validate_subagent_evidence_result_packet(payload, task=None)` provide a pure local-only schema/protocol validator for bounded read-only subagent evidence packets. Task packets require `schema_id="hisys.subagent_evidence.task.v1"`, non-empty identifiers/objective, safe relative repo/include/exclude refs, allowlisted read-only tools (`read_file`, `search_files`), `advisory_only=true`, and `requires_human_review=true`. Result packets require `schema_id="hisys.subagent_evidence.result.v1"`, matching task IDs when a task is supplied, safe relative artifact/source refs, `external_call_made=false`, `mutation_performed=false`, `raw_source_content_persisted=false`, and `parent_verification_required=true`. Validation errors carry deterministic local protocol codes such as `unsafe_ref`, `mutation_tool_not_allowed`, `external_call_not_allowed`, and `parent_verification_required_missing`. The validator does not execute subagents, call `delegate_task`, spawn processes, use network/browser/model tools, access credentials, read `.git/`, open artifact refs, verify file existence, persist raw source, mutate repositories, or alter pass-contract reason codes/promotion semantics. |
| Evaluate-code-analysis-contract CLI wrapper (M21.8-CLI) | docs/plans/m21-8-cli-evaluate-code-analysis-contract-implementation-tasks.md; ralph.md M21.8-CLI; src/hisys/cli/main.py; src/hisys/operations/code_analysis_pass_contract.py; src/hisys/contracts/{pass_registry.py,evaluator.py}; tests/fixtures/pass-contracts/code_analysis/*.json; tests/unit/test_domain_cli.py | Implemented: `hisys evaluate-code-analysis-contract --instance <root> --date <YYYYMMDD> --contract-ref <json> --question-type <name> [--coverage-report <json>] [--boundary-report <json>] [--freshness-report <json>] [--benchmark-report <json>] [--change-impact-report <json>] [--architecture-candidates-report <json>] [--human-approval-ref <token>]` exposes the M21.8.1 adapter and writer through a thin argparse subparser and dispatcher. `--question-type` is restricted to the five supported strings via argparse `choices=sorted(_SUPPORTED_QUESTION_TYPES)`. Report inputs are accepted only via explicit caller-supplied JSON file paths through the reused `_load_json_report` helper; the CLI does not crawl `runtime-boundary/` directories, does not infer latest artifacts, and does not read raw source. The CLI loads the pass-contract registry via `load_pass_contract_registry`, uses `entries[0]` (matching the existing `_cmd_evaluate_pass_contract` precedent), calls `build_code_analysis_evidence_summary(...)`, then `evaluate_pass_contract(entry, summary)`, then `write_code_analysis_pass_contract_evaluation(...)`. Prints bounded `evaluate-code-analysis-contract evaluation`, `contract_id`, `question_type`, `quality_gate`, `blockers` (comma-separated or `none`), `advisory_only: true`, `requires_human_review: true`, `external_call_made: false`, `mutation_performed: false`, `raw_source_content_persisted: false`, and `allowed_actions: advisory_only`. Returns exit code `0` whenever an evaluation artifact is written, regardless of the quality gate; adapter or writer `ValueError` propagates as a non-zero exit. The CLI never calls `date.today()`, never reads `.git/`, never calls `subprocess`, never expands the question-type or reason-code taxonomy, never mutates the contract registry, and never advances any candidate to active. |
| Code-analysis pass-contract fixture registry contracts (M21.8.2) | docs/plans/m21-8-code-analysis-pass-contract-loop-implementation-tasks.md; ralph.md M21.8.2; tests/fixtures/pass-contracts/code_analysis/{traceability_coverage_review,runtime_boundary_consistency_review,codebase_map_freshness_review,change_impact_review,architecture_candidate_review}.json; tests/unit/test_code_analysis_pass_contract_fixtures.py | Implemented: five fixture pass-contract entries under `tests/fixtures/pass-contracts/code_analysis/` provide the M21.8 PREP code-analysis question-type taxonomy as `status=candidate`, `active=false`, `automatic_promotion_allowed=false`, `external_call_made=false`, `mutation_performed=false`, `publication_or_live_action_approved=false`, `human_approval_ref=null`, `promotion_gate=human_reviewed_traceable_change`. Contract IDs are `code_analysis_{traceability_coverage,runtime_boundary_consistency,codebase_map_freshness,change_impact,architecture_candidate}_review_v0_1_candidate`. `minimum_evidence` keys are limited to the existing taxonomy (`artifact_refs_required`, `alternative_set_required`, `claim_coverage_required`, `contradiction_check_required`, `dars_critique_required`); `blocked_if` codes are limited to existing reasons (`no_traceable_artifact_refs`, `alternative_set_incomplete`, `claim_coverage_incomplete`, `contradiction_unchecked`, `dars_critique_missing`, `boundary_violation_detected`, `human_approval_required`). Each contract's gate composition mirrors what the M21.8.1.A..D adapter naturally emits for its question type: traceability-coverage/runtime-boundary/freshness contracts require artifact-refs + claim-coverage; change-impact additionally requires cross-signal contradiction-check; architecture-candidate additionally requires alternative-set + contradiction-check. No fixture sets `dars_critique_required` because the M21.8.1 adapter does not populate `dars_critique_refs`. The fixture sanity test verifies file presence, taxonomy limits, candidate-only status, and end-to-end `evaluate_pass_contract` results for representative clean/blocked payloads from `build_code_analysis_evidence_summary`. No CLI surface, no new pass-contract reason codes, no promotion authority, no live access, no credential lookup, no `subprocess`, no `.git/` read, no `date.today()`. |
| Code-analysis pass-contract evidence adapter (M21.8.1.A..D) | docs/plans/m21-8-code-analysis-pass-contract-loop-implementation-tasks.md; ralph.md M21.8.1.A, M21.8.1.B, M21.8.1.C, and M21.8.1.D; src/hisys/operations/code_analysis_pass_contract.py; tests/unit/test_code_analysis_pass_contract.py | Implemented: `build_code_analysis_evidence_summary(*, question_type, coverage_report=None, boundary_report=None, freshness_report=None, benchmark_report=None, change_impact_report=None, architecture_candidates_report=None)` and `write_code_analysis_pass_contract_evaluation(*, instance_root, date, contract_id, result, human_approval_ref=None)` provide a pure local-only adapter and writer that map caller-supplied trusted M21.1..M21.7 dict payloads into the existing `hisys.contracts.evaluator.EvidenceSummary` value without adding fields to `EvidenceSummary`, `PassContractRegistryEntry`, `PassContractEvaluationResult`, or `NeedsMoreEvidenceReason`. All five M21.8 PREP question types are now supported: `traceability_coverage_review` (maps M21.1 unreferenced-requirement count to `claims_covered`); `runtime_boundary_consistency_review` (maps M21.3 `unsafe_refs` / `outside_runtime_boundary_refs` to `boundary_violation_detected` and lists problematic refs as `artifact_refs`); `codebase_map_freshness_review` (maps M21.4 `unsafe_partitions` to `boundary_violation_detected`, requires non-empty `fresh_partitions` with empty `stale_partitions`/`incomplete_partitions` for `claims_covered=true`, and lists problematic-or-fresh partition refs as `artifact_refs`); `change_impact_review` (maps M21.6 `unsafe_changed_refs` to `boundary_violation_detected`, requires impacted-signal present AND no `unmapped_changed_refs` for `claims_covered=true`, lists impacted requirement/test/design/runtime refs plus unmapped refs as `artifact_refs`, and sets `contradiction_checked=true` when an M21.1 coverage payload is provided as cross-signal); and `architecture_candidate_review` (maps M21.7 candidate count to `alternative_count`, deduplicates candidate `supporting_refs` into `artifact_refs`, sets `boundary_violation_detected=true` when M21.4 `unsafe_partitions` or M21.6 `unsafe_changed_refs` is non-empty in cross-signal payloads, requires non-empty candidates AND no boundary violation for `claims_covered=true`, and sets `contradiction_checked=true` only when all three cross-signal reports — M21.1, M21.4, M21.6 — are provided). Unknown question types raise `ValueError`. The adapter never opens a file body, never crawls `runtime-boundary/`, never calls `subprocess`, never reads `.git/`, and never calls `date.today()`. The writer persists JSON/Markdown only under `runtime-boundary/code-analysis-pass-contracts/<YYYYMMDD>/<contract_id>-evaluation.{json,md}` through `resolve_instance_runtime_ref`, rejects non-`YYYYMMDD` dates, records `human_approval_ref` verbatim when supplied, and carries `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, `raw_source_content_persisted=false`. No CLI surface, no fixture registry contracts, no new pass-contract reason codes, no promotion authority, no live access, no credential lookup, no artifact repair/deletion. |
| Architecture candidates CLI wrapper (M21.7-CLI) | docs/plans/m21-7-cli-architecture-candidates-cli-wrapper-implementation-tasks.md; ralph.md M21.7-CLI; src/hisys/cli/main.py; src/hisys/operations/architecture_candidates.py; tests/unit/test_domain_cli.py | Implemented: `hisys architecture-candidates --instance <root> --date <YYYYMMDD> [--coverage-report <json>] [--freshness-report <json>] [--change-impact-report <json>] [--current-head-short <hash>]` exposes the M21.7 pure generator through a thin argparse subparser and dispatcher. Input reports are accepted only via explicit caller-supplied JSON file paths; the CLI does not crawl `runtime-boundary/` directories, does not infer latest artifacts, and does not read raw source. Missing report arguments become `None` inputs to the pure generator. `--current-head-short` is optional and recorded verbatim. The CLI calls `build_architecture_candidate_report` and `write_architecture_candidate_report` without reshaping candidate semantics, prints bounded `candidate_count`, `advisory_only: true`, `requires_human_review: true`, `external_call_made: false`, `mutation_performed: false`, `raw_source_content_persisted: false`, and `allowed_actions: advisory_only` summary lines, and always returns exit code `0`. The CLI never calls `date.today()`, never reads `.git/`, never calls `subprocess`, never expands the candidate-kind or recommendation-strength vocabulary, and never adds approval/readiness wording. |
| Architecture candidate generator (M21.7) | docs/plans/m21-7-architecture-candidate-generator-implementation-tasks.md; ralph.md M21.7; src/hisys/operations/architecture_candidates.py; tests/unit/test_architecture_candidates.py | Implemented: `ArchitectureCandidateInputs`, `ArchitectureCandidate`, `ArchitectureCandidateReport`, `build_architecture_candidate_report`, `render_architecture_candidates_markdown`, and `write_architecture_candidate_report` provide a pure local-only advisory generator that consumes caller-supplied trusted M21.1 `hisys.traceability.coverage.v1`, M21.4 `hisys.codebase_map.freshness.v1`, and M21.6 `hisys.change_impact.v1` dict payloads and emits bounded `ArchitectureCandidate` records with `kind` limited to `coverage_gap`, `freshness_drift`, `change_impact_concentration`, or `cross_signal_alignment` and `recommendation_strength` limited to `advisory_candidate` or `advisory_candidate_low_evidence`. Candidate `summary`/`rationale` strings are pinned by `test_build_architecture_candidate_report_rejects_imperative_wording` to never contain `recommended`, `required`, `approved`, `must`, `next step`, or `should`. The generator reads no file bodies, never calls `subprocess`, never reads `.git/`, never calls `date.today()`. The writer persists JSON/Markdown only under `runtime-boundary/architecture-candidates/<YYYYMMDD>/architecture-candidates-report.{json,md}` through `resolve_instance_runtime_ref` and rejects non-`YYYYMMDD` dates. The report carries `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, and `raw_source_content_persisted=false`. No CLI surface, no live access, no credential lookup, no artifact repair/deletion, no stronger recommendation labels, and no architectural change proposals naming specific refactors/replacements. |
| Change-impact CLI wrapper (M21.6-CLI) | docs/plans/m21-6-cli-change-impact-cli-wrapper-implementation-tasks.md; ralph.md M21.6-CLI; src/hisys/cli/main.py; src/hisys/operations/change_impact.py; tests/unit/test_domain_cli.py | Implemented: `hisys change-impact --instance <root> --date <YYYYMMDD> [--repo <repo>] [--changed-ref <relative-ref>]* [--current-head-short <hash>]` exposes the M21.6 pure analyzer through a thin argparse subparser and dispatcher. Changed refs are accepted only via repeatable `--changed-ref` flags; a `--from-git-diff` / `--scan` mode is intentionally deferred. `--repo` defaults to the current working directory and is used only by the bounded `load_repo_traceability_anchors` loader. The CLI calls `build_change_impact_report` and `write_change_impact_report` without reshaping report semantics, prints bounded count and `external_call_made: false` / `allowed_actions: advisory_only` summary lines, and always returns exit code `0` (advisory-only). The CLI never calls `date.today()`, never reads `.git/`, never shells out to `git diff` / `git log`, and never calls `subprocess`. No live access, credential lookup, artifact repair/deletion, or approval authority is added. |
| Change-impact analyzer (M21.6) | docs/plans/m21-6-change-impact-analyzer-implementation-tasks.md; ralph.md M21.6; src/hisys/operations/change_impact.py; tests/unit/test_change_impact.py | Implemented: `ChangeImpactRequest`, `ChangeImpactReport`, `build_change_impact_report`, `render_change_impact_markdown`, and `write_change_impact_report` provide a pure local-only advisory analyzer that maps caller-supplied changed file refs to impacted requirement IDs, test IDs/refs, design/interface refs, runtime-boundary refs, unmapped refs, and unsafe refs by reading only the M21.1 `TraceabilityAnchors` value. The analyzer never shells out to `git`, never calls `subprocess`, never reads `.git/`, never calls `date.today()`, and never opens changed-file bodies. The writer persists JSON/Markdown only under `runtime-boundary/change-impact/<YYYYMMDD>/impact-report.{json,md}` through `resolve_instance_runtime_ref` and rejects non-`YYYYMMDD` dates. The report carries `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, and `raw_source_content_persisted=false`. No CLI surface, no live access, no credential lookup, no artifact repair/deletion, and no test execution authority. |
| Codebase regression benchmark fixtures (M21.5) | docs/plans/m21-5-regression-benchmark-fixture-repositories-implementation-tasks.md; ralph.md M21.5; src/hisys/operations/codebase_regression_benchmarks.py; tests/unit/test_codebase_regression_benchmarks.py; tests/fixtures/codebase_repos/benchmark_manifest.json | Implemented: `BenchmarkFixture`, `BenchmarkFixtureManifest`, `build_codebase_regression_benchmark_report`, `load_codebase_regression_benchmark_manifest`, and `write_codebase_regression_benchmark_report` provide a manifest-driven, local-only advisory benchmark surface for tiny synthetic fixture repositories. The fixture set covers empty repository, single Python module, docs/code mix, missing test anchor, and malformed runtime-ref expectation cases. Reports persist only JSON/Markdown under `runtime-boundary/codebase-regression-benchmarks/<YYYYMMDD>/benchmark-report.{json,md}` and carry `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, and `raw_source_content_persisted=false`. No CLI surface, live clone/network, credential lookup, package install, broad raw source archival, repair/delete, or publication authority is added. |
| Codebase-map-freshness-review CLI wrapper (M21.4-CLI) | docs/plans/m21-4-cli-codebase-map-freshness-review-implementation-tasks.md; ralph.md M21.4-CLI; src/hisys/cli/main.py; src/hisys/operations/codebase_map_freshness.py; tests/unit/test_domain_cli.py | Implemented: `hisys codebase-map-freshness-review --instance <root> --date <YYYYMMDD> --current-date <YYYY-MM-DD> --max-age-days <int> [--current-head-short <hash>]` exposes the M21.4 pure checker through a thin argparse subparser and dispatcher. `--current-date` is required and parsed with `date.fromisoformat`; `--max-age-days` is required (no default); `--current-head-short` is optional and recorded verbatim. The CLI calls `build_codebase_map_freshness_report` and `write_codebase_map_freshness_report` without reshaping report semantics, prints bounded partition counts and `external_call_made: false` / `allowed_actions: advisory_only` summary lines, and always returns exit code `0`. The CLI never calls `date.today()`, never reads `.git/`, and never opens artifact bodies. |
| Codebase map freshness review (M21.4) | docs/plans/m21-4-codebase-map-freshness-drift-review-implementation-tasks.md; ralph.md M21.4; src/hisys/operations/codebase_map_freshness.py; tests/unit/test_codebase_map_freshness.py | Implemented: `build_codebase_map_freshness_report` is a pure local-only checker over caller-supplied `instance_root`, `current_date`, `max_age_days`, and optional `current_head_short`. Each existing `runtime-boundary/codebase-analysis/<YYYYMMDD>/<REQUEST_ID>/` partition is classified as `fresh`, `stale`, `incomplete`, or `unsafe_partition` based on directory listings and file presence only — artifact bodies are never opened, `date.today()` is never called, and `.git/` is never read. `write_codebase_map_freshness_report` persists JSON/Markdown only under `runtime-boundary/codebase-map-freshness/<YYYYMMDD>/`. The report carries `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, and `raw_source_content_persisted=false`. No CLI surface, no live access, no credential lookup, no artifact repair/regeneration/deletion. |
| Runtime-boundary-check CLI wrapper (M21.3-CLI) | docs/plans/m21-3-cli-runtime-boundary-check-implementation-tasks.md; ralph.md M21.3-CLI; src/hisys/cli/main.py; src/hisys/operations/runtime_boundary_consistency.py; tests/unit/test_domain_cli.py | Implemented: `hisys runtime-boundary-check --instance <root> --date <YYYYMMDD> [--ref <relative-ref>]*` exposes the M21.3 pure checker through a thin argparse subparser and dispatcher. Refs are accepted only via repeatable `--ref` flags; a recursive `--scan` mode is intentionally deferred. The CLI calls `build_runtime_boundary_consistency_report` and `write_runtime_boundary_consistency_report` without reshaping report semantics, prints bounded refs/counts and `external_call_made: false` / `allowed_actions: advisory_only` summary lines, and always returns exit code `0` (advisory-only). No live access, credential lookup, artifact repair/deletion, or approval authority is added. |
| Runtime-boundary consistency checker (M21.3) | docs/plans/m21-3-runtime-boundary-consistency-checker-implementation-tasks.md; ralph.md M21.3; src/hisys/operations/runtime_boundary_consistency.py; tests/unit/test_runtime_boundary_consistency.py | Implemented: `build_runtime_boundary_consistency_report` is a pure local-only checker over caller-supplied runtime-boundary refs that classifies each ref into `ok`, `unsafe_ref`, `missing_file`, `malformed_json`, `missing_markdown_pair`, `missing_advisory_flag`, or `outside_runtime_boundary`. All file reads pass through the existing `resolve_instance_runtime_ref` chokepoint; refs that escape the instance root or use `..` traversal are rejected. `write_runtime_boundary_consistency_report` persists JSON/Markdown only under `runtime-boundary/runtime-boundary-consistency/<YYYYMMDD>/`. The report carries `advisory_only=true`, `requires_human_review=true`, `external_call_made=false`, `mutation_performed=false`, and `raw_source_content_persisted=false`. No CLI surface, no live access, no credential lookup, no artifact repair/deletion. |
| Traceability coverage CLI wrapper (M21.2) | docs/plans/m21-2-traceability-coverage-cli-wrapper-implementation-tasks.md; ralph.md M21.2; src/hisys/cli/main.py; src/hisys/operations/traceability_coverage.py; scripts/report_traceability_coverage.py; tests/unit/test_domain_cli.py; tests/unit/test_traceability_coverage.py | Implemented: `hisys traceability-coverage --instance <root> --date <YYYYMMDD> [--repo <repo>]` calls the bounded local anchor loader and M21.1 coverage reporter, writes the same JSON/Markdown runtime-boundary artifacts, and prints advisory-only/no-external-call summary lines. The shared loader records only IDs and relative refs; no raw source text, credentials, live access, publication, or approval authority is added. |
| Traceability coverage report (M21.1) | docs/plans/m21-1-traceability-coverage-report-implementation-tasks.md; ralph.md M21.1; src/hisys/operations/traceability_coverage.py; scripts/report_traceability_coverage.py; tests/unit/test_traceability_coverage.py; docs/traceability/README.md; HISYS-REPO-001; HISYS-IMP-001; HISYS-IDD-001 | Implemented: `build_traceability_coverage_report` computes deterministic requirement coverage, unreferenced requirement IDs, orphan test IDs, and advisory boundary flags from bounded anchor IDs without embedding raw source text. `write_traceability_coverage_report` writes JSON/Markdown only under `runtime-boundary/traceability-coverage/<YYYYMMDD>/` through `resolve_instance_runtime_ref`; `scripts/report_traceability_coverage.py` provides the M21.1 standalone script wrapper without adding a Hisys CLI subcommand. Tests pin the RED-start pure reporter behavior and bounded runtime artifact writer. |
| I0 Repository skeleton | HISYS-IMP-001 (implementation-plan.md) Section 3 | Complete |
| I1 Schemas and IDs | HISYS-IMP-001 Section 3; HISYS-SCHEMA-001 (schema-definitions.md) | Initial Pydantic v2 models for all named records |
| I2 Source governance | HISYS-IMP-001 Section 3; HISYS-SRC-REG-INIT-001; HISYS-CHECK-WEB-001 | Initial in-memory registry, fixture registry, and web compliance gate |
| I3 Adapter framework | HISYS-IMP-001 Section 3; HISYS-IDD-001 HISYS-IF-003/HISYS-IF-015; HISYS-FIXTURE-001 | Common DataSource contract, fixture adapters, registry-gated runtime, health report, and failure isolation |
| I4 Investigator foundation | HISYS-INST-INV-001; HISYS-RUNTIME-DIR-001; HISYS-HARNESS-GUIDE-001; HISYS-D-015; HISYS-D-016 | Runtime instance paths, YAML config loader, audit/observation persistence, Hermes boundary writer, Investigator collection skeleton, example instance, and CI smoke gate |
| I4 CLI glue | HISYS-INST-INV-001; HISYS-RUNTIME-DIR-001; HISYS-D-015; HISYS-D-016 | `hisys validate-config`, fixture-backed `hisys collect`, Hermes Markdown boundary record persistence, and JSON/Markdown run summary persistence |
| I4 Investigator direct memo foundation | HISYS-INST-INV-001; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-TPL-RESEARCH-SEARCH-001; HISYS-DATA-002 | `hisys investigate-memo` collects from registry-gated fixture sources, applies the research topic search template, writes linked `ExtractedSignal` evidence-interpretation records, and persists runtime-local investigation `ZettelMemo` JSON/Markdown plus report artifacts without copying raw payload into the memo |
| I5 Extraction foundation | HISYS-IMP-001 Section 3; HISYS-SCHEMA-001 Section 5; HISYS-FR-EXT-001..005 | Fixture-backed `RawObservation` -> `ExtractedSignal` extractor, local signal JSON persistence, and `hisys extract` CLI report path |
| I6 Editorial foundation and vault-write dry-run boundary | HISYS-IMP-001 Section 3; HISYS-SCHEMA-001 Sections 6-7; HISYS-FR-PER-001..004; HISYS-FR-MEM-001..005; HISYS-IF-007; HISYS-DATA-002; HISYS-DATA-005; HISYS-CON-012 | Fixture-backed active perspective application, `ZettelMemo` draft JSON/Markdown persistence, `hisys draft-memo` CLI report path, `hisys review-memos` duplicate/conflict flagging report path, and `hisys.integrations.obsidian_vault` dry-run vault-write previews with sanitized target paths and runtime-boundary reports while live vault writes remain disabled |
| I7-A/B/C/D/E/F/G/H/I Chief Editor alert decision, suppression, approval-gate, dry-run action-plan, approval-transition, send-candidate, disabled-connector, product-factory, and live-connector-control foundation | HISYS-IMP-001 Section 3; HISYS-SCHEMA-001 Section 8; HISYS-FR-CE-001..006; HISYS-CE-POLICY-001; HISYS-FR-AGT-004; HISYS-T-020; HISYS-T-022; HISYS-CON-010; HISYS-CON-012; HISYS-CON-022..023 | Fixture-backed Chief Editor policy reads runtime-local memo review outputs, persists `AlertDecisionRecord` JSON/Markdown decisions, records duplicate non-escalation decisions, suppresses same-date repeated `suppression_key` alert candidates, requests approval for high/critical or non-local target candidates, selects `analysis_only` or `alert_delivery_dry_run` products from config/CLI, writes `alert-decision-report.{json,md}`, applies runtime-local approve/reject transitions with `alert-approval-transition-report.{json,md}`, writes dry-run `alert-action-plan-report.{json,md}` with approved pending decisions marked as `would_send=true` candidates while live delivery remains disabled, writes disabled connector execution records/reports with `execution_status=blocked`, exposes `hisys decide-alerts`/`hisys review-alert-approval`/`hisys plan-alert-actions`/`hisys execute-alert-actions` without live alert sending, and `hisys.integrations.live_connectors` records blocked runtime-boundary decisions for requested live connector actions unless connector/action/approval gates are satisfied |
| I8-A/B/C/D DARS advisory handoff loopback, config-validation contract, data-format contract, and commercial registry/use-case design | HISYS-IMP-001 Section 3; HISYS-SCHEMA-001 Section 9; HISYS-FR-AGT-001..005; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-DARS-CONTRACT-001; HISYS-T-019; HISYS-T-020; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Runtime-local `hisys request-dars-critique` creates advisory-only `AgentHandoffPackage` JSON/Markdown records linked to disabled connector executions and returns loopback placeholder critique records by default because DARS is not implemented yet; artifacts record `dars_backend=loopback_placeholder`, `external_call_made=false`, `allowed_actions=advisory_only`, `action_taken=none`, and no live DARS call or external action occurs. `hisys.config.validation` and `hisys.agents.dars_config` validate common JSON config envelopes plus DARS role/backend specs from `config/dars.json` before any DARS adapter can run. `docs/architecture/design-philosophy.md` records the adopted philosophy: Hermes orchestrates, Hisys governs investigation/evidence/alternatives, DARS critiques advisory alternatives, and human/system governance approves consequential action; it also marks ontology-guided configuration suitability management as a future extension. `docs/contracts/dars-data-format.md` defines canonical `DarsRequestEnvelope` and `DarsResponseEnvelope` boundary artifacts plus progressive adversarial decision-trace/rubric-score fields for future DARS protocol models, `hisys.agents.dars_protocol` implements the canonical envelope validators with advisory-only safety rejection for mutation, execution, external side effects, and blocking behavior, and `hisys.agents.dars_dispatch` records runtime-boundary allow/block decisions before any backend adapter is invoked; `hisys.agents.dars_backend` implements a deterministic fixture-file backend that validates local fixture responses against the canonical envelope, writes validation reports for accepted/rejected outputs, and writes runtime-boundary response artifacts; `hisys.agents.dars_trace` writes end-to-end source/memo/alert/evidence-to-critique lineage artifacts; `DarsMockEndpointAdapter` and the `mock_endpoint` config entry define a disabled-by-default no-network mock endpoint boundary; `docs/contracts/dars-evaluation-rubrics.md` defines the controlled evaluation matrix and separate rubric-file loading rule; `docs/contracts/dars-prompt-registry.md` defines the adopted file/database-backed prompt registry for commercial prompt governance; `docs/contracts/configuration-governance.md` defines the adopted file/database-backed configuration registry strategy for non-prompt operational settings; `docs/use-cases/hermes-hisys-domain-tool.md` defines Hisys as a Hermes-callable domain-general investigation/DARS decision-support tool; `docs/use-cases/codebase-analysis-design-candidates.md` defines the read-only `domain="codebase"` specialization for current-codebase, approved open-source-reference, and previous-project-result evidence, producing design candidates, alternative decision sets, DARS critique, and better-use recommendations. |
| I9-A Secret/redaction scan baseline | HISYS-IMP-001 Section 3; HISYS-T-021; HISYS-NFR-SEC-001..002; HISYS-FR-ADM-001; HISYS-R-008 | `hisys.security.secret_scan` and `scripts/scan_secrets.py` scan repository/runtime text files for assignment-style secret-like values, skip runtime caches, and emit redacted reports only so quality-gate output does not leak matched values |
| I9-B Backup manifest and restore dry-run baseline | HISYS-IMP-001 Section 3; HISYS-T-023; HISYS-FR-ADM-003; HISYS-DATA-001..004 | `hisys.operations.backup` creates runtime-local zip backups plus SHA-256 manifests for controlled config/templates/harness/data/runtime-boundary/report files, excludes local-only secrets/tmp/cache/logs/backups, and verifies archive members through restore dry-run reports without writing restored files |
| I9-C Operator health status baseline | HISYS-IMP-001 Section 3; HISYS-FR-ADM-004; HISYS-T-006; HISYS-T-020; HISYS-T-023; HISYS-FR-AGT-004; HISYS-DARS-CONTRACT-001 | `hisys.operations.health` reports required runtime-directory readiness and disabled/loopback connector status without live external probes or side effects |
| I9-D Release-readiness evidence baseline | HISYS-IMP-001 Section 4; HISYS-T-024; HISYS-FR-ADM-001..004; HISYS-DATA-001..005; HISYS-CON-* | `hisys.operations.release_readiness` summarizes required quality gates, HISYS-T-024 trace-path evidence, known gaps, and the human-review/continue-hardening release decision without running live connectors |
| HISYS-T-027 Investigator multi-agent fixture research | docs/plans/investigator-multi-agent-research.md; HISYS-INST-INV-001; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-TPL-RESEARCH-SEARCH-001 | Implemented: `ResearchTask`, `EvidencePackage`, fixture research agents, evidence gate/merger, and `investigate-memo --agent ...` orchestration let multiple deterministic agents produce validated evidence packages before one template-based memo is built |
| HISYS-T-029 Formalism domain fixture research | HISYS-INST-INV-001; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-TPL-RESEARCH-SEARCH-001 | Implemented: `formalism_comparison` and `self_organization_mechanism` research agents produce domain-specific `EvidencePackage` claims about Dynamic Structure DEVS, graph rewriting, agent-based modeling, assessment criteria, selection heuristics, expressiveness/simulation/verification tradeoffs, self-organization mechanisms, and open questions for simulation semantics/proof/topology-change requirements |
| HISYS-T-030 Purpose-specific Investigator guidelines and evidence agents | HISYS-INST-INV-001; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-TPL-RESEARCH-SEARCH-001 | Implemented: `investigate-memo --purpose auto` selects `general_investigation`, `research_idea_discovery`, or `investment_decision_support` guideline profiles from topic/goal cues and records the selected `guideline_profile_id` in memo tags/body and the run report; `formalism_gap_analysis` produces explicit DSDEVS/graph-rewriting/ABM gap statements, hybrid novelty candidates, evaluation scenarios, and research questions; `investment_decision_support` produces bounded company fundamentals, market/competitor, valuation/risk, and needs-more-evidence decision-frame claims with a not-financial-advice safety limitation |
| HISYS-T-031 Purpose-aware automatic agent planning | HISYS-INST-INV-001; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-TPL-RESEARCH-SEARCH-001 | Implemented: if no explicit `--agent` is supplied, `investigate-memo` converts the selected guideline profile into a default evidence-agent plan: `research_idea_discovery` -> `formalism_gap_analysis`, `investment_decision_support` -> `investment_decision_support`, and `general_investigation` -> no extra agent; explicit `--agent` lists remain authoritative |
| HISYS-T-032 Configurable Investigator connector registry | HISYS-INST-INV-001; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-DATA-005; HISYS-TPL-RESEARCH-SEARCH-001 | Implemented: `investigator-agents.yaml` declares global connector safety policy, purpose default/optional agent plans, and disabled optional connector definitions for publisher web search, Claude/Codex read-only evidence extraction, local LLM offline mapping, market/news search, and company filing search; `investigate-memo` resolves default agents from config, records `agent_plan_source`, `disabled_optional_agent_refs`, and `blocked_agent_refs`, and blocks disabled explicit external connector requests before execution |
| HISYS-T-033 Orchestrator-to-Investigator harness source plan | HISYS-INST-INV-001; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-DATA-005; HISYS-TPL-RESEARCH-SEARCH-001 | Implemented: `investigate-memo --orchestrator-harness <json>` accepts orchestrator-planned `source_ids`, optional `agent_types`, and `user_opinion`, records `agent_plan_source=orchestrator_harness`, `harness_source_refs`, `orchestrator_harness_ref`, and `user_opinion`, and writes memo sections for harness/user-opinion context while still requiring source registry membership and blocking disabled external connectors before execution |
| HISYS-T-028-SEL Selenium read-only research harness | docs/plans/investigator-multi-agent-research.md; HISYS-CON-022..023; HISYS-D-015; HISYS-DATA-002 | Implemented: disabled-by-default `SeleniumReadOnlyAgent` enforces read-only and forbidden-action gates, rejects non-allowed live domains, extracts only local static HTML fixture content into an `EvidencePackage` with hash/path/title evidence, records `external_side_effects=false`, and performs no live browsing or network access. Historical local traceability label renamed to `HISYS-T-028-SEL` so controlled `HISYS-T-028` remains uniquely owned by investment domain migration governance. |
| Hisys MVP A5/A6 Chief Editor research review and DARS acceptance | docs/use-cases/hermes-hisys-domain-tool.md; HISYS-FR-INV-001..006; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-CON-010..012 | Implemented: `investigate-domain` writes a Chief Editor `research_recommendation_review` artifact that recommends with conditions, preserves source-validation status, and explicitly accepts safe DARS non-executable recommended actions as Chief Editor conditions via `dars_acceptance_decision=accepted_as_conditions` while rejecting advisory-boundary violations. |
| Live-A Source connector governance foundation | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: live research connector boundary docs, disabled-by-default `source-connectors.yaml`, governed source connector registry schemas/loader, dispatch decision gate with runtime-boundary evidence, and live source provenance records for URL/access-time/hash/license-signal and quote-vs-interpretation separation. No live adapter execution is enabled. |
| Live-B Fixture-backed source connector planning and evidence integration | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys plan-source-connectors` writes dry-run connector plan artifacts without external calls, `hisys.connectors.fixture_publisher` extracts local publisher-shaped fixture evidence/provenance, `investigate-domain` links fixture source evidence into investigation data, DARS trace lineage, and Chief Editor source-validation review conditions. No live adapter execution is enabled. |
| Live-C Manual DOI metadata smoke boundary | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `doi_metadata_search` has explicit manual-smoke policy fields, the read-only DOI metadata connector uses injectable transport for tests with fixture/local transport recorded as `external_call_made=false`, and `hisys smoke-source-connector` writes dry-run/blocked smoke reports unless an operator supplies approval plus `HISYS_ALLOW_LIVE_SMOKE=1`. CI performs no live network call. |
| Live-D Legal open-access PDF collector boundary | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `open_access_pdf_fetch` has explicit manual-smoke policy fields, `hisys.connectors.open_access_pdf` fixture-collects OA PDF bytes only after `license_signal=open_access`, and `hisys smoke-source-connector` dry-runs/blocks PDF smoke when license/approval/env gates are missing. CI performs no live PDF fetch. |
| Live-E DOI metadata to OA PDF candidate planning | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.pdf_candidate_planner` derives `candidate_plan_only` PDF candidates from DOI metadata OA hints, `hisys plan-pdf-candidates` writes dry-run candidate plan/report artifacts, and source connector planning records the `doi_metadata_search -> open_access_pdf_fetch` planned handoff. CI performs no live PDF fetch. |
| Live-F Approved manual OA PDF fetch smoke | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.open_access_pdf.collect_manual_smoke` supports injectable PDF transport, `hisys smoke-source-connector --transport-fixture-pdf` verifies the approved manual path without CI network while recording fixture transport as `external_call_made=false`, and completed smoke reports record `manual_pdf_smoke_completed`, `pdf_downloaded=true`, source-access refs, and source-evidence refs. Checked-in config remains disabled. |
| Live-G Manual OA PDF evidence promotion | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.pdf_evidence_promotion` validates explicit OA PDF source refs, `hisys investigate-domain --promote-pdf-source-access-ref --promote-pdf-source-evidence-ref` records `promoted_pdf_evidence_refs` in `InvestigationDataPackage`, and DARS/Chief Editor traces preserve promoted refs without implicit PDF discovery or live fetch. |
| Live-H PDF quote extraction from promoted OA evidence | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.pdf_quote_extractor` creates quote-only `source_quote_refs` from explicit promoted OA PDF evidence refs, `hisys extract-pdf-quotes` writes source-quote artifacts/reports without OCR, parsing, or network calls, and `hisys investigate-domain --source-quote-ref` preserves quote refs in investigation data, DARS trace, and Chief Editor review while keeping novelty claims conditional. |
| Live-I Quote-to-claim evidence ledger | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.claim_evidence_ledger` maps explicit source quotes to support/contradict/needs-evidence interpretation records, `hisys build-claim-evidence-ledger` writes claim ledger artifacts/reports without network calls or quote mutation, and `hisys investigate-domain --claim-evidence-ledger-ref` preserves `claim_evidence_ledger_refs` with Chief Editor `claim_evidence_ledger_present` status while keeping claims conditional. |
| Live-J Claim evidence summary | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.claim_evidence_summary` aggregates explicit claim ledger refs into advisory support/contradict/needs-evidence balance records, `hisys build-claim-evidence-summary` writes summary artifacts/reports without network calls or source mutation, and `hisys investigate-domain --claim-evidence-summary-ref` preserves `claim_evidence_summary_refs` with Chief Editor `claim_evidence_summary_present` status while keeping confidence advisory and conditional. |
| Live-K Claim coverage gate | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.claim_coverage_gate` checks required recommendation claims against explicit summary refs, `hisys build-claim-coverage-gate` writes gate artifacts/reports without network calls or source mutation, and `hisys investigate-domain --claim-coverage-gate-ref` preserves `claim_coverage_gate_refs` with Chief Editor `claim_coverage_gate_present` status and `conditional_manuscript_language_only` manuscript-language gate. |
| Live-L Recommendation claim registry | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.recommendation_claim_registry` records controlled required recommendation claims from explicit recommendation text, `hisys build-recommendation-claim-registry` writes registry artifacts/reports without network calls or source mutation, and `hisys investigate-domain --recommendation-claim-registry-ref` preserves `recommendation_claim_registry_refs` with Chief Editor `recommendation_claim_registry_present` status while requiring Live-K coverage before stronger manuscript-facing claims. |
| Live-M Approved live ideation run | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys live-ideation-run` gates approved/read-only DOI metadata source access with `--explicit-live-source-enable`, `HISYS_ALLOW_LIVE_IDEATION=1`, dispatch approval, and provenance records; the resulting source refs flow into `investigate-domain`, DARS trace, and Chief Editor review. Tests use injected metadata fixtures; real DOI metadata access remains approval-gated and non-mutating. |
| Live-N Approved live ideation persistence pipeline | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys live-ideation-persist` chains approved live DOI metadata ideation, DARS/Chief Editor review, approval-gated vault transaction apply, and approval-gated Obsidian Git sync. The command requires explicit source/write/Git enables, one approval ref, credential refs only, and clean/scoped vault Git confirmation; reports record vault refs, source refs, real-vault mutation state, network push state, and mutation state. |
| Live-O Standing autonomous approval envelope | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys live-ideation-persist --standing-approval-policy` accepts a prior approved scoped policy as an autonomous operating envelope. The policy must provide approval ref, source/write/Git capabilities, expiry and scope controls for domain, vault root, remote, branch, and credential ref; out-of-scope requests block before source access or mutation and completed reports record `standing_approval_applied`. |
| Live-P Autonomous queue runner | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys live-autonomy-run` consumes a JSON queue and executes each item through the standing-approved `live-ideation-persist` pipeline in isolated runtime sub-instances, writing a batch `live-autonomy-run-report.json` with completed/blocked counts, per-entry report refs, vault refs, external-call/mutation/network-push flags, and queue-entry blocking before any live action when required DOI/request refs are missing. |
| Live-Q Queue idempotency and retry ledger | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys live-autonomy-run` persists a queue retry ledger under `data/live-autonomy-ledgers/<date>/<queue_id>.json` or `--ledger`, skips completed entries on rerun before live action, records per-entry attempts/retry eligibility/reason/report refs, enforces `--max-retries`, and reports ledger refs plus skipped-completed, retry-exhausted, and retry-eligible counts. |
| Live-R Queue state transitions and watchdog reports | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: the live-autonomy ledger records `current_state` plus `state_history` transitions (`queued`, `running`, `completed`, `blocked`, `skipped_completed`, `skipped_retry_exhausted`) and each queue run writes `live-autonomy-watchdog-report.json|md` with scheduler readiness, health status, retry counts, ledger ref, and next scheduler action for cron/watchdog wrappers. |
| Live-S Scheduler tick wrapper | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented and hardened: `hisys live-autonomy-tick` discovers queue JSON files from `--queue-dir`, processes up to `--max-queues` through the standing-approved `live-autonomy-run` path, writes `live-autonomy-scheduler-tick-report.json|md`, treats empty queues as idle success, returns scheduler-friendly attention status with queue report/watchdog refs, namespaces per-queue run/watchdog reports under `run-summaries/<YYYYMMDD>/<queue_id>/`, terminalizes non-retryable malformed entries as `skipped_non_retryable` on later ticks, and validates standing approval `expires_on` as `YYYYMMDD` before live action. |
| Live-T Queue lifecycle handoff | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys live-autonomy-tick --queue-lifecycle` treats `--queue-dir` as incoming and moves queue files through deterministic `active`, `done`, `attention`, and `rejected` handoff directories, with active copies removed at finalization, invalid JSON queues rejected before live action, attention-needed queues retained for operator review, and lifecycle refs/states recorded in scheduler reports without prompt-based checks. |
| Live-U Queue admission validator | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented and prompt-reviewed: `hisys live-autonomy-admit` validates candidate queue JSON files before scheduler intake, admits valid queues to `--incoming-dir`, rejects malformed, schema-incomplete, or unsafe-path queues to `--rejected-dir`, writes deterministic admission reports with no external call, vault mutation, or network push, treats candidate rejection as report-level attention while returning exit `0` for routine admission ticks, and keeps scheduler overhead limited to local JSON/filesystem checks. |
| Live-V Compact operator status dashboard | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys live-autonomy-status` reads only existing admission reports, scheduler tick reports, watchdog reports, and queue ledgers, writes compact JSON/Markdown operator status summaries, reports missing source artifacts without failing, records no external call/vault mutation/network push, and avoids prompt/LLM review in routine dashboard generation. |
| Live-W Queue artifact hashing | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: live autonomy admission, scheduler tick, queue-run, watchdog, ledger, and status reports include deterministic SHA-256 queue and entry hashes computed from canonical sorted JSON for valid queues or raw bytes for malformed JSON, enabling cheap replay/unchanged-work identity without external calls, vault mutation, Git/network push, or prompt-based checks. |
| Live-X Duplicate/replay classification | docs/use-cases/live-research-connectors.md; docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-FR-MEM-001..005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented and prompt-reviewed: `hisys live-autonomy-admit` compares candidate queue and entry hashes against existing incoming/rejected handoff artifacts and records `new`, `same_hash_replay`, `changed_same_entry_id`, or `duplicate_queue_content` classifications as deterministic report-only metadata. Scope is intentionally limited to local admission handoff artifacts; processed queues remain covered by scheduler/ledger/status records, and malformed JSON matches only byte-identical malformed artifacts. No external call, vault mutation, Git/network push, or routine prompt check is added. |
| Live-Z Governed general web-search planning | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `source-connectors.yaml` declares disabled-by-default `general_web_search` with read-only/manual-smoke policy, no credentials, allowlisted search fixture domains, and forbidden live actions; `hisys plan-source-connectors` includes it in research-domain plans while routine execution remains blocked and records no external calls or mutations. |
| Search-A Fixture-local governed web-search evidence connector | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.connectors.general_web_search` converts injected JSON search-result fixtures into source access/evidence records with `external_call_made=false`, and `hisys smoke-source-connector --connector-id general_web_search --transport-fixture-search` validates the approved manual path. Missing fixture transport blocks with `search_fixture_transport_required`; live provider-backed search remains separately gated by explicit provider refs and approval. |
| Search-B Governed topic search command and Investigator harness | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys search-topic` gates `general_web_search`, requires approval/env/fixture transport in tested paths, writes search topic reports plus source access/evidence refs, and emits an orchestrator-authored Investigator harness preserving topic, user opinion, planned registry source id `SRC-GENERAL-WEB-SEARCH-001`, and evidence refs. Missing gates block before provider access; no login, form submit, upload, purchase, post, credential use, or mutation is allowed. `hisys source-connector-preflight` now evaluates the same read-only dispatch boundary without executing adapters, resolving provider endpoint values, or making live calls, and writes a preflight report with `live_execution_ready=false` plus high-impact confirmation required. |
| Search-C Approved provider-backed governed search | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys search-topic` supports provider-backed search via `--provider-url-ref env:...` and optional `--credential-ref env:...`, resolves endpoint/token values only at runtime, persists refs only, gates provider domain through the connector allowlist before access, supports deterministic provider-response fixtures with `external_call_made=false` for tests/staging, and keeps search read-only/no-mutation with login/form/upload/purchase/post/credential-value persistence blocked. |
| Investment Decision Packet schema | docs/public/investment-decision-packet.md; src/hisys/schemas/investment.py; tests/unit/test_investment_decision_packet_schema.py; HISYS-NFR-SEC-004; HISYS-FR-CE-002..006 | Implemented: `InvestmentDecisionPacket` records evidence-backed buy/hold/sell-style decision support with signals, bull/base/bear scenarios, risk/contradiction scores, Chief Editor/Devil/DARS status, human insight refs, approval gate, and optional non-executing order-ticket draft; execution/live action approval defaults false and requires approved human approval plus no-autonomous-execution/not-financial-advice disclaimers. `hisys build-investment-evidence-package` now promotes source connector SourceAccessRecord/SourceEvidenceItem artifacts into product `EvidencePackage` artifacts, and the E2E product test drives `search-topic -> build-investment-evidence-package -> run-investment-decision-dry-run` without fixture backend, external call, mutation, publication, or execution. `hisys completion-status` writes JSON/Markdown completion status with product components, gaps, validation statuses, and closed high-impact/no-live-action gates. |
| Investment structured-domain adapter migration | docs/public/investment-decision-packet.md; docs/use-cases/hermes-hisys-domain-tool.md; src/hisys/domain/specs.py; src/hisys/domain/use_cases.py; src/hisys/cli/main.py; tests/unit/test_investment_structured_domain_spec.py; HISYS-FR-DOM-006; HISYS-FR-DOM-003..004; HISYS-NFR-SEC-001..004; HISYS-T-028 (Investment domain migration governance) | Implemented: `investment_spec()` over `InvestmentAnalysisUseCase` migrates the investment domain into the shared structured-domain substrate while reusing the existing `InvestmentDecisionPacket`/`InvestmentWeightPolicy`/dry-run/operator-review CLI as the system of record. The structured adapter is advisory-only: recommendation summary embeds `not financial advice`, `no autonomous execution`, `execution_authorized=false`, and `publication_or_live_action_approved=false`; `requires_human_review=true`, `external_call_made=false`, and `mutation_performed=false` are preserved; packet/weight-policy refs supplied through `request.sources` flow through to bridged evidence/source refs without copying packet contents. The default registry order is `_ResearchGapDomainAdapter -> StructuredDomainAdapter(research_spec()) -> StructuredDomainAdapter(codebase_spec()) -> StructuredDomainAdapter(investment_spec())`. No live broker, order placement, publication, credential use, or external mutation is enabled. |
| Hisys Lapidary governance schema | docs/use-cases/obsidian-live-research-layout.md; src/hisys/schemas/lapidary_governance.py; tests/unit/test_lapidary_governance_schema.py; HISYS-SCHEMA-001; HISYS-FR-INV-001..006; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-CON-010..012 | Implemented: selective `hisys_mode.level` governance (`none` through `publication`), Stone/Gem/Jewel evidence-chain records (`decision/Jewel -> synthesis/Gem -> claim ledger -> evidence/Stone -> attachment/source`), Lapidary role assignments with technical `agent_role`/`function` retained, archive-not-delete temporal decay policy, internal/external evidence-origin weighting for alternatives, and DARS/Appraiser advisory separation from approval/execution authority. |
| Public browser beta profile, wrapper, readiness, quickstart, and Hermes tool deployment | docs/public/browser-quickstart.md; docs/public/public-beta-manual.md; docs/public/agent-tool-manual.md; docs/public/hermes-tool-deployment.md; examples/instance/config/profiles/public-browser.yaml; src/hisys/browser/public_profile.py; src/hisys/browser/public_summary.py; src/hisys/hermes_deploy.py; tests/unit/test_hermes_deploy.py; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys validate-public-browser-profile` validates the public beta browser safety envelope before live use, requiring `playwright_read_only`, `transport_kind=playwright_live`, read-only external-call posture, orchestrator-decided domain scoping, credentials/mutation disabled, and public fixture UX disabled. `hisys public-browser-readiness` writes a no-network readiness report for profile/config/import checks with `external_call_made=false`, `mutation_performed=false`, and no publication/live-action approval. `hisys public-browser-run` provides the public operator wrapper across browser acquisition, Chief Editor readiness review, advisory DARS/Devil review, revision resolution, final Chief Editor browser review, and public summary artifacts while preserving governed human-reviewed-use boundaries. `hisys get-run-summary`, `hisys list-run-artifacts`, and `hisys show-artifact` expose safe CLI-first artifact retrieval for agents; `show-artifact` is confined to safe relative JSON/Markdown refs under the runtime instance. `hisys deploy-hermes-tool` deploys a controlled wrapper, manifest, public browser profile copy, runtime directory, immutable `releases/<release_id>/source` snapshot, `releases/current` pointer, and Discord channel snippet under `~/.hermes/tools/hisys` without mutating Hermes config automatically; the wrapper resolves its executable root through tool-local `config/runtime.json`, fails closed unless `execution_mode=installed_snapshot` and `releases/current/source` are enforced, and executes the deployed snapshot rather than the live development checkout, preventing uncommitted edits or branch switches from changing the deployed tool unexpectedly. `.github/workflows/deploy-hermes-tool.yml` runs after successful `Hisys CI` and uses `scripts/verify_hermes_tool_deploy.py` plus wrapper smoke tests to validate snapshot deployment in CI before operator installation. `hisys deployment-status`, `hisys build-hermes-deploy-report`, and `hisys rollback-hermes-tool` expose safe operator inspection, CI artifact reporting, and atomic local rollback of `releases/current` with rollback provenance. Camoufox remains rejected unless explicitly experimental, and public docs use installed CLI plus Playwright Chromium while preserving fixture mode for CI/developer reproduction only. |
| Governed evidence store and Stone promotion | docs/public/evidence-store.md; src/hisys/evidence_store.py; tests/unit/test_evidence_store.py; HISYS-FR-INV-001..006; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys evidence-store-init`, `evidence-store-status`, `evidence-store-import-investigation`, `evidence-stone-candidates`, and `evidence-promote-stone` manage search/evidence outputs in a dedicated store outside the personal `/home/cbchoi/me` vault. Writes require approval refs, personal-vault projection is disabled by default, and Stone promotion records provenance back to evidence-store refs. |
| Hisys host environment config and vault target registry | docs/public/config-structure.md; src/hisys/environment_config.py; tests/unit/test_environment_config.py; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys environment-init` and `hisys environment-status` record host-local paths for the deployed Hisys tool root, source repo, evidence store, store config, personal `/home/cbchoi/me` vault, and lab `/home/cbchoi/obsidian` vault while keeping raw evidence writes forbidden and curated vault projection approval-gated. Status checks block configs that point the evidence store at a vault, source repo, or tool root, and perform no external calls or vault writes. |
| Browser-H Final Chief Editor browser acceptance review | docs/use-cases/live-research-connectors.md; src/hisys/schemas/browser_acceptance.py; HISYS-FR-INV-001..006; HISYS-FR-AGT-001..005; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys final-review-browser-investigation` consumes a Browser-G revision-resolution artifact, requires `ready_for_final_acceptance_review` with complete segment normalization/corroboration mapping and no blockers, writes `data/chief-editor-final-browser-reviews/<date>/FINAL-CHIEF-REVIEW-*-BROWSER.json`, and records `decision=accept_for_human_reviewed_use` only for governed human-reviewed use. Formal schemas now validate final acceptance artifacts and reports, preserving `publication_or_live_action_approved=false`, `human_approval_required_for_consequential_use=true`, `action_taken=none`, and no external calls/mutations. |
| Browser-I Full browser acceptance chain | docs/use-cases/live-research-connectors.md; src/hisys/schemas/browser_acceptance.py; HISYS-FR-INV-001..006; HISYS-FR-AGT-001..005; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys review-browser-investigation` closes the missing Chief Editor readiness step between browser acquisition and DARS/Devil review, and the end-to-end test drives completed browser report -> Chief Editor review -> advisory DARS review -> deterministic DARS revision resolution -> final Chief Editor acceptance while validating the formal Browser-G/Browser-H schemas at write time. |
| Browser-G DARS revision resolution gate | docs/use-cases/live-research-connectors.md; src/hisys/schemas/browser_acceptance.py; HISYS-FR-INV-001..006; HISYS-FR-AGT-001..005; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys resolve-browser-dars-revisions` consumes an advisory browser DARS review plus its Chief Editor/competitive-matrix refs, writes `data/browser-dars-revision-resolutions/<date>/REVISION-*-BROWSER.json`, and deterministically verifies segment normalization plus high-strength independent corroboration before setting `ready_for_final_acceptance_review`; formal schemas now enforce complete gates/no blockers for readiness, advisory-only safety flags, and no external calls/mutations. |
| Browser-F DARS/Devil adversarial browser review | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-FR-AGT-001..005; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys request-browser-dars-review` consumes a Chief Editor browser-investigation review that was accepted for Devil/DARS review, verifies the sufficiency gate permits Devil review, writes an advisory-only `AgentHandoffPackage`, and persists `data/dars-browser-reviews/<date>/DARS-REVIEW-*-BROWSER.json` with deterministic adversarial findings, required revisions, no external DARS calls, and no mutation before final acceptance. |
| Browser-E Source-candidate population and review sufficiency gate | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: browser investigations now write `data/source-candidates/<date>/SRC-CANDIDATES-*-BROWSER.json` with URL, source type, usefulness score, evidence role, and explicit `evidence_quality_source_discovery_not_cybersecurity` classification; `--orchestrator-corroborating-url` records concrete orchestrator-provided corroborating candidates without fetching them until separately promoted/approved; investigations also write `data/evidence-sufficiency/<date>/SUFF-*-BROWSER.json` that blocks Chief Editor / Devil review until source breadth and independent corroboration are sufficient for fair review. |
| Browser-D Bounded detail-link following and competitive matrix | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys browser-investigate-topic --follow-links --max-follow-links-per-source N` selects same-domain product/technology links from browser-visible anchors, rechecks each followed URL through the source connector dispatch gate, persists followed source refs in the run report, and writes `data/competitive-matrices/<date>/MATRIX-*-BROWSER.json` plus a memo matrix section with heuristic segment/technology/strength signals while preserving read-only/no-mutation browser boundaries. |
| Browser-C Orchestrator-decided browser domains | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `domain_decision_policy: orchestrator_decided` plus `hisys browser-investigate-topic --orchestrator-decide-domains` derives a per-run scoped allowlist from orchestrator-selected source URLs, writes an `orchestrator-domain-decision` runtime-boundary record with approval ref, selected URLs, decided domains, requested read-only action, and preserved forbidden actions, then reuses the normal dispatch gate. This removes hand-maintained domain lists for browser investigations without allowing unrestricted browsing or mutation. |
| Browser-B Actual browser investigation command | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-T-027; HISYS-T-028; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys browser-investigate-topic` accepts approved company/publisher URLs, gates each through `playwright_read_only`, collects page title/visible text with Playwright runtime when `--browser-fixture-html` is omitted or with paired local HTML fixtures for deterministic parity, writes source access/evidence refs, transforms browser page evidence into an Investigator `EvidencePackage`, and renders a memo-ready browser investigation artifact with evidence table and claims while preserving read-only, env/approval/domain gates, citations, hashes, and no mutation. Optional dependency group `browser` provides Playwright runtime support. |
| Browser-A Governed Playwright page collector | docs/use-cases/live-research-connectors.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `playwright_read_only` and `hisys.connectors.playwright_browser` collect company/publisher page title and visible text through local HTML fixture transport in tests (`external_call_made=false`) or Playwright Chromium in approved manual runs (`external_call_made=true`), emit source access/evidence records with hashes and citations, and keep browser acquisition disabled-by-default, read-only, domain-allowlisted, approval/env-gated, and blocked from login/form/upload/purchase/post/mutation/access-control bypass. |
| Live-Obsidian-Config-A scaffold | docs/use-cases/obsidian-live-research-layout.md; Claude Code architecture review; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Scaffolded: Claude-reviewed Obsidian live-research layout and examples define `registry.json`, `topics/INDEX.json`, canonical topics under `topics/`, group overlays, `topic-manifest.json`, `investigation-manifest.json`, `runtime-index.json`, `attachment-index.json`, content-addressed attachment blobs, structured type/phase metadata, evidence-citing Topic Gatekeeper decisions, and future `vault-plan --dry-run`/`vault-validate` tasks while making no real vault writes. |
| Live-Obsidian-Config-B vault planner | docs/use-cases/obsidian-live-research-layout.md; examples/obsidian-live/registry.json; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_vault_plan` and `hisys vault-plan --dry-run` compute registry-first canonical topic/investigation paths, write only runtime-boundary planner and run-summary reports, preserve evidence-citing gatekeeper score refs, reject unsafe titles/path traversal, and record `vault_write_attempted=false`, `external_call_made=false`, and `mutation_performed=false` without writing the real Obsidian vault. |
| Live-Obsidian-Config-C vault validator | docs/use-cases/obsidian-live-research-layout.md; examples/obsidian-live/*.json; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.validate_vault_manifests` and `hisys vault-validate` validate fixture registry/topic/investigation/gatekeeper manifests, reject missing gatekeeper evidence refs, unsafe vault-relative refs, invalid topic IDs/slugs, and merge/split decisions missing approval refs, and write validation reports without real vault writes or external calls. |
| Live-Obsidian-Config-D memo ontology template planner | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_vault_template_plan` and `hisys vault-template-plan` plan controlled memo ontology templates, relation vocabulary, and required indexes using structured `type`/`phase`/`links` metadata while treating wikilinks as projections and writing only runtime-boundary/report artifacts. |
| Live-Obsidian-Config-E validator hardening | docs/use-cases/obsidian-live-research-layout.md; examples/obsidian-live/*.json; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.validate_vault_manifests` shares relation vocabulary with `build_vault_template_plan` and rejects unknown structured link relations, invalid group IDs, invalid investigation/run IDs, and overlong vault-relative refs while continuing to write validation reports only and never touching the real Obsidian vault. |
| Live-Obsidian-Config-F fixture vault apply | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.apply_vault_plan_to_fixture` and `hisys vault-apply` materialize approved vault-plan projections into an explicit fixture vault root only. The command requires `--approval-ref` and `--fixture-vault-only`, blocks `/home/cbchoi/obsidian`, writes runtime-boundary apply reports, and records `real_obsidian_vault_write_performed=false` and `external_call_made=false`. |
| Live-Obsidian-Config-G topic transition plan | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_topic_identity_transition_plan` and `hisys vault-topic-transition-plan` plan approval-gated, non-destructive topic merge/split transitions with tombstone refs, manifest-update previews, no topic-folder deletion, no real vault writes, and runtime-boundary transition artifacts. |
| Live-Obsidian-Config-H fixture vault roundtrip validation | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.validate_fixture_vault_roundtrip` and `hisys vault-roundtrip-validate` validate plan-to-fixture-apply coherence, missing/unexpected files, projection metadata, and apply-report consistency without real vault writes or external calls. |
| Live-Obsidian-Config-I live vault preflight | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_live_vault_preflight_report` and `hisys vault-live-preflight` inspect vault root, `.obsidian`, Git marker, and attachment ignore policy without writing. The report records `write_probe_performed=false`, `live_write_enabled=false`, and no real vault mutation. |
| Live-Obsidian-Config-J live vault approval package | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_live_vault_approval_package` and `hisys vault-live-approval-package` create a human approval package listing planned vault-relative writes, required approvals, rollback strategy, and final gates while keeping `live_write_enabled=false` and making no vault mutation. |
| Live-Obsidian-Config-K live vault write gate | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_live_vault_write_gate_report` and `hisys vault-live-write-gate` evaluate final live-write preconditions but intentionally stay blocked with `implementation_boundary=gate_only_no_writer`, `live_write_enabled=false`, and no real vault mutation. |
| Live-Obsidian-Config-L live vault transaction plan | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_live_vault_transaction_plan` and `hisys vault-live-transaction-plan` produce a non-executable transaction manifest with planned operations, rollback hints, and placeholder hashes while never reading or writing the live vault. |
| Live-Obsidian-Config-M fixture transaction rehearsal | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.rehearse_live_vault_transaction_in_fixture` and `hisys vault-live-transaction-rehearse` exercise live transaction manifests against an explicit fixture vault only, refuse the real vault, and record no real mutation. |
| Live-Obsidian-Config-N approved transaction apply | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.apply_live_vault_transaction` and `hisys vault-live-transaction-apply` provide an approval-gated writer boundary requiring approval, explicit enablement, and clean-git confirmation; the real Obsidian vault remains additionally guarded by `--allow-real-obsidian-vault`. |
| Live-Obsidian-Config-O completion status | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_live_obsidian_config_status_report` and `hisys vault-live-config-status` record completion of A through O with zero open stages and no real Obsidian vault write performed. |
| Topic-Gatekeeper complete | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_topic_gatekeeper_decision` and `hisys vault-topic-gatekeeper` create read-only evidence-citing routing decisions; helper APIs cover approval package, transaction planning, fixture rehearsal, and completion status without real vault writes. |
| Obsidian Evidence Promotion-A | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_obsidian_evidence_promotion_plan` and `hisys vault-evidence-promotion-plan` plan explicit source/evidence/claim/decision refs into canonical topic indexes; fixture rehearsal remains projection-only and no real vault writes are performed. |
| Obsidian Milestone Status | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-INV-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_obsidian_milestone_status_report` and `hisys vault-obsidian-milestone-status` record Live-Obsidian-Config, Topic-Gatekeeper, Obsidian Evidence Promotion, and Obsidian Git Management complete with zero open milestones and no real vault writes. |
| Obsidian Git Management-A | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-MEM-001..005; HISYS-FR-INV-001..006; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.build_obsidian_git_initialization_plan` and `build_obsidian_git_sync_plan` capture the design correction that Hisys initializes the Obsidian vault as a Git-managed repo, stores only credential refs, requires explicit approval refs for push-bearing plans, rejects raw tokens, and plans operation-time commit/push for at least one approved memo or runtime-boundary ref after approved vault writes while remaining plan-only with no mutation or external calls. |
| Obsidian Git Management-B | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-MEM-001..005; HISYS-FR-INV-001..006; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.execute_obsidian_git_initialization_in_fixture` and `execute_obsidian_git_sync_in_fixture` execute approved Git plans only against local fixture vaults/remotes, require `fixture_git_only`, refuse the real Obsidian vault, never resolve credential refs, and record fixture push evidence with no live external calls. |
| Obsidian Git Management-C | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-MEM-001..005; HISYS-FR-INV-001..006; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys vault-git-fixture-init`, `hisys vault-git-fixture-sync`, and the updated `hisys vault-obsidian-milestone-status` expose fixture Git execution through CLI/runtime-boundary reports and mark Obsidian Git Management complete without live vault writes or external calls. |
| Obsidian Git Management-D | docs/use-cases/obsidian-live-research-layout.md; HISYS-FR-MEM-001..005; HISYS-FR-INV-001..006; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.config.obsidian_live.execute_obsidian_git_sync_live` and `hisys vault-git-live-sync` execute an approved live Git sync plan after explicit live enablement, approval-ref match, clean/scoped Git status confirmation, and real-vault opt-in; the executor stages only approved memo/runtime-boundary refs, commits, pushes, and records runtime-boundary evidence including network push state. |
| Lapidary governance hardening | src/hisys/schemas/lapidary_governance.py; tests/unit/test_lapidary_governance_schema.py; HISYS-SCHEMA-001; HISYS-FR-INV-001..006; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-CON-010..012 | Implemented: `EvidenceChainRecord` validators reject blank refs, allow optional `decision_ref` for non-decision Stone/Gem chains while requiring `synthesis_refs`+`claim_ledger_refs` only when a decision is present, and require non-empty `evidence_refs`+`source_refs` on every chain; `TemporalArchivePolicy` keeps `delete_allowed=false` and preserves historical evidence; `WeightedDecisionAlternative` requires positive total `origin_weight` and computes a true weighted score using per-origin weights. |
| Hisys mode bound to investment decisions | src/hisys/schemas/investment.py; tests/unit/test_investment_decision_packet_schema.py; HISYS-SCHEMA-001; HISYS-FR-CE-002..006; HISYS-NFR-SEC-004; HISYS-DARS-CONTRACT-001 | Implemented: `InvestmentDecisionPacket` carries `hisys_mode` and an optional `EvidenceChainRecord`; `decision`/`publication` modes require a decision-level chain (`decision_ref` plus `synthesis_refs`, `claim_ledger_refs`, `evidence_refs`, `source_refs`) while preserving the not-financial-advice and no-autonomous-execution disclaimers and approval gating. |
| Hisys mode bound to investigation packages | src/hisys/schemas/domain_investigation.py; tests/unit/test_domain_investigation.py; HISYS-SCHEMA-001; HISYS-FR-INV-001..006; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-CON-010..012 | Implemented: `InvestigationDataPackage` carries `hisys_mode` and an optional `EvidenceChainRecord`; `claim`, `synthesis`, `decision`, and `publication` modes enforce progressively stricter chain refs (claim ledger -> synthesis -> decision) so selective Lapidary governance gates downstream advisory/Chief Editor use without being required at `none`/`stone` levels. |
| DARS appraiser separation enforced at dispatch | src/hisys/agents/dars_dispatch.py; tests/unit/test_dars_appraiser_separation.py; HISYS-DARS-CONTRACT-001; HISYS-FR-AGT-001..005; HISYS-T-019; HISYS-T-020; HISYS-T-024; HISYS-CON-010..012 | Implemented: `DarsDispatchGate` records the requesting `intent` and `policy_ref` on every `DarsDispatchDecision`, classifies the intent against the advisory-only appraiser policy before any backend resolution, and fail-closed blocks any non-advisory authority intent (`approve_decision`, `execute_action`, `publish_output`, or unknown) with `reason_code=appraiser_separation_policy_violation` while keeping `allowed_actions=advisory_only` and `action_taken=none`. |
| Chief Editor evidence-chain emission | src/hisys/chief_editor/runtime.py; tests/unit/test_chief_editor_runtime.py; HISYS-FR-CE-001..006; HISYS-CE-POLICY-001; HISYS-SCHEMA-001; HISYS-FR-INV-001..006; HISYS-T-014..018; HISYS-T-024 | Implemented: `ChiefEditorRuntime` accepts a `HisysMode` and, at `decision`/`publication` levels, builds an `EvidenceChainRecord` for each escalated `AlertDecisionRecord` (decision_ref -> memo perspective synthesis -> memo claim ledger -> signal evidence -> source refs), persists it next to the decision JSON, and surfaces `evidence_chain_refs` on the `AlertDecisionRunReport` while leaving lower modes unchanged. |
| Lapidary governance audit persistence | src/hisys/audit/governance.py; src/hisys/audit/__init__.py; tests/unit/test_lapidary_governance_audit.py; HISYS-FR-ADM-002; HISYS-SCHEMA-001; HISYS-FR-INV-001..006; HISYS-DARS-CONTRACT-001; HISYS-D-015; HISYS-T-024 | Implemented: `LapidaryGovernanceAuditWriter` appends `EvidenceChainRecord`, `WeightedDecisionAlternative`, and `AppraiserSeparationPolicy` records under `data/audit/<YYYYMMDD>/lapidary-governance/{evidence-chains,weighted-alternatives,appraiser-policies}/<id>.json` as canonical sorted JSON that round-trips through the originating schema; Chief Editor evidence-chain emission writes through this audit boundary so governance trace traversal stays on the existing audit path. |
| Shared DARS appraiser separation guard | src/hisys/agents/appraiser_separation.py; tests/unit/test_dars_appraiser_guard.py; HISYS-DARS-CONTRACT-001; HISYS-FR-AGT-001..005; HISYS-SCHEMA-001; HISYS-CON-010..012 | Implemented: `hisys.agents.appraiser_separation` centralises advisory/authority intent classification (`ADVISORY_INTENTS`, `AUTHORITY_INTENTS`, `AppraiserSeparationVerdict`, `AppraiserSeparationViolation`, `classify_intent`, `enforce_advisory_intent`); `dars_dispatch`, `DarsFixtureBackend`, and `DarsMockEndpointAdapter` consult the shared module so an alternate code path cannot route a non-advisory authority intent past the appraiser-separation policy without writing a `rejected` validation report and raising. |
| Investment Decision Packet product CLI workflow | docs/public/investment-decision-packet.md; src/hisys/cli/main.py; tests/unit/test_investment_decision_packet_cli.py; HISYS-SCHEMA-001; HISYS-FR-CE-002..006; HISYS-NFR-SEC-004; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-CON-010..012 | Implemented: `hisys build-investment-decision-packet` validates an `InvestmentDecisionPacket` JSON input, writes runtime-boundary JSON/Markdown packet artifacts plus `investment-decision-packet-report.json`, optionally persists an `InvestmentWeightPolicy` artifact supplied by `--weight-policy`, persists the packet's decision-level evidence chain and weighted alternatives through the Lapidary audit writer, and records `action_taken=none`, `external_call_made=false`, `mutation_performed=false`, `execution_authorized=false`, and `publication_or_live_action_approved=false` unless a validated packet explicitly represents approved human-gated scope; `hisys review-investment-decision-packet` reads the persisted packet/report pair and prints a bounded operator summary without mutation or external calls; `hisys run-investment-decision-dry-run` consumes already-materialized `EvidencePackage` artifacts and a weight policy to assemble/persist the packet path without a fixture backend, recording `fixture_backend_used=false` and rejecting fixture/mock provenance in product dry-run evidence packages. |
| Operational Lapidary governance flow/routing | src/hisys/operations/lapidary_flow.py; tests/integration/test_operational_governance_flow.py; HISYS-SCHEMA-001; HISYS-FR-INV-001..006; HISYS-FR-CE-004; HISYS-DARS-CONTRACT-001; HISYS-T-024; HISYS-CON-010..012 | Implemented: deterministic `HisysMode` routing selection from evidence/claim/synthesis refs, decision/publication requests, and risk score; weighted alternative generation from decision-level evidence chains plus audit persistence; application of DARS advisory responses to investment decision packets without transferring approval/execution/publication authority; and fixture-backed full-flow coverage from memo investigation evidence through Chief Editor evidence-chain emission, audit persistence, weighted alternative persistence, DARS advisory linkage, and final human-gated decision packet. |
| Pass-contract self-improvement workflow | docs/contracts/pass-contract-self-improvement.md; docs/plans/2026-05-13-pass-contract-self-improvement-roadmap.md; src/hisys/contracts/; src/hisys/cli/main.py; tests/unit/test_pass_contract_*.py; tests/unit/test_needs_more_evidence_*.py; tests/unit/test_domain_investigation_pass_contracts.py; tests/integration/test_pass_contract_self_improvement_flow.py; HISYS-FR-INV-001..006; HISYS-DARS-CONTRACT-001; HISYS-T-024 | Implemented: governed pass-contract self-improvement now covers proposal creation, inactive candidate conversion, local evidence evaluation, advisory review packages, human-approved promotion to active registry artifacts, explicit active-registry consumption by `investigate-domain`, and needs-more-evidence audit summaries. The workflow preserves `automatic_promotion_allowed=false`, `external_call_made=false`, `mutation_performed=false`, and `publication_or_live_action_approved=false` on routine proposal/evaluation/review/audit artifacts; active promotion requires a human approval ref plus local review and validation refs, so Hisys can self-diagnose and widen coverage without self-authorizing lower standards. |
| Spec-first agent workflow packets | docs/public/agent-workflow-packets.md; src/hisys/operations/agent_workflow.py; src/hisys/cli/main.py; tests/unit/test_agent_workflow_packets.py; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys build-spec-first-packet` and `hisys build-finish-packet` persist local runtime-boundary JSON/Markdown packets for spec-first bounded agent work and closure review. Packets record scope, non-goals, allowed actions, evidence contracts, gate criteria, validation results, review findings, unresolved blockers, and human-gate state while preserving `external_call_made=false`, `mutation_performed=false`, `publication_or_live_action_approved=false`, and `action_taken=none`; they do not install Superpowers, authorize live actions, weaken `needs_more_evidence`, or let subagents own final Hisys decisions. |
| Local DARS / ByeSys evidence-sufficiency provenance | docs/plans/2026-05-16-local-dars-byesys-provenance.md; src/hisys/provenance/source_weighting.py; src/hisys/agents/dars.py; tests/unit/test_source_weighting.py; tests/unit/test_dars_runtime.py; HISYS-FR-AGT-001..005; HISYS-DARS-CONTRACT-001; HISYS-T-019; HISYS-T-020; HISYS-T-024; HISYS-CON-010..012 | Implemented: `hisys.provenance.source_weighting` exposes `is_byesys_source`, `EvidenceSufficiencyVerdict`, and `claim_has_sufficient_non_byesys_evidence` so reviewer call sites can gate Jeweler/Appraiser acceptance against a minimum non-ByeSys evidence weight without prose parsing. `DarsCritiqueSourceWeight` on `DarsCritiqueRecord.source_weights` is a Pydantic model that normalizes any ByeSys-classified entry to `weight=0.0` and `kind=byesys`, preserving the ByeSys-zero invariant at the DARS schema boundary. Reviewer-terminology aliases (Chief Editor -> Jeweler, DARS Devil/DARS reviewer -> Appraiser) are normalized centrally. The helper remains a reusable primitive: it does not silently rewire existing prose-based reviewer paths and does not authorize live or external behavior. |
| Local DARS openai-compatible loopback adapter and fake-server smoke | docs/plans/2026-05-16-local-dars-byesys-provenance.md; docs/operations/local-dars-smoke.md; src/hisys/agents/dars.py; src/hisys/agents/dars_config.py; src/hisys/agents/dars_dispatch.py; src/hisys/domain/use_cases.py; tests/unit/test_dars_runtime.py; tests/unit/test_dars_config.py; tests/unit/test_dars_dispatch.py; tests/unit/test_domain_runtime_artifacts.py; tests/unit/helpers/fake_openai_server.py; HISYS-FR-AGT-001..005; HISYS-DARS-CONTRACT-001; HISYS-D-015; HISYS-T-019; HISYS-T-020; HISYS-T-023; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: the `openai_compatible` DARS backend kind is accepted by `hisys.agents.dars_config` only when its endpoint resolves to a loopback host with an `http`/`https` scheme and `mode=local_network_only`; `hisys.agents.dars_dispatch` requires an explicit `approval_ref` even for localhost dispatch and treats a local LLM call as a model-boundary event (`local_model_call_made=true`, `model_boundary_crossed=true`, `external_call_made=false`). The adapter persists a `dars-local-llm-boundary-<request_id>.json` runtime-boundary artifact with `schema_id=hisys.dars.local_llm_boundary` capturing endpoint, model, request hash, timing, and response excerpt without leaking secrets. `hisys.domain.use_cases` writes a paired `dars-decision-<request_id>.json` advisory placeholder (`status=pending_human_review`) so structured-domain runtime refs always resolve to real files. `tests/unit/helpers/fake_openai_server.py` provides a loopback-only HTTP responder used by the smoke cohort; `docs/operations/local-dars-smoke.md` records the controlled fake-server smoke procedure with the expected boundary output (`dars_backend=local_llm_dars`, `external_call_made=false`, `model_boundary_crossed=true`, `local_model_call_made=true`, `endpoint_scope=localhost_only`, `mutation_performed=false`). No live local-runner install, no model download, no non-localhost endpoint, and no replacement of the working Claude DARS runtime config is enabled by this surface. |
| Codebase analysis inventory foundation (M14.1 + M15.1..M15.4) | docs/public/codebase-analysis.md; revision_plan_v004.md; ralph.md M14.1/M15; src/hisys/operations/codebase_analysis.py; src/hisys/cli/main.py; tests/unit/test_codebase_analysis_inventory.py; HISYS-FR-DOM-005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: the codebase-analysis surface is governed by `SPEC-HISYS-CODEBASE-ANALYSIS-001` (built via `hisys build-spec-first-packet`). `hisys.operations.codebase_analysis.build_codebase_inventory` performs a deterministic name-sorted walk of a caller-supplied repository root, prunes a fixed set of transient/generated directories (`.git`, `.hg`, `.svn`, `.venv`, `venv`, `env`, `__pycache__`, `.pytest_cache`, `.mypy_cache`, `.ruff_cache`, `.tox`, `.cache`, `.eggs`, `build`, `dist`, `htmlcov`, `node_modules`), records a `PathPolicy` (no symlink following; outside-repo symlinks rejected with `outside_repo_symlink` skip events; 1 MiB large-file threshold; 8 KiB null-byte probe; generated-file suffix and marker heuristics), and classifies files into binary/large/generated counts with `raw_source_content_persisted=false`. `write_codebase_inventory` persists JSON and Markdown artifacts under `runtime-boundary/codebase-analysis/<YYYYMMDD>/<REQUEST_ID>/inventory.{json,md}` after slug-validating `date`/`request_id` and rejecting traversal segments. `hisys build-codebase-inventory` wraps the builder/writer for CLI use with an optional `--scope` filter. No external repository clone, raw source archival, model call, live network access, credential use, remote push, or publication is enabled by this surface. |
| Codebase analysis risk-boundary scanner (M18.1..M18.4) | docs/public/codebase-analysis.md; revision_plan_v004.md; ralph.md M18; src/hisys/operations/codebase_analysis.py; src/hisys/cli/main.py; tests/unit/test_codebase_risk_boundary_scan.py; HISYS-FR-DOM-005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.operations.codebase_analysis.scan_codebase_risk_boundaries` is a conservative AST-only scanner that surfaces boundary-crossing call sites as review evidence (not vulnerability verdicts). The scanner detects `network_external_call` for `requests.<verb>`, `httpx.<verb>`, and `urllib3.<verb>`; `browser_external_call` for `webbrowser.{open,open_new,open_new_tab}`; `filesystem_mutation` for `<receiver>.write_text` / `.write_bytes`; `runtime_boundary_artifact_write` when the same write methods appear in a module whose string literals contain the controlled `runtime-boundary` token; `subprocess_execution` for `subprocess.{run,Popen,call,check_call,check_output,getoutput}` and `os.{system,spawnX}`; `model_llm_boundary` for `openai.*`/`anthropic.*` attribute-chain calls and for `requests.<verb>`/`httpx.<verb>` in a module whose literals contain model endpoint tokens (`/v1/chat/completions`, `/v1/completions`, `/v1/messages`, `/v1/embeddings`); and `byesys_generated_evidence` for string literals containing the controlled `ByeSys` or `byesys_generated` markers. Each `RiskBoundaryFinding` records `action_authorized=false` and the scan envelope records `raw_source_content_persisted=false`, `action_authorized=false`, `external_call_made=false`, `mutation_performed=false`, and `publication_or_live_action_approved=false`. `write_codebase_risk_scan` persists `risk-scan.{json,md}` under `runtime-boundary/codebase-analysis/<YYYYMMDD>/<REQUEST_ID>/` after the same slug validation that protects the other writers, and the Markdown preamble explicitly states the artifact is review evidence rather than a vulnerability verdict. `hisys scan-codebase-boundaries` wraps the scanner/writer for CLI use with an optional `--scope` filter. No external repository clone, raw source archival, model call, live network access, credential use, remote push, or publication is enabled by this surface. |
| Codebase analysis scope map and validation plan (M17.1..M17.4) | docs/public/codebase-analysis.md; revision_plan_v004.md; ralph.md M17; src/hisys/operations/codebase_analysis.py; src/hisys/cli/main.py; tests/unit/test_codebase_scope_map.py; HISYS-FR-DOM-005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.operations.codebase_analysis.list_codebase_scope_profiles` exposes a static `CodebaseScopeProfile` registry (`docs-traceability`, `domain-adapter`, `runtime-boundary`) that names each scope's entry files, focused tests, and controlled docs without reading source content. `build_codebase_scope_map` consumes already-loaded `CodebaseInventory` and `PythonSymbolIndex` records (no source content read) and partitions each profile's declared refs into present-vs-missing lists, filters the symbol-index modules and parse errors per scope, and isolates traceability refs under the `docs/traceability/` subtree. `build_codebase_validation_plan` derives a deterministic `CodebaseValidationPlan` whose per-scope `commands[]` cover `git_diff_check`, `traceability`, `focused_tests`, `secret_scan`, and `full_tests`; `requires_full_suite=true` escalates when missing entry files or expected tests indicate drift, or when the scope crosses subsystems (currently `runtime-boundary`). `write_codebase_scope_map` persists `scope-map.{json,md}` under `runtime-boundary/codebase-analysis/<YYYYMMDD>/<REQUEST_ID>/` alongside the matching inventory and symbol-index artifacts. `resolve_instance_runtime_ref` rejects empty refs, absolute paths, `..` traversal segments, and symlinks whose real target escapes the instance root, and is the chokepoint the `hisys build-codebase-map` CLI uses before reading caller-supplied `--inventory-ref` and `--symbol-index-ref` artifacts. The writer/CLI envelope records `raw_source_content_persisted=false`, `external_call_made=false`, `mutation_performed=false`, and `publication_or_live_action_approved=false`. No external repository clone, raw source archival, model call, live network access, credential use, remote push, or publication is enabled by this surface. |
| Codebase analysis python symbol index (M16.1..M16.4) | docs/public/codebase-analysis.md; revision_plan_v004.md; ralph.md M16; src/hisys/operations/codebase_analysis.py; src/hisys/cli/main.py; tests/unit/test_codebase_symbol_index.py; HISYS-FR-DOM-005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.operations.codebase_analysis.build_python_symbol_index` reuses the inventory walk to extract a deterministic Python AST symbol index with `schema_id=hisys.codebase.symbol_index` and `raw_source_content_persisted=false`. Per Python file the index records the module qualname, sorted top-level imports (`module`, `name`, `asname`, `line`), top-level functions and class methods (`name`, `line_start`, `line_end`, `is_async`, `parameters`, heuristic `tags` — `cli_handler` for `_cmd_*`, `parser_builder` for `argparse.ArgumentParser` builders, `pytest_test` for `test_*` names including methods of `TestXxx` classes), and classes with recursive `nested_classes`. Parse errors are captured as `parse_errors[]` evidence with path/line/column/message so a single broken module does not halt the build, and aggregate counters report `module_count`, `import_count`, `class_count`, `function_count`, and `parse_error_count`. `write_python_symbol_index` persists JSON and Markdown artifacts under `runtime-boundary/codebase-analysis/<YYYYMMDD>/<REQUEST_ID>/symbol-index.{json,md}` after the same slug validation that protects the inventory writer, with the result envelope recording `external_call_made=false`, `mutation_performed=false`, and `publication_or_live_action_approved=false`. `hisys build-code-symbol-index` wraps the builder/writer for CLI use with an optional `--scope` filter. No external repository clone, raw source archival, model call, live network access, credential use, remote push, or publication is enabled by this surface. |
| DARS critic panel runtime foundation (MB-DARS-CP-M1 / MB-DARS-CP-T001) | docs/milestone-bootstrap/reports/milestone_plan_v0.0.1.md; docs/milestone-bootstrap/tasks/milestone_tasks_v0.0.1.yaml; docs/requirements/dars-critic-panel-runtime-requirements.md; docs/design/dars-critic-panel-runtime-sdd.md; docs/test/dars-critic-panel-runtime-std.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_runtime.py; HISYS-FR-DARS-CP-001..008; HISYS-NFR-DARS-CP-001..002; HISYS-T-DARS-CP-001..009; HISYS-FR-AGT-001..005; HISYS-NFR-SEC-001..004; HISYS-NFR-REL-001 | Implemented: `hisys.agents.dars_panel.DarsCriticPanelRuntime` is a fixture-local advisory-only orchestrator that turns a `DarsCriticPanelConfig` plus candidate/evidence/rubric refs into a `DarsRoundPlan` (one independent `DarsCriticTask` per critic role plus a single synthesis task; `DarsRoundEdge` records each critic-to-synthesis dependency), validates the panel config (rejecting duplicate `critic_id` values and any `output_contract` other than `DarsCritiqueRecord`), executes critic tasks in a serial loop whose `execution_mode` is reported as `serial` or `bounded_parallel` from `max_parallel_critics`, and writes per-critic `DarsCritiqueRecord` JSON artifacts, a `DarsCritiqueSynthesis` artifact with critic role provenance, and a `DarsRoundTrace` artifact linking candidate-to-critique-to-synthesis lineage under `<instance>/data/dars-panel/<YYYYMMDD>/<REQUEST_ID>/`. Dispatch is gated: critic backends starting with `external-` or with `external_call_allowed=true` are blocked unless an explicit `approval_ref` is set, recording `status=blocked` and `external_call_made=false`; fixture failure backends (`fail` token) record `status=failed` while sibling critics still complete and synthesis disposition becomes `needs_more_evidence`. All persisted artifacts set `advisory_only=true`, `requires_human_review=true`, `human_approved=false`, `action_authorized=false`, and `external_call_made=false`; no network call, mutation, publication, or downstream decision authority is enabled. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_runtime.py -q` (9 passed); adjacent DARS regression `PYTHONPATH=src:. pytest tests/unit/test_dars_runtime.py tests/unit/test_dars_config.py tests/unit/test_dars_dispatch.py -q` (51 passed). |
| DARS critic adapter registry (M-CP-EXT-1) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-1-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_adapters.py; tests/unit/test_dars_critic_panel_runtime.py; HISYS-FR-DARS-CP-001; HISYS-FR-DARS-CP-007; HISYS-NFR-DARS-CP-001 | Implemented: `hisys.agents.dars_panel.CriticAdapterRegistry` plus `FixtureCriticAdapter` make critic dispatch class (`fixture` / `loopback` / `external`) and fixture outcome (`completed` / `failed` / `blocked` / `skipped`) explicit per `(critic_role, backend_id)` registration. `DarsCriticPanelRuntime.__init__` accepts an optional `adapter_registry`; without one it falls back to a closed default policy that classifies `external-*` backends as `external` (which the registry always blocks because `external_dispatch_allowed=False`) and treats `fixture-failing-critic` as a typed `failed` outcome — preserving the `MB-DARS-CP-T001` test contract without inspecting `backend_id` substrings inside `run_round`. External adapter resolution requires both `external_dispatch_allowed=True` *and* a truthy `approval_ref`; otherwise `resolve` raises `PermissionError` and the panel records `status=blocked` with `external_call_made=false`. The runtime removed the prior `"fail" in backend_id` failure heuristic and the inline external-prefix dispatch check; `FAILURE_BACKEND_MARKER`, `_is_fixture_failure`, and `_evaluate_dispatch` are deleted. Module `__all__` now exports `AdapterClass`, `BackendDispatchOutcome`, `CriticAdapterRegistry`, and `FixtureCriticAdapter` alongside the existing panel surface. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py -q` (13 passed). No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, and no remote push is enabled. |
| DARS critic panel execution graph plan (M-CP-EXT-3) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-3-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; src/hisys/agents/dars_panel_graph.py; tests/unit/test_dars_critic_panel_execution_graph_plan.py; tests/unit/test_dars_critic_panel_tool_execution_runtime.py; tests/unit/test_dars_critic_panel_adapters.py; tests/unit/test_dars_critic_panel_runtime.py; HISYS-FR-DARS-CP-006; HISYS-NFR-DARS-CP-001 | Implemented: `hisys.agents.dars_panel_graph.ExecutionGraphPlan` is a frozen, timestamp-free dataclass that captures critic-and-synthesis task nodes plus `critic -> synthesis` edges in a new sidecar module (re-exported from `hisys.agents.dars_panel` for compatibility). `ExecutionGraphPlan.from_task_ids` builds the graph from explicit critic + synthesis IDs and tags critic nodes with `dars-critics`/synthesis nodes with `dars-synthesis` concurrency groups; `ExecutionGraphPlan.from_round_plan` bridges existing `DarsRoundPlan` instances without changing runtime behavior. `__post_init__` raises `ValueError` for duplicate task IDs, unknown dependency endpoints (`source` or `target` not in node set), and dependency cycles (DFS three-color walk). `ExecutionGraphPlan.ready_set` returns deterministic lexically-sorted ready task IDs, excluding terminal (`completed`/`failed`/`blocked`/`skipped` per `TERMINAL_TASK_STATUSES`) and in-progress task IDs; synthesis becomes ready only after every critic task is terminal. `ExecutionGraphPlan.bounded_parallel_chunks(max_parallel=N)` chunks the current sorted ready-set into deterministic lists of at most `N` task IDs and raises `ValueError` for `max_parallel < 1`. `DarsCriticPanelRuntime.run_round` now constructs `ExecutionGraphPlan.from_round_plan(plan)` and asserts that the start-of-round ready-set equals the sorted critic task IDs as a structural consistency guard; the runtime remains serial, execution order is unchanged, and existing critique/synthesis/round-trace/boundary-record artifacts are unchanged. Module `__all__` now exports `DARS_CRITICS_CONCURRENCY_GROUP`, `DARS_SYNTHESIS_CONCURRENCY_GROUP`, `ExecutionGraphEdge`, `ExecutionGraphNode`, `ExecutionGraphPlan`, and `TERMINAL_TASK_STATUSES` alongside the existing surface. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_execution_graph_plan.py tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py -q` (38 passed). Deferred to follow-on increments: actual bounded-parallel runtime execution, deterministic clock injection, typed adapter-missing `LookupError` -> `status=blocked`, and a read-only `hisys run-dars-panel` CLI. No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, no CLI activation, and no remote push is enabled. |
| DARS critic panel per-task duration_ms (M-CP-EXT-9) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-9-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_tool_execution_runtime.py; HISYS-FR-DARS-CP-003; HISYS-FR-DARS-CP-004; HISYS-NFR-DARS-CP-001 | Implemented: closed the M-CP-EXT-8 open item (b) by adding a derived integer `duration_ms` field to every persisted `ExecutionBoundaryRecord`. `ExecutionBoundaryRecord` now carries `duration_ms: int = 0` immediately after `completed_at: str`. `DarsCriticPanelRuntime.run_round` now binds each per-task clock read to a timezone-aware `datetime` (`task_started = self._clock()`, `task_completed = self._clock()`) before formatting them with `_format_iso_timestamp`, and derives `task_duration_ms = max(0, int((task_completed.astimezone(timezone.utc) - task_started.astimezone(timezone.utc)).total_seconds() * 1000))`. Computing from the raw datetimes (before second-truncated string formatting) preserves sub-second intervals so a 250 ms task records `duration_ms=250` even when `started_at == completed_at` after formatting. The `max(0, ...)` clamp keeps record stability if an injected or system clock moves backward. The persisted JSON `adapter_class` value space, the safety envelope (`external_call_made`, `mutation_performed`, `action_authorized`, `advisory_only`, `requires_human_review`), the M-CP-EXT-5 byte-identical-under-fixed-clock regression, and the M-CP-EXT-8 per-task distinct timing regression all remain unchanged because the new field is derived from existing seams and a constant clock yields `duration_ms=0` deterministically. No CLI flag, no config-schema change, no `hisys run-dars-panel` output contract change, no parallel-execution activation. New tests: `test_panel_runtime_records_duration_ms_per_task` (counter clock returning four monotonically increasing offsets; asserts `[250, 750]` and integer typing) and `test_panel_runtime_clamps_negative_duration_ms_to_zero` (backward clock; asserts persisted `duration_ms == 0`). Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_cli.py tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_execution_graph_plan.py -q` (48 passed). Deferred to follow-on increments: behavior-preserving package split of `src/hisys/agents/dars_panel.py`, actual bounded-parallel execution activation, and the M20 codebase-domain bridge. No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, no CLI surface change, and no remote push is enabled. |
| DARS critic panel per-task distinct started_at/completed_at (M-CP-EXT-8) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-8-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_tool_execution_runtime.py; HISYS-FR-DARS-CP-003; HISYS-FR-DARS-CP-004 | Implemented: closed the M-CP-EXT-5 open item by moving the existing `self._clock` seam read from a single pre-loop `timestamp = _format_iso_timestamp(self._clock())` call to two per-task reads inside the `for plan_task, critic in zip(plan.critic_tasks, panel_config.critics, strict=True):` loop body of `DarsCriticPanelRuntime.run_round`. Each persisted `ExecutionBoundaryRecord` now carries a distinct `started_at = _format_iso_timestamp(self._clock())` read at the top of the loop body (before adapter resolution and dispatch decision logic) and a distinct `task_completed_at` read immediately before the record's construction. Under the default wall-clock lambda the two reads are naturally increasing; under an injected counter clock they are strictly distinct. No constructor signature change, no new clock parameter, no new schema field, no CLI flag added, no parallel-execution activation. The naive-datetime rejection invariant from M-CP-EXT-5 is preserved because both per-task reads still flow through `_format_iso_timestamp`. The M-CP-EXT-5 byte-identical-under-fixed-clock regression continues to pass because a constant clock returns the same value on consecutive reads. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_cli.py tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_execution_graph_plan.py -q` (46 passed). Deferred to follow-on increments: optional per-task `duration_ms` derived from `started_at`/`completed_at`, the package split of `src/hisys/agents/dars_panel.py`, and actual bounded-parallel execution activation. No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, no CLI surface change, and no remote push is enabled. |
| DARS critic panel read-only run-dars-panel CLI (M-CP-EXT-6) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-6-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/cli/main.py; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_cli.py; HISYS-FR-DARS-CP-001; HISYS-FR-DARS-CP-003; HISYS-FR-DARS-CP-007; HISYS-NFR-DARS-CP-001 | Implemented: added `hisys run-dars-panel`, a read-only argparse subcommand that wraps the already-implemented fixture-local `DarsCriticPanelRuntime.run_round` surface. The CLI parses a local JSON panel config file via the new `_load_dars_panel_config` helper into `DarsCriticPanelConfig` + `DarsCriticRoleConfig`, constructs `DarsCriticPanelRuntime(instance=InstanceRoot(...))` using the default fixture policy (no caller-supplied adapter registry, no external-dispatch flag, no clock seam override), and calls `run_round` with the explicit `--date`/`--request-id`/`--candidate-ref`/`--evidence-ref` inputs. The handler then prints a bounded JSON or text summary with `request_id`, `panel_id`, `execution_mode` (derived from `max_parallel_critics`), per-task `task_statuses`, `critique_refs`, `synthesis_ref`, `round_trace_ref`, and `execution_boundary_refs`. Typed `blocked` outcomes for `external-*` backends flow through the default fixture policy's `PermissionError`/`LookupError` arms of `run_round` unchanged: the task is reported as `status="blocked"`, no critique ref is written, the persisted boundary record carries the locked safety envelope (`external_call_made=false`, `mutation_performed=false`, `action_authorized=false`, `advisory_only=true`, `requires_human_review=true`) with `adapter_class="unresolved"`. The CLI returns exit code 0 whenever `run_round` persists the round, even if individual critic tasks are `blocked` or `failed`; non-zero exit codes remain reserved for argparse rejections and uncaught runtime invariant errors. The increment introduces no new service module, no new clock seam, no adapter-registry override flag, no external-dispatch enable flag, no worker/thread/subprocess/async spawn, and no candidate/evidence/rubric mutation. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_cli.py tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_execution_graph_plan.py -q` (45 passed). Deferred to follow-on increments: per-task `started_at` / `completed_at` distinct from a single round-level clock tick, package split of the increasingly large `src/hisys/agents/dars_panel.py`, and actual bounded-parallel execution activation. No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, and no remote push is enabled. |
| DARS critic panel unresolved adapter class literal (M-CP-EXT-7) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-7-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_tool_execution_runtime.py; tests/unit/test_dars_critic_panel_adapters.py; tests/unit/test_dars_critic_panel_runtime.py; tests/unit/test_dars_critic_panel_execution_graph_plan.py; HISYS-FR-DARS-CP-007 | Implemented: closed the M-CP-EXT-2/M-CP-EXT-4 deferred refinement by widening `AdapterClass` to `Literal["fixture", "loopback", "external", "unresolved"]` and changing the single substitution inside `DarsCriticPanelRuntime.run_round` from `adapter_class=... else "fixture"` to `adapter_class=... else "unresolved"`. Every `ExecutionBoundaryRecord` persisted from a `disabled` / `PermissionError` / `LookupError` branch (where `adapter is None`) now carries `adapter_class="unresolved"` so reviewers can distinguish "the role was bound to a fixture adapter and the adapter chose this outcome" from "no adapter resolution attempt yielded an adapter for this role". `FixtureCriticAdapter.__post_init__` continues to reject `adapter_class` values outside `{"fixture", "loopback", "external"}`; `"unresolved"` is reserved for `ExecutionBoundaryRecord` reporting and is never returned by `CriticAdapterRegistry.resolve(...)`. The persisted JSON `adapter_class` field is unchanged in name and type; only its value space expands, and no test asserted `"fixture"` on a blocked-no-adapter-resolved boundary record before this increment. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_execution_graph_plan.py -q` (43 passed). Deferred to follow-on increments: a read-only `hisys run-dars-panel` CLI (M-CP-EXT-6), per-task `started_at` / `completed_at` distinct from a single round-level clock tick, and actual bounded-parallel runtime execution. No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, no CLI activation, and no remote push is enabled. |
| DARS critic panel deterministic clock seam (M-CP-EXT-5) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-5-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_tool_execution_runtime.py; tests/unit/test_dars_critic_panel_adapters.py; tests/unit/test_dars_critic_panel_runtime.py; tests/unit/test_dars_critic_panel_execution_graph_plan.py; HISYS-FR-DARS-CP-003; HISYS-NFR-DARS-CP-002 | Implemented: `DarsCriticPanelRuntime.__init__` now accepts an optional `clock: Callable[[], datetime] | None = None` parameter that defaults to `lambda: datetime.now(timezone.utc)`; `DarsCriticPanelRuntime.run_round` reads the round-level timestamp through `self._clock()` and formats it through the new private helper `_format_iso_timestamp(moment)`, which rejects naive datetimes (`ValueError("clock must return timezone-aware datetime")`) and converts the result to a deterministic UTC ISO-8601 `...Z` string with microseconds truncated. Tests can now inject a fixed clock and assert byte-identical `ExecutionBoundaryRecord` JSON output across consecutive `run_round` invocations. The runtime continues to read the clock exactly once per round; per-task `started_at == completed_at` is preserved (real per-task timing remains a deferred increment, now cheap because the clock is already a per-runtime dependency). Production callers that do not pass `clock` are unaffected; the wall-clock default lambda always returns a timezone-aware UTC datetime. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_execution_graph_plan.py -q` (41 passed). Deferred to follow-on increments: per-task `started_at` / `completed_at` distinct from a single round-level clock tick, a read-only `hisys run-dars-panel` CLI (M-CP-EXT-6), a non-structural `adapter_class="unresolved"` literal, and actual bounded-parallel execution. No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, no CLI activation, and no remote push is enabled. |
| DARS critic panel typed adapter-missing blocked (M-CP-EXT-4) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-4-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_adapters.py; tests/unit/test_dars_critic_panel_runtime.py; tests/unit/test_dars_critic_panel_tool_execution_runtime.py; tests/unit/test_dars_critic_panel_execution_graph_plan.py; HISYS-FR-DARS-CP-007; HISYS-NFR-DARS-CP-001 | Implemented: `DarsCriticPanelRuntime.run_round` now catches `(LookupError, PermissionError)` from `self.adapter_registry.resolve(...)` in a single sibling `except` arm so an explicit caller-supplied `CriticAdapterRegistry` with no adapter for a `(critic_role, backend_id)` pair yields a typed `DarsTaskResult(status="blocked", error_message=str(exc))` plus a matching `ExecutionBoundaryRecord(dispatch_decision="blocked", dispatch_reason=str(exc))` instead of crashing the round. The `LookupError` message (`"no critic adapter registered for role=... backend_id=..."`) flows verbatim into both the task result `error_message` and the persisted boundary record `dispatch_reason`. The boundary record continues to lock `external_call_made=False`, `mutation_performed=False`, `action_authorized=False`, `advisory_only=True`, and `requires_human_review=True`, and the existing `adapter_class="fixture"` structural default (already used by the `disabled` and `PermissionError` branches when no adapter was resolved) applies to the new branch as well. `CriticAdapterRegistry.resolve` itself is unchanged and continues to raise `LookupError` for callers other than `run_round`; `_DefaultFixturePolicy` synthesizes adapters on demand and is therefore unaffected. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_execution_graph_plan.py -q` (39 passed). Deferred to follow-on increments: a non-structural `adapter_class="unresolved"` literal (open item from the M-CP-EXT-2 reflection), deterministic clock injection (M-CP-EXT-5), a read-only `hisys run-dars-panel` CLI (M-CP-EXT-6), and actual bounded-parallel runtime execution. No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, no CLI activation, and no remote push is enabled. |
| DARS critic panel execution-boundary record (M-CP-EXT-2) | docs/plans/dars-critic-panel-platform-runtime-next.md; docs/plans/dars-critic-panel-mcp-ext-2-implementation-tasks.md; docs/traceability/dars-critic-panel-runtime-traceability.md; src/hisys/agents/dars_panel.py; tests/unit/test_dars_critic_panel_tool_execution_runtime.py; tests/unit/test_dars_critic_panel_adapters.py; tests/unit/test_dars_critic_panel_runtime.py; HISYS-FR-DARS-CP-003; HISYS-FR-DARS-CP-004; HISYS-FR-DARS-CP-007; HISYS-NFR-DARS-CP-001; HISYS-NFR-DARS-CP-002 | Implemented: `hisys.agents.dars_panel.ExecutionBoundaryRecord` is a typed per-task dispatch boundary record that locks `external_call_made=False`, `mutation_performed=False`, `action_authorized=False`, `advisory_only=True`, and `requires_human_review=True`; the dataclass `__post_init__` raises `ValueError` on any unsafe override and on `dispatch_decision` outside `{allowed, blocked}`. `write_execution_boundary_record` persists each record as deterministic JSON (UTF-8, `indent=2`, `sort_keys=True`) under `<instance>/runtime-boundary/dars-panel/<YYYYMMDD>/<REQUEST_ID>/<TASK_ID>.json` after slug-validating `date`/`request_id`/`task_id` via shared `_DATE_PATTERN` (`^[0-9]{8}$`) and `_REQUEST_ID_PATTERN`/`_TASK_ID_PATTERN` (`^[A-Za-z0-9_-]+$`) constants that mirror `src/hisys/operations/codebase_analysis.py`; empty strings, hyphenated `YYYY-MM-DD` dates, absolute paths (`/abs`), and traversal segments (`..`) are rejected before any directory is created. `DarsCriticPanelRuntime.run_round` slug-validates `yyyymmdd` and `request_id` once at entry, then writes one `ExecutionBoundaryRecord` per critic task — for every dispatch outcome (`enabled=false` → `dispatch_decision=blocked`; `PermissionError` from the adapter registry → `blocked`; `external_call_allowed without approval_ref` → `blocked`; `fixture_outcome=failed` → `allowed` + `status=failed`; `fixture_outcome in {blocked, skipped}` → `blocked`; resolved fixture → `allowed` + `status=completed` with `critique_ref` filled in). The resulting refs are exposed on `DarsRoundResult.execution_boundary_refs: list[str]`. `_panel_dir` shares the same slug validator so any future caller (CLI, other operations) inherits the same chokepoint. Module `__all__` now exports `DispatchDecision`, `ExecutionBoundaryRecord`, `RUNTIME_BOUNDARY_SUBTREE`, and `write_execution_boundary_record` alongside the existing surface. Focused gate: `PYTHONPATH=src pytest tests/unit/test_dars_critic_panel_tool_execution_runtime.py tests/unit/test_dars_critic_panel_adapters.py tests/unit/test_dars_critic_panel_runtime.py -q` (28 passed). No live DARS dispatch, no external call, no credential resolution, no mutation, no publication, and no remote push is enabled. |
| Codebase domain `investigate-domain` CLI smoke (M20.4) | docs/plans/m20-codebase-domain-artifact-bridge-m20-4-implementation-tasks.md; ralph.md M20.4; tests/unit/test_domain_cli.py; src/hisys/cli/main.py; src/hisys/domain/specs.py; src/hisys/domain/domain_adapters.py; src/hisys/domain/translation.py; HISYS-FR-DOM-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `tests/unit/test_domain_cli.py::test_investigate_domain_codebase_smokes_local_bundle` builds a complete local codebase-analysis bundle under a temporary instance root via the existing artifact writers (`build_codebase_inventory`, `build_python_symbol_index`, `build_codebase_scope_map`, `build_codebase_validation_plan`, `scan_codebase_risk_boundaries`, plus their `write_*` writers), writes a `DomainInvestigationRequest` JSON with five `runtime_record` `DomainSourceRef` entries (one per role), and runs `main(["investigate-domain", ...])` end-to-end. No new CLI argument is added; the existing argparse entry point dispatches through `_default_domain_adapter_registry`, the structured codebase `StructuredDomainAdapter` from `codebase_spec()` runs the M20.3 enrichment, and the persisted `hisys-tool-result-*.json` envelope preserves `domain=="codebase"`, `external_call_made==false`, `mutation_performed==false`, `requires_human_review==true`, `quality_gate=="passed"`, and `status=="completed"`. The persisted structured `domain-investigation-result-*.json` artifact contains exactly one `codebase_analysis_bundle` evidence package with ordered four-role `evidence_refs=[inventory, symbol_index, scope_map, risk_scan]`. The run-summary report `reports/run-summaries/20260520/domain-investigation-report.json` carries the `codebase` domain and the tool-result ref. No live repo clone, model/network call, credential resolution, destructive Git, raw source archival, or remote push is enabled by the CLI path. Gate: `PYTHONPATH=src pytest tests/unit/test_codebase_domain_artifact_bridge.py tests/unit/test_domain_three_layer_use_cases.py tests/unit/test_structured_domain_adapter.py tests/unit/test_domain_runtime_artifacts.py tests/unit/test_domain_cli.py -q` (29 passed); DARS critic-panel focused regression (48 passed); traceability validator OK; secret scan `hit_count=0`; `git diff --check` clean. Deferred: M20.5 milestone finish packet; a future repeatable `--codebase-artifact` argparse flag remains backlog. |
| Codebase domain bundle enrichment of DomainInvestigationResult (M20.3) | docs/plans/m20-codebase-domain-artifact-bridge-m20-3-implementation-tasks.md; ralph.md M20.3; src/hisys/domain/layers.py; src/hisys/domain/translation.py; src/hisys/domain/domain_adapters.py; tests/unit/test_codebase_domain_artifact_bridge.py; HISYS-FR-DOM-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `DomainUseCase.run` derives `quality_gate` from `investigation.codebase_bundle_gate` (`candidate_complete` → `passed`, else `needs_more_evidence`). `DomainUseCaseArtifactPacket` and `DomainUseCaseArtifactTranslator` propagate `codebase_artifact_refs`, `codebase_bundle_gate`, and `codebase_missing_evidence`. New `build_codebase_bundle_enrichment` helper in `src/hisys/domain/translation.py` calls `load_codebase_review_bundle` (which routes every caller-supplied ref through `resolve_instance_runtime_ref`) only when the role gate is `candidate_complete`; on success it emits a bounded `DomainEvidencePackage` with `evidence_type="codebase_analysis_bundle"`, ordered `evidence_refs=[inventory, symbol_index, scope_map, risk_scan]`, summary limited to `inventory_files` / `scopes` / `risk_categories` counts, and limitations preserving advisory/human-review boundaries. Loader failures (`FileNotFoundError`, `ValueError`, `OSError`) are caught and produce an `unreadable bundle` evidence package plus `override_quality_gate="needs_more_evidence"`; incomplete role refs produce a `missing role` evidence package with the same downgrade. Regression-pin tests in `tests/unit/test_codebase_domain_artifact_bridge.py` cover both failure paths: `test_codebase_domain_result_maps_incomplete_bundle_refs_to_needs_more_evidence` exercises the missing-role path (4 refs, no validation_plan, loader never called) and asserts a `missing role: validation_plan` limitation; `test_codebase_domain_result_maps_unreadable_complete_bundle_to_needs_more_evidence` exercises the loader-failure path (5 refs to non-existent files, work-product gate `candidate_complete`, `FileNotFoundError` caught) and asserts an `unreadable` limitation. `build_domain_investigation_result` accepts the optional enrichment and an optional `override_quality_gate` and appends the codebase package after the existing generic structured package. `StructuredDomainAdapter.investigate` calls the helper with `context.instance_root` so the safe-loader chokepoint stays in force. No CLI flag, no live repository clone, no model/network/credential resolution, no destructive Git, no raw source archival, and no remote push is enabled. Gate: `PYTHONPATH=src pytest tests/unit/test_codebase_domain_artifact_bridge.py tests/unit/test_domain_three_layer_use_cases.py tests/unit/test_structured_domain_adapter.py tests/unit/test_domain_runtime_artifacts.py -q` (20 passed); DARS critic-panel focused regression (48 passed); traceability validator OK; secret scan `hit_count=0`; `git diff --check` clean. Deferred: M20.4 CLI smoke, M20.5 docs/gate finish. |
| Codebase domain artifact bundle gate (M20.2) | docs/plans/m20-codebase-domain-artifact-bridge-m20-2-implementation-tasks.md; ralph.md M20.2; src/hisys/domain/layers.py; src/hisys/domain/use_cases.py; tests/unit/test_codebase_domain_artifact_bridge.py; HISYS-FR-DOM-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `InvestigationWorkProduct.codebase_bundle_gate` and `codebase_missing_evidence` classify refs-only codebase-analysis bundles without reading JSON, cloning repositories, adding CLI flags, or authorizing action. Incomplete required role sets (`inventory`, `symbol_index`, `scope_map`, `validation_plan`, `risk_scan`) yield `needs_more_evidence` with sorted missing roles and `requires_human_review=True`; complete role sets yield `candidate_complete`, not final approval. Gate: `tests/unit/test_codebase_domain_artifact_bridge.py` plus domain/DARS focused regressions, traceability validator, secret scan, and `git diff --check`. Deferred: M20.3 safe loader/schema validation and result enrichment, M20.4 CLI smoke, M20.5 docs/gate finish. |
| Codebase domain artifact bundle acceptance (M20.1) | docs/plans/m20-codebase-domain-artifact-bridge-implementation-tasks.md; ralph.md M20.1; src/hisys/domain/layers.py; src/hisys/domain/use_cases.py; tests/unit/test_codebase_domain_artifact_bridge.py; HISYS-FR-DOM-001..006; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `InvestigationWorkProduct.codebase_artifact_refs` carries ordered, deduplicated refs for `runtime_record` sources under `runtime-boundary/codebase-analysis/` while rejecting `..` path segments and preserving non-codebase runtime/source IDs in `evidence_refs`. M20.1 is refs-only: it reads no codebase-analysis JSON, resolves no instance-root files, adds no CLI flag, and does not surface the bundle into `DomainInvestigationResult`; loading, completeness gating, enrichment, and CLI integration remain deferred to M20.2..M20.4. Gate: `PYTHONPATH=src pytest tests/unit/test_codebase_domain_artifact_bridge.py tests/unit/test_domain_three_layer_use_cases.py tests/unit/test_structured_domain_adapter.py tests/unit/test_domain_runtime_artifacts.py -q`; DARS focused regression; traceability validator; secret scan; `git diff --check`. |
| Codebase analysis source-inspection decision packet (M19.1..M19.4) | docs/public/codebase-analysis.md; revision_plan_v004.md; ralph.md M19; src/hisys/operations/codebase_analysis.py; src/hisys/cli/main.py; tests/unit/test_codebase_source_inspection_decision.py; HISYS-FR-DOM-005; HISYS-T-024; HISYS-CON-010..012; HISYS-CON-022..023 | Implemented: `hisys.operations.codebase_analysis.review_codebase_source_inspection` is a pure reviewer that consumes the four-file codebase-analysis bundle (inventory, Python symbol index, scope map + validation plan, risk-boundary scan) and returns a `CodebaseSourceInspectionDecision` with `schema_id=hisys.codebase.source_inspection_decision`. The decision-value Pydantic `Literal` is `complete_for_human_review` or `blocked_needs_more_evidence` and structurally rejects `approved`, `safe_to_deploy`, and `ready_for_live_action`. The reviewer enumerates a sorted `missing_evidence` list when any of the five required records is `None`, populates a sorted `validation_findings` list when any per-record safety invariant fails (`<artifact>.raw_source_content_persisted=true` for any of the five records, `risk_scan.action_authorized=true`, any `RiskBoundaryFinding.action_authorized=true`, `scope_map.inventory_schema_id` mismatch with `inventory.schema_id`, and `scope_map.symbol_index_schema_id` mismatch with `symbol_index.schema_id`), and records caller-supplied `unresolved_blockers`; the decision is `complete_for_human_review` only when all three lists are empty. `load_codebase_review_bundle` is the safe-ref-and-load chokepoint that calls `resolve_instance_runtime_ref` on each of the four caller-supplied refs (rejecting empty refs, absolute paths, `..` traversal segments, and symlinks whose real target escapes the instance root) before any read; the scope-map JSON is the M17.4 `{scope_map, validation_plan}` wrapper that the loader unpacks. `write_codebase_source_inspection_decision` persists `source-inspection-decision.{json,md}` under `runtime-boundary/codebase-analysis/<YYYYMMDD>/<REQUEST_ID>/` after the same slug validation that protects the other writers; the Markdown preamble explicitly states the artifact is review evidence and enumerates the three forbidden decision values. `hisys review-codebase-analysis` wraps the loader + reviewer + writer for CLI use; it exits 0 for `complete_for_human_review` and 2 for `blocked_needs_more_evidence` so automation can branch on the decision without re-parsing the JSON. The decision record and writer envelope record `raw_source_content_persisted=false`, `action_authorized=false`, `external_call_made=false`, `mutation_performed=false`, and `publication_or_live_action_approved=false`. No external repository clone, raw source archival, model call, live network access, credential use, remote push, or publication is enabled by this surface. |

I4 is present as a fixture-backed foundation/skeleton with CLI glue for local
runtime execution. I5 is present as a fixture-backed extraction foundation.
I6 is present as a fixture-backed editorial draft and duplicate/conflict review
foundation that writes only runtime-local memo draft/report artifacts and a
controlled Obsidian vault-write dry-run boundary that writes preview reports
under `runtime-boundary/obsidian/` without modifying the target vault. I7-A is
present as a fixture-backed Chief Editor alert decision foundation that writes
runtime-local alert decisions/reports, suppresses same-date repeated alert
candidates by `suppression_key`, requests approval for high/critical or non-local
target alert candidates, selects a Chief Editor product factory variant
(`analysis_only` or `alert_delivery_dry_run`) from `config/chief-editor.yaml` or
CLI override, writes dry-run alert action plans with blocked reasons
and approved pending send-candidate markers, applies runtime-local approve/reject
transitions while keeping `action_taken=none`, takes no live alert actions, and
records blocked live-connector decisions for explicit connector/action/approval
safety gates before any live adapter can be added. I8-A/B/C/D adds runtime-local advisory DARS handoff loopback artifacts without
implementing DARS or making live DARS calls, plus a common configuration
envelope validator, DARS backend/role config contract, canonical
Hisys↔DARS request/response data-format contract, and implemented envelope
validators in `hisys.agents.dars_protocol` for advisory-only request/response
boundary models, plus `hisys.agents.dars_dispatch` runtime-boundary dispatch
decision records before any backend adapter can run, and `hisys.agents.dars_backend`
deterministic fixture-file response loading/validation with accepted/rejected
validation reports, and `hisys.agents.dars_trace` source/memo/alert/evidence-to-critique lineage artifacts, plus disabled-by-default no-network mock endpoint boundary. Investigator multi-agent fixture
research, the disabled Selenium read-only harness, purpose-specific evidence
agents, purpose-aware auto-planning, and the configurable connector registry are
implemented. Live-A adds the governed live-source search foundation: boundary
policy docs, disabled source connector examples, registry validation, dispatch
decision records, and provenance schemas. Live-B adds dry-run connector planning
and local fixture-publisher evidence integration into the domain investigation,
DARS advisory trace, and Chief Editor research review, still with no live adapter
execution enabled. Live-C adds a manual-smoke DOI metadata boundary with
injectable-transport tests, dry-run/blocked CLI evidence, and an opt-in operator
environment flag for manual live metadata smoke only; CI still performs no live
network call. Live-D adds a fixture-first OA PDF collector with license-signal
gating, manual PDF smoke env gating, and dry-run/blocked CLI evidence; CI still
performs no live PDF fetch. Live-E adds DOI metadata to OA PDF candidate planning
with `candidate_plan_only` artifacts and planned connector handoffs while still
fetching no PDF bytes. Live-F adds approved manual OA PDF smoke retrieval with
injectable transport, fixture-transport CLI tests, and completed smoke reports
that truthfully record `pdf_downloaded=true` and `external_call_made=true` only
after gates pass; checked-in config and CI remain no-live-PDF. Live-G promotes
explicit approved OA PDF source refs into `InvestigationDataPackage`, DARS trace,
and Chief Editor review with `promoted_pdf_evidence_refs` and no implicit PDF
discovery or live fetch. Live-H derives quote-only `source_quote_refs` from
explicit promoted OA PDF evidence, links them into `InvestigationDataPackage`,
DARS trace, and Chief Editor review, and keeps novelty claims conditional while
performing no OCR/PDF parsing/network calls. Live-I maps explicit source quotes
to `claim_evidence_ledger_refs` using support/contradict/needs-evidence
relations, links the ledger into `InvestigationDataPackage`, DARS trace, and
Chief Editor review with `claim_evidence_ledger_present` status, and preserves
the quote/interpretation boundary without strengthening publication claims.
Live-J aggregates explicit ledger refs into `claim_evidence_summary_refs` with
support/contradict/needs-evidence balance and advisory confidence, links summary
refs into `InvestigationDataPackage`, DARS trace, and Chief Editor review with
`claim_evidence_summary_present` status, and keeps confidence conditional rather
than proof of novelty or publication readiness. Live-K checks required
recommendation claims against explicit summary refs, links `claim_coverage_gate_refs`
into `InvestigationDataPackage`, DARS trace, and Chief Editor review with
`claim_coverage_gate_present` status and `conditional_manuscript_language_only`
so manuscript-facing claims remain conditional. Live-L records controlled
required recommendation claims as `recommendation_claim_registry_refs`, links
those refs into `InvestigationDataPackage`, DARS trace, and Chief Editor review
with `recommendation_claim_registry_present` status, and keeps stronger
manuscript-facing claims blocked until Live-K coverage gates are run.
I9-A adds a redacted secret-like value scanner for HISYS-T-021
quality gates, I9-B adds backup manifest plus restore dry-run verification for
HISYS-T-023, I9-C adds local operator health status for required runtime
directories and disabled/loopback connector posture, and I9-D adds
release-readiness evidence summaries for HISYS-T-024 trace path and gate status.
Full workflow coverage remains pending; later increments (real DARS adapter,
critique feedback, and any explicitly approved live connector adapter) are not
implemented yet.

## Module to controlled-doc map

| Module | Controlled doc / SRS area | Test |
|---|---|---|
| `hisys.core.ids` | HISYS-DATA-001 (stable IDs) | `tests/unit/test_core.py` |
| `hisys.core.time` | HISYS-IDD-001 Section 2 (ISO timestamps) | `tests/unit/test_core.py` |
| `hisys.core.errors` | HISYS-SDD-001 Section 8 (failure handling) | `tests/unit/test_core.py` |
| `hisys.core.result` | HISYS-IDD-001 Section 2 (error status) | `tests/unit/test_core.py` |
| `hisys.schemas.source` | HISYS-FR-SRC-001..005, HISYS-SCHEMA-001 Section 3 | `tests/unit/test_schemas.py` |
| `hisys.schemas.compliance` | HISYS-CHECK-WEB-001, HISYS-NFR-SEC-003, HISYS-NFR-SEC-005, HISYS-CON-022..023 | `tests/unit/test_registry.py` |
| `hisys.registry.source_registry` | HISYS-SRC-REG-INIT-001, HISYS-FR-SRC-001..005, HISYS-T-001..002 | `tests/unit/test_registry.py` |
| `hisys.adapters.base` | HISYS-IDD-001 Section 4, HISYS-FR-DS-001..006, normalize/provenance/error contract | `tests/unit/test_adapters.py` |
| `hisys.adapters.runtime` | HISYS-FR-DS-005, HISYS-NFR-REL-001, HISYS-CON-014, HISYS-T-006 | `tests/unit/test_adapters.py` |
| `hisys.schemas.observation` | HISYS-FR-INV-002..005, HISYS-DATA-002, HISYS-SCHEMA-001 Section 4 | `tests/unit/test_schemas.py` |
| `hisys.schemas.signal` | HISYS-FR-EXT-001..004, HISYS-SCHEMA-001 Section 5 | `tests/unit/test_schemas.py` |
| `hisys.schemas.perspective` | HISYS-FR-PER-001..004, HISYS-SCHEMA-001 Section 6 | `tests/unit/test_schemas.py` |
| `hisys.schemas.memo` | HISYS-FR-MEM-001..005, HISYS-SCHEMA-001 Section 7 | `tests/unit/test_schemas.py` |
| `hisys.schemas.alert` | HISYS-FR-CE-002..006, HISYS-SCHEMA-001 Section 8 | `tests/unit/test_schemas.py` |
| `hisys.schemas.handoff` | HISYS-FR-AGT-001..005, HISYS-SCHEMA-001 Section 9, HISYS-DARS-CONTRACT-001 | `tests/unit/test_schemas.py` |
| `hisys.schemas.hermes_trace` | HISYS-FR-DS-006, HISYS-FR-INV-006, HISYS-FR-AGT-005, HISYS-DATA-005, HISYS-SCHEMA-001 Section 10 | `tests/unit/test_schemas.py` |
| `hisys.schemas.audit` | HISYS-FR-ADM-002, HISYS-SCHEMA-001 Section 11 | `tests/unit/test_schemas.py` |
| `hisys.schemas.domain_investigation` | HISYS-FR-INV-001..006, HISYS-DARS-CONTRACT-001, HISYS-T-024, HISYS-CON-010..012 | `tests/unit/test_domain_investigation.py` |
| `hisys.adapters.hardware_mock` | HISYS-FIXTURE-001 hardware-mock-temperature, HISYS-T-003 | `tests/unit/test_adapters.py` |
| `hisys.adapters.web_news_mock` | HISYS-FIXTURE-001 web-news-rss-permitted, HISYS-T-004 | `tests/unit/test_adapters.py` |
| `hisys.adapters.agent_system_mock` | HISYS-FIXTURE-001 agent-dars-critique, HISYS-T-005 | `tests/unit/test_adapters.py` |
| `hisys.adapters.hermes_tool_mock` | HISYS-FIXTURE-001 hermes-tool-hierarchy, HISYS-T-005A | `tests/unit/test_adapters.py`, `tests/integration/test_trace_path.py`, `tests/integration/test_cli_hermes_runtime.py` |
| `hisys.config.instance` | HISYS-RUNTIME-DIR-001, HISYS-D-015, HISYS-D-016 | `tests/unit/test_instance_config.py` |
| `hisys.config.loader` | HISYS-FR-SRC-001..005, HISYS-T-001..002 | `tests/unit/test_instance_config.py`, `tests/unit/test_example_instance.py` |
| `hisys.config.validation` | HISYS-FR-AGT-001..005, HISYS-T-019, HISYS-T-020, HISYS-CON-010..012 | `tests/unit/test_config_validation.py` |
| `hisys.audit.writer` | HISYS-FR-ADM-002, HISYS-D-015, secret redaction guard | `tests/unit/test_runtime_writers.py` |
| `hisys.audit.governance` | HISYS-FR-ADM-002, HISYS-SCHEMA-001, HISYS-FR-INV-001..006, HISYS-DARS-CONTRACT-001, HISYS-D-015, HISYS-T-024 | `tests/unit/test_lapidary_governance_audit.py` |
| `hisys.integrations.hermes_boundary` | HISYS-FR-DS-006, HISYS-FR-INV-006, HISYS-DATA-005 | `tests/unit/test_runtime_writers.py` |
| `hisys.integrations.obsidian_vault` | HISYS-FR-MEM-001..005, HISYS-IF-007, HISYS-DATA-002, HISYS-DATA-005, HISYS-CON-012 | `tests/unit/test_vault_writer.py` |
| `hisys.config.obsidian_live` Git lifecycle plans/executors | HISYS-FR-MEM-001..005, HISYS-FR-INV-001..006, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_obsidian_git_management.py`, `tests/unit/test_obsidian_git_fixture_executor.py` |
| `hisys.integrations.live_connectors` | HISYS-FR-AGT-004, HISYS-T-020, HISYS-T-022, HISYS-CON-010, HISYS-CON-012, HISYS-CON-022..023 | `tests/unit/test_live_connectors.py` |
| `hisys.connectors.live_source_config` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_live_source_config.py` |
| `hisys.connectors.live_source_dispatch` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_live_source_config.py` |
| `hisys.connectors.live_source_evidence` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_live_source_evidence.py` |
| `hisys.connectors.fixture_publisher` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_fixture_publisher_connector.py` |
| `hisys.connectors.doi_metadata` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_doi_metadata_connector.py` |
| `hisys.connectors.open_access_pdf` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_open_access_pdf_connector.py` |
| `hisys.connectors.pdf_candidate_planner` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_pdf_candidate_planner.py` |
| `hisys.connectors.pdf_evidence_promotion` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_pdf_evidence_promotion.py`, `tests/unit/test_domain_cli.py` |
| `hisys.connectors.pdf_quote_extractor` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_pdf_quote_extractor.py`, `tests/unit/test_pdf_quote_cli.py`, `tests/unit/test_domain_cli.py` |
| `hisys.connectors.claim_evidence_ledger` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_claim_evidence_ledger.py`, `tests/unit/test_claim_evidence_ledger_cli.py`, `tests/unit/test_domain_cli.py` |
| `hisys.connectors.claim_evidence_summary` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_claim_evidence_summary.py`, `tests/unit/test_claim_evidence_summary_cli.py`, `tests/unit/test_domain_cli.py` |
| `hisys.connectors.claim_coverage_gate` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_claim_coverage_gate.py`, `tests/unit/test_claim_coverage_gate_cli.py`, `tests/unit/test_domain_cli.py` |
| `hisys.connectors.recommendation_claim_registry` | HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_recommendation_claim_registry.py`, `tests/unit/test_recommendation_claim_registry_cli.py`, `tests/unit/test_domain_cli.py` |
| `hisys.investigator.runtime` | HISYS-INST-INV-001, HISYS-FR-INV-001..006, HISYS-T-007..008 | `tests/unit/test_investigator_runtime.py` |
| `hisys.extraction.extractor` | HISYS-FR-EXT-001..005, HISYS-DATA-002, HISYS-T-009..010 | `tests/unit/test_extraction_runtime.py` |
| `hisys.extraction.runtime` | HISYS-FR-EXT-001..005, HISYS-D-015, HISYS-T-009..010 | `tests/unit/test_extraction_runtime.py` |
| `hisys.editor.drafter` | HISYS-FR-PER-001..004, HISYS-FR-MEM-001..005, HISYS-DATA-002, HISYS-T-011..012 | `tests/unit/test_editor_runtime.py` |
| `hisys.editor.runtime` | HISYS-FR-PER-001..004, HISYS-FR-MEM-001..005, HISYS-D-015, HISYS-T-011..013 | `tests/unit/test_editor_runtime.py` |
| `hisys.chief_editor.policy` | HISYS-FR-CE-001..006, HISYS-CE-POLICY-001, HISYS-T-014..018 | `tests/unit/test_chief_editor_runtime.py` |
| `hisys.chief_editor.runtime` | HISYS-FR-CE-001..006, HISYS-D-015, HISYS-T-014..018 | `tests/unit/test_chief_editor_runtime.py` |
| `hisys.chief_editor.product` | HISYS-FR-CE-001..006, HISYS-CE-POLICY-001, HISYS-D-015, HISYS-T-025 | `tests/unit/test_chief_editor_runtime.py` |
| `hisys.chief_editor.action_plan` | HISYS-FR-CE-001..006, HISYS-D-015, HISYS-T-019, HISYS-T-021 | `tests/unit/test_chief_editor_runtime.py` |
| `hisys.chief_editor.approval` | HISYS-FR-CE-006, HISYS-D-015, HISYS-T-020 | `tests/unit/test_chief_editor_runtime.py` |
| `hisys.chief_editor.connector` | HISYS-FR-CE-006, HISYS-FR-AGT-004, HISYS-D-015, HISYS-T-022 | `tests/unit/test_alert_connector_runtime.py` |
| `hisys.agents.dars` | HISYS-FR-AGT-001..005, HISYS-DARS-CONTRACT-001, HISYS-D-015, HISYS-T-023..024 | `tests/unit/test_dars_runtime.py` — also covers `DarsCritiqueSourceWeight`/`DarsCritiqueRecord.source_weights` ByeSys-zero normalization and the optional `openai_compatible` local-LLM-boundary adapter |
| `hisys.provenance.source_weighting` | HISYS-FR-AGT-001..005, HISYS-DARS-CONTRACT-001, HISYS-CON-010..012 | `tests/unit/test_source_weighting.py` — `is_byesys_source`, `EvidenceSufficiencyVerdict`, `claim_has_sufficient_non_byesys_evidence`, reviewer-terminology aliases |
| `hisys.agents.dars_config` | HISYS-FR-AGT-001..005, HISYS-T-019, HISYS-T-020, HISYS-CON-010..012 | `tests/unit/test_dars_config.py` |
| `hisys.agents.dars_protocol` | HISYS-DARS-CONTRACT-001, HISYS-FR-AGT-001..005, HISYS-T-019, HISYS-T-020, HISYS-T-024, HISYS-CON-010..012 | `tests/unit/test_dars_protocol.py` |
| `hisys.agents.dars_dispatch` | HISYS-DARS-CONTRACT-001, HISYS-FR-AGT-001..005, HISYS-T-019, HISYS-T-020, HISYS-T-024, HISYS-CON-010..012 | `tests/unit/test_dars_dispatch.py`, `tests/unit/test_dars_appraiser_separation.py` |
| `hisys.agents.appraiser_separation` | HISYS-DARS-CONTRACT-001, HISYS-FR-AGT-001..005, HISYS-SCHEMA-001, HISYS-CON-010..012 | `tests/unit/test_dars_appraiser_guard.py` |
| `hisys.agents.dars_backend` | HISYS-DARS-CONTRACT-001, HISYS-FR-AGT-001..005, HISYS-T-019, HISYS-T-020, HISYS-T-024, HISYS-CON-010..012 | `tests/unit/test_dars_backend.py`, `tests/unit/test_dars_mock_endpoint.py` |
| `hisys.agents.dars_trace` | HISYS-DARS-CONTRACT-001, HISYS-T-024, HISYS-FR-AGT-001..005, HISYS-FR-INV-001..006, HISYS-FR-MEM-001..005 | `tests/unit/test_dars_trace.py` |
| `hisys.operations.backup` | HISYS-T-023, HISYS-FR-ADM-003, HISYS-DATA-001..004 | `tests/unit/test_backup_restore.py`; `tests/unit/test_release_ops_cli.py`; CLI `hisys backup-runtime`, `hisys restore-backup-dry-run` |
| `hisys.operations.codebase_analysis` | HISYS-FR-DOM-005, HISYS-T-024, HISYS-CON-010..012, HISYS-CON-022..023 | `tests/unit/test_codebase_analysis_inventory.py`, `tests/unit/test_codebase_symbol_index.py`, `tests/unit/test_codebase_scope_map.py`, `tests/unit/test_codebase_risk_boundary_scan.py`, `tests/unit/test_codebase_source_inspection_decision.py`; CLI `hisys build-codebase-inventory`, `hisys build-code-symbol-index`, `hisys build-codebase-map`, `hisys scan-codebase-boundaries`, `hisys review-codebase-analysis` |
| `hisys.operations.health` | HISYS-FR-ADM-004, HISYS-T-006, HISYS-T-020, HISYS-T-023, HISYS-FR-AGT-004, HISYS-DARS-CONTRACT-001 | `tests/unit/test_health_status.py`; `tests/unit/test_release_ops_cli.py`; CLI `hisys health-status` |
| `hisys.operations.release_readiness` | HISYS-T-024, HISYS-FR-ADM-001..004, HISYS-DATA-001..005, HISYS-CON-* | `tests/unit/test_release_readiness.py`; CLI `hisys release-readiness` |
| `hisys.security.secret_scan` | HISYS-T-021, HISYS-NFR-SEC-001..002, HISYS-FR-ADM-001, HISYS-R-008 | `tests/unit/test_secret_scan.py` |
| `scripts/scan_secrets.py` | HISYS-T-021 quality-gate script | `tests/unit/test_secret_scan.py` |
| `hisys.cli.main` | HISYS-PKG-ARCH-001 Section 3, HISYS-RUNTIME-DIR-001, HISYS-INST-INV-001, HISYS-T-001, HISYS-T-005A, HISYS-T-007..026, HISYS-T-030..032, HISYS-FR-INV-001..006, HISYS-T-024, HISYS-CON-010..012 | `tests/unit/test_cli_runtime.py`, `tests/unit/test_domain_cli.py`, `tests/unit/test_source_connector_cli.py`, `tests/integration/test_cli_hermes_runtime.py` |
| `examples/instance` | HISYS-RUNTIME-DIR-001, HISYS-HARNESS-GUIDE-001, HISYS-D-015, HISYS-D-016 | `tests/unit/test_example_instance.py` |

## End-to-end trace path tests

`tests/integration/test_trace_path.py` exercises the schema-level path required by
`HISYS-IMP-001` Section 4 and `HISYS-T-024`:

    SourceRegistryEntry
      -> HermesCollectionTrace + RawObservation
      -> ExtractedSignal
      -> ZettelMemo
      -> AlertDecisionRecord
      -> AuditEvent

`tests/integration/test_cli_hermes_runtime.py` exercises the runtime CLI path:

    validate-config
      -> collect SRC-HERMES-TOOL-001
      -> RawObservation JSON
      -> HermesCollectionTrace JSON
      -> Hermes Markdown boundary record
      -> Audit JSONL
      -> collection-report JSON/Markdown

For each path the tests assert:

- evidence and interpretation records are linked but distinct;
- Hermes hierarchical fields (campaign_id, hermes_parent_run_id, user_input_ref,
  delegated_task_id, tool_invocation_id, prompt_or_query_ref, tool_output_ref,
  boundary_record_ref, scope_policy_ref, approval_state) are populated;
- the boundary record path matches the controlled convention
  `hisys/runtime-boundary/hermes/<YYYYMMDD>/<campaign_id>/<record_kind>-<stable_id>.md`.

## Constraints honored here

- HISYS-CON-022..023: no live web/network calls in this code except the
  explicitly manual `smoke-source-connector` DOI metadata path, which is blocked
  unless an operator supplies approval and the matching manual-smoke env flag;
  CI tests use injected fake transports or local fixtures only. Live-A adds
  disabled live-source connector registry/dispatch/provenance records and Live-B
  adds dry-run connector planning plus local fixture-publisher evidence
  integration.
- HISYS-T-001..002: source registry entries require governance metadata;
  web/news collection is blocked without compliance checklist evidence.
- HISYS-T-003..006: adapter contract covers initialization, health,
  collection, normalization, provenance capture, bounded error records, and
  source failure isolation.
- HISYS-T-007..008: Investigator foundation and CLI glue collect from
  registered fixture sources, write RawObservation JSON records, skip
  unregistered sources, append audit JSONL records, persist Hermes Markdown
  boundary records for Hermes sources, and persist JSON/Markdown run summaries
  in the local runtime instance.
- HISYS-T-009..010: Extraction foundation and CLI glue create
  `ExtractedSignal` records from local `RawObservation` evidence references,
  preserve confidence/uncertainty/contradiction metadata, avoid raw payload
  copying, persist signal JSON under the runtime instance, and persist
  JSON/Markdown extraction reports.
- HISYS-T-011..012: Editorial foundation applies an active fixture
  `PerspectiveProfile`, rejects inactive perspectives, creates atomic
  `ZettelMemo` draft records with source/signal refs, confidence, tags,
  revision/review metadata, avoids raw payload copying, and persists runtime-
  local JSON/Markdown memo drafts plus memo draft reports.
- HISYS-T-013: Fixture duplicate/conflict review scans runtime-local memo
  drafts, flags duplicated summaries as `flagged_duplicate`, flags simple
  high-vs-normal source conflicts as `flagged_conflict`, rewrites reviewed memo
  JSON/Markdown records, and persists memo review JSON/Markdown reports.
- HISYS-T-026: Investigator direct memo foundation runs registry-gated fixture
  source investigation from a research topic/goal, applies
  `HISYS-TPL-RESEARCH-SEARCH-001`, persists linked `ExtractedSignal` records and
  `data/investigation-memos/<YYYYMMDD>/` `ZettelMemo` JSON/Markdown artifacts,
  writes `investigation-memo-report.{json,md}`, and keeps raw payload content out
  of the memo body by preserving only observation, signal, source, payload-ref,
  and payload-hash references.
- HISYS-T-027: Multi-agent Investigator research introduces explicit
  `ResearchTask` and `EvidencePackage` contracts, fixture research agents,
  evidence package validation/merging, and `investigate-memo --agent ...` so
  several subagents can research independently while the Investigator remains
  the only component that builds the final template memo.
- HISYS-T-028: Selenium/browser research remains disabled by default and
  is limited to a read-only harness using allowed domains/static local HTML,
  forbidden action gates, DOM/text/hash evidence capture, and
  `external_side_effects=false` package validation before any live browsing can
  be considered.
- HISYS-T-014..025: Chief Editor foundation reads runtime-local memo review
  reports and reviewed memo drafts, applies the fixture `HISYS-CE-POLICY-001`
  policy through a product factory selected by `config/chief-editor.yaml` or CLI,
  creates `AlertDecisionRecord` JSON/Markdown records for conflict
  escalation candidates, records duplicate memo non-escalations as suppressed
  decisions, suppresses repeated same-date alert candidates whose
  `suppression_key` already appears in prior non-suppressed alert decisions,
  requests human approval for high/critical or non-local target candidates while
  keeping `action_taken=none`, lets `analysis_only` close judgments with
  `target_channel=null` and no send candidate while `alert_delivery_dry_run`
  preserves the approval/action-plan path, persists alert decision JSON/Markdown reports,
  writes dry-run `AlertActionPlanRecord` JSON/Markdown records and action-plan
  reports with blocked reasons and approved pending `would_send=true` candidate
  markers while `live_delivery_permitted=false`, applies runtime-local
  approve/reject transitions to requested decisions while keeping
  `action_taken=none`, persists approval transition JSON/Markdown reports,
  writes disabled connector execution records/reports with
  `execution_status=blocked`, `live_delivery_permitted=false`, and
  `action_taken=none`, and performs no live alert sends or external connector
  actions.
- HISYS-T-023..024: I8-A/B DARS foundation creates runtime-local advisory
  `AgentHandoffPackage` JSON/Markdown artifacts from disabled connector
  execution evidence and returns loopback placeholder critique JSON/Markdown
  records because DARS is intentionally not implemented yet. The contract
  records `dars_backend=loopback_placeholder`, `external_call_made=false`,
  `allowed_actions=advisory_only`, and `action_taken=none`, persists
  `dars-critique-report.{json,md}`, and is shaped so a future DARS adapter can
  replace the loopback without changing downstream artifacts.
- HISYS-D-015: I4 persistence baseline is local JSON/JSONL, not a live database
  or external service.
- HISYS-D-016: Hermes foundation is collection-only and scoped to preapproved
  registered sources and Markdown boundary records.
- HISYS-CON-021: Python 3.11+ runtime baseline.
- HISYS-NFR-SEC-001..002: only fake non-secret tokens in fixtures.
- HISYS-DATA-005: Hermes provenance fields are non-optional in the trace
  record schema and validated at construction time.
