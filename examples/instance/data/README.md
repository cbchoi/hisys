# Runtime data directory

This example directory is intentionally empty except for this README. Actual
runtime outputs are generated under subdirectories such as:

- raw-observations/
- raw-payloads/
- hermes-traces/
- audit/
